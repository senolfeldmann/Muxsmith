/target
.superpowers/
gh-log.md

# session resume state: replace-in-place, snapshotted to
# docs/process-journal/artifacts/handoffs/ at plan closes; never committed live
/HANDOFF.md

# parallel SDD worktree streams
.worktrees/

# JS/Tauri frontend
node_modules/
dist/
src-tauri/gen/
src-tauri/binaries/

# Task 12: Playwright + the e2e mock harness build (never ships in the
# Tauri binary -- see e2e/vite.harness.config.ts)
e2e/.generated/
playwright-report/
test-results/
