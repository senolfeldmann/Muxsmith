{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window: file/folder pickers and clipboard-copy, the minimum the GUI needs beyond Tauri's core defaults. Extend deliberately as GUI features land.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "dialog:allow-open",
    "clipboard-manager:allow-write-text"
  ]
}
