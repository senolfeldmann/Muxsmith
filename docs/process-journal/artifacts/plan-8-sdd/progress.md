# SDD ledger — plan: docs/superpowers/plans/2026-07-23-plan-8-packaging-release.md

BASE (master at execution start): aec4cef
Streams: A=.worktrees/plan8-a (T1->T2), B=.worktrees/plan8-b (T3), C=.worktrees/plan8-c (T4), D=.worktrees/plan8-d (T5); wave 2 = T6 rehearsal on master after merges+push

Task 1: dispatched (stream A implementer, cheap tier, BASE aec4cef)
Task 3: dispatched (stream B implementer, mid tier, BASE aec4cef)
Task 4: dispatched (stream C implementer, mid tier, BASE aec4cef)
Task 5: dispatched (stream D implementer, mid tier, BASE aec4cef)
Task 1: report DONE (commit 7e36f96); review dispatching
Task 3: report DONE (commit c890b0f); cp-alias lesson ledgered; review dispatching
Task 5: report DONE (commit 92c62f1); review dispatching. Close item: the two ROADMAP "Ledger hygiene" rider bullets get their DONE note at plan close (T5's controller item 1).
Task 4: report NEEDS_CONTEXT (commit 9449447) - design section-2 YAML unparseable (line 1334, unquoted scalar with ": "); controller ruling: Option A (quote the value, byte-identical text) via four-eyes design amendment (resumed author + delta review); T4 resumes after. Concern 2 (bundle permissions implicit via workflow default) controller-adjudicated: leave as written, matches D83's stated behavior; task review may still flag.
Task 4: design amendment A1 committed (d21a19f); design delta review running; T4 implementer resumes after approval
Task 1: minor (deferred, report-side, accepted): m1 warm-cache evidence grep - claim proven true by reviewer; ledgered as build-recheck-forces-rebuild
Task 1: observation (deferred to whole-branch triage): i3 - no acceptance observable reads a BUNDLER-produced version (R9 reads clap); tauri-build propagation confirmed in pinned crate sources
Task 1: complete (commits aec4cef..7e36f96, review clean - both verdicts APPROVED)
Task 2: dispatched (stream A implementer, mid tier, serial after T1 on plan8-a)
Task 4: implementer resumed post-amendment-A1 (design delta APPROVED)
Task 5: minor (deferred, whole-branch triage): yaml.ReaderError escapes the parse try as traceback (pre-existing on master; inline-wrapper-keeps-try-scope ledgered)
Task 5: minor (deferred, whole-branch triage): BUILDING.md:92-96 CI-jobs enumeration stale once T5 merges (names cargo deny check as THE independent job; one-line update owed, stream-A file)
Task 5: complete (commits aec4cef..92c62f1, review clean - both verdicts APPROVED)
Task 4: report DONE post-A1 (single amended commit fe46424); review dispatching
Task 2: report DONE_WITH_CONCERNS (commit c884bd6); review dispatching
Whole-branch item (consolidated): BUILDING.md update pass when plan-8 merges - (a) :65-68 "tauri build out of scope / not in CI gate" wrong once 4.4 + release.yml land (T2 concern 1); (b) :92-96 CI-jobs enumeration names cargo deny check as THE independent job, stale once T5's lint job lands (T5 review minor). One doc hunk, whole-branch fix wave.
Task 4: complete (commit fe46424 single amended, review clean - both verdicts APPROVED; permissions concern upheld on the merits; cross-stream consumption contracts verified by the reviewer)
Task 2: complete (commits 7e36f96..c884bd6, review clean - both verdicts APPROVED; Q1: BUILDING.md untouched was correct, defect in brief authoring)
Correction to the consolidated BUILDING.md item: T2's :65-68 - only the "out of scope for local development" half goes wrong; "not part of the CI gate" STAYS TRUE (release.yml is not the gate). T5's :92-96 item unchanged.
Merging stream A (plan8-a: T1+T2) into master; full nine-part gate follows.
Merge A landed (b093c3e); nine-part gate RED at cargo test joblog collision test - root-caused NOT the merge (fails identically at pre-merge base): calendar time bomb (fixed 20260710 stamps aged past D35's 14-day prune). Standalone fix dispatch on master; gate re-runs after.
Joblog date-bomb fix: DONE (c06b8dd, 12/12 + workspace 494 green; sweep 33 hits classified 6/3/24, exactly one must-survive site). Harness security warning = documented SI-4 false-alarm class; commit content-verified. Review dispatching. Whole-branch note: four sites pass stale stamps as create's run_id ARGUMENT, safe via the implicit prune-before-leaf invariant (report concern 3) - comment-note candidate on next touch.
CORRECTION to the joblog-fix sweep figures above: 34 hits across 4 files (not 33/3) - e2e/smoke.spec.ts:481 was missed by the false-empty non-Rust sweep; verified benign (mocked IPC, no filesystem). Commit message carries the stale figure (immutable, no rewrite); verdict + this line are the record.
Joblog fix: complete (c06b8dd, review APPROVED with the sweep-figure Medium routed to the surface-entry promotion). Full nine-part gate re-running.
Merge-A gate re-run post-joblog-fix: GREEN (exit 0, all nine parts). Merge chain holds for T3 review (fixed order A,B,C,D binding). Push deferred to post-D per plan choreography.
CORRECTION: the T3 review was never actually dispatched after the package was built (the 'review dispatching' line above stalled unexecuted; controller re-read the ledger and caught it). Reviewer dispatched now. Merge B holds on it as before.
Task 3: complete (commit c890b0f, review clean - PASS/PASS; 1 pre-existing LOW = D75 cross-ref "section 1" vs section 0:51 -> plan-close design one-liner batch; 9 wording items -> owner rendered-surface pass, list in task-3-verdict.md)
Merging stream B (plan8-b: T3) into master; gate follows, then C, then D.
Stream B merged (fcb0795); gate GREEN. Merging stream C.
Stream C merged (cd9c56a); gate GREEN. Merging stream D.
Stream D merged (2afa39d); gate GREEN. Sanity runs OK (version-sync 0.1.0, ledger-lint 464). Pushed 9d0fc45..2afa39d; CI run 30260189439 watch running (in-CI ledger-lint green evidence). Wave 2 (rehearsal) dispatches after green watch.
CI watch RED: windows-2025 clippy unused-import (pre-existing since plan-7 900db87, five failure runs unobserved). Fix dispatched; rehearsal holds until a green watched run.
Windows-clippy fix: APPROVED (f4f932e). Whole-branch candidate from its harvest: local --target x86_64-pc-windows-msvc clippy as pre-push gate part (class recurred despite in-tree naming comment; a comment is not a gate) - owner ruling at plan-8 close.
CI run 30261258685 (dbd0dc3): SUCCESS, watched. Wave 2 (rehearsal) dispatching.
Task 6 (rehearsal): three consecutive 529-overload kills before any GitHub dispatch (verified: no release.yml run exists). Backoffs 3min + 10min; final resume attempt after second backoff, else session cut with rehearsal as the follow-up session's first action.
Task 6 (rehearsal): BLOCKED, evaluated (run A 30263340264 failure: both Windows legs die in WiX light; guard/macos/linux green; 2/10 R full-pass, partials documented; run B correctly not dispatched - decision memo in task-6-report.md). Open forks for next session, in order:
 1. Confirming dispatch: surface light.exe stderr (one workflow-debug dispatch) to verify the U+015E/Windows-1252 hypothesis.
 2. If confirmed: design amendment on D86 (fallback trigger gated on R8-rendering is unreachable when the char breaks the BUILD; copyright carries the same char) + OWNER ruling - the publisher string is Şenol's name, transliteration is his call.
 3. Owner authorization pending: sudo dnf install dpkg msitools for R6's local inspections (system-state change, his call); alternatives ar/7z/bsdtar exist if he prefers a substitution ruling.
 4. Draft-cleanup ruling RECORDED: the plan text governs (owner deletes at close); the controller dispatch line saying otherwise was the error, caught by the implementer.
 5. R1 observable wording: one-line alignment (ledgered).
Whole-branch review for plan 8 runs AFTER the rehearsal reaches its green (post-fix re-run).
OWNER RULINGS (2026-07-27, in-session): (6) Ş/Windows = option (a) NOW as post-plan fix - debug first (stderr capture + ASCII A/B on a debug branch), then ONE bounded UTF-8/codepage attempt, ASCII fallback "Senol Feldmann" in msi METADATA ONLY on demonstrated failure (all other artifacts keep Ş); owner-ruled cascade, not implementer latitude. (7) cross-target windows clippy as tenth gate part APPROVED - BUILDING.md task lands in the plan-8 whole-branch fix wave. R6 tools verified present (dpkg-deb + msiextract) - resolved. Memory commit: owner does it himself post-session.
Ş/WiX fix: FIXED via technical route (07c0255: wix language map + locale-en-US.wxl Codepage 1254; both Windows legs green on proof run 30269169772; MSIs binary-inspected, Ş intact, zero fallbacks). Diagnosis run 30268008932 captured LGHT0311 verbatim AND killed the fallback branch empirically (three sinks, not two). Residuals for plan-8 close one-liner batch: (a) upstream cosmetic - license RTF header hardcodes ansicpg1252, dialog rendering of Ş unverified without a Windows install; (b) citation correction - pinned bundler is 2.9.4 (2.11.4 is the CLI version) - sweep the design/verdict mentions. Review dispatching; after APPROVED: push + watch, then rehearsal re-run.
WiX fix review: APPROVED (wix-fix-verdict.md; citations line-accurate, merge behavior double-verified, MSIs re-downloaded and re-verified PID_CODEPAGE 1254 / Ş intact).
CORRECTION to the wix-fix report: "raw UTF-8 Ş = 0" is wrong - 89 (x64) / 102 (arm64) whole-file, ALL inside the compressed CAB, zero in the metadata region, real mojibake 0; substance holds on the metadata-region basis (verdict F1).
Plan-8 close one-liner batch additions (verdict F2): design :958/:1012/:1511/:2007 + plan :248/:262 still carry the pre-fix "language": ["en-US"] list and know no locale-en-US.wxl (change owner-authorized, ruling 6); plan :718 trigger 7 premise stale; plus the earlier bundler-2.9.4 citation sweep and the ansicpg1252 cosmetic residual.
Pushing fix; rehearsal re-run follows.
Task 6 (rehearsal) RE-RUN: DONE - runs 30272619000 (A) + 30273529210 (B) both success, all jobs green; R1-R7+R9 PASS with cross-run positive controls beyond the design's ask; R8 (draft inspection) + R10 (draft deletion) pending OWNER by design - draft "rehearsal-30273529210" preserved as his R8 input. Close-batch additions: R6 deb-payload path form doc one-liner (dpkg 1.23.7 emits usr/bin/... without leading ./); D86 publisher-fallback SUPERSEDED ON THE MERITS (code page is the instrument; ASCII-izing provably insufficient) - design bookkeeping line; R1 gate-green echo names SHA not run id (already ledgered).
Task 6: complete (no repo commits by design; the acceptance test is the deliverable). Plan-8 remaining: whole-branch review + plan close (next session) + owner R8/R10.
Gate/CI-watch logs secured from /tmp scratchpad to .superpowers/sdd/plan-8/gate-logs/ (11 files; progress.md cites them; /tmp dies with reboot).
CONTROLLER, from the plan-7.5 close blocked-pool sweep (S23, 2026-07-27) - three items owed at the PLAN-8 close, recorded here so they survive the sweep that found them:
 (a) ci-13-packaging-deferred (process-conventions.yaml) is STALE by the sweep test: blocked_on names "Plan 8" and Plan 8's work has landed (release.yml builds msi/dmg/deb/rpm/AppImage). Resolve at the plan-8 close: settled + a resolving occurrence citing the merge commits. Its statement also carries a stale premise ("while the repo is private" - public since 2026-07-10), which the resolution rewrites.
 (b) ci-16-mise-not-ci (decision-ledger) earned a reinforcing occurrence from D85: the release legs deliberately install node/pnpm via pinned setup actions and run no mise. The entry stays blocked (ci.yml still uses mise-action; removal is the v1.x ROADMAP item), but the occurrence is owed.
 (c) proc-subagent-commit-trailer-set (decision-ledger, blocked on "the next commit-policy touch") gained a second facet: both plan documents mandate the trailer string "Co-Authored-By: Claude Fable 5" verbatim, and the S22 tiering re-instantiation makes that wrong for every Opus-5-tier implementer from now on. Measured on master: of five recent subagent/controller commits (406e91b, 07c0255, c06b8dd, f4f932e, fe46424) all five carry Co-Authored-By and exactly one carries Claude-Session. Owner question at the plan-8 close.
WHOLE-BRANCH REVIEW (top tier, path-scoped aec4cef..7302e1b): NEEDS FIXES - 0 blocker, 1 important (ledger-lint ReaderError escape, and the record's "pre-existing on master" qualifier disproven: introduced by 92c62f1), 3 minor. 13 adjudications ruled; verdict whole-branch-verdict.md.
Fix wave dispatched and complete: ecab53a (ledger-lint try-scope + docstring trigger claim), bcb67f3 (BUILDING.md two stale sites + the owner-ruled tenth gate part, superseded comment section deleted), 6d81738 (design amendment A2 + the four language sites + fallback markings + R1/R6/D75, plus the plan's two supersession lines). No premise refuted. Report fix-wave-report.md.
Controller rulings on the two returned forks: frozen plan copies STAY frozen and get a supersession line (uniform with the plan-7.5 close rule, so the house has one pattern); the Windows clippy target is a documented BUILDING.md prerequisite, NOT a rust-toolchain.toml targets key (the gate is run by whoever pushes; the key would cost every contributor a Windows std they never need).
DELTA RE-REVIEW (resumed original reviewer): READY. All 18 edit sites discharged, the ledger-lint fix confirmed as code by independent reproduction of both states, design section-2 fence still byte-identical to release.yml, and the 3.1-fence-vs-shipped-config divergence closed. F1 (ci.yml cites the deleted BUILDING.md section title) ruled record-with-vehicle, gated on the next ci.yml-touching change, exact replacement text carried in the ROADMAP v1.x mise-removal entry. F2 (A2's bare :line refs) stands - backward-looking provenance, each paired with a content anchor.
Close-batch, controller (item E), status: inline-wrapper-keeps-try-scope occurrence corrected (the false "pre-existing" qualifier); test-fixture-dates-outside-retention-windows +1 occurrence for the four run_id-argument sites (adjudication 11); adjudication 8 residual (b) CLOSED by verification - the reviewer's sweep found zero live bundler-version miscitations, so the recorded citation sweep is done-by-verification, not by edit; the design's nine triggers mirrored into ROADMAP Triggers; both Ledger-hygiene riders marked DONE with commit 92c62f1; the tenth gate part promoted to tier 2 (gate-includes-cross-target-lint-for-the-unrun-os); F1 recorded on the mise-removal entry. R8 addendum (license-dialog rendering of the publisher's S-cedilla during the Windows install) handed to the owner in session. Remaining: the owner's wording pass + its review, then R8/R10 and the plan close.
Owner rendered-surface pass: the nine wording items were put to the owner with a per-item recommendation; he ruled eight as recommended and reserved item 7 (release-body continuation lines) for his own look at the rendered draft. Applied in e477e37. TWO CONTROLLER-BRIEF PREMISES REFUTED by the implementer, both correctly: (i) the brief's macOS claim - I asserted sha256sum is absent from macOS; Apple's own md5(1) man page lists the *sum aliases with GNU-mode -c on 13/14+, while the 2020 capture of the same page lists none, so the truth is a bracket (absent on 11/12, present on 13+) and the install docs claim macOS 11+ support. Implementer withheld the clause and recommended shasum -a 256 instead of inventing an introducing version. Owner ruled: add it in the shasum form. (ii) the brief's Windows artifact name muxsmith-<version>-windows-x64.msi exists nowhere; the scheme is windows-x86_64, swept with a positive control. Fourth controller-brief defect of the session, all four found downstream.
Wording pass review: APPROVED (e477e37 + 4716b0c), 1 MEDIUM + 4 LOW. MEDIUM: the documented checksum command fails loudly for the ordinary single-artifact download - six FAILED-open-or-read lines and exit 1, indistinguishable at a glance from the tampering it exists to detect; the rehearsal missed it because R4 downloads all eight assets. Three factual claims independently re-verified by the reviewer (coreutils/shasum/Get-FileHash; glibc 2.35 from the runner label; and the one-file-three-commands format equivalence, checked by running the unmodified upstream shasum against a GNU-produced SHA256SUMS with four malformed controls).
Wording fix round: e61264f (--ignore-missing on the Linux and macOS commands, case-insensitivity clause for Windows, generic <file> placeholder, corrected PATH claim for the tar.gz) + 645faf0 (design amendment A3 recording that the owner pass superseded section 4's verbatim blocks; section 4 byte-unchanged, pointer line at its head). Tamper control run as required: a tampered file under --ignore-missing still reports FAILED and exit 1 on both implementations, the green state is reachable, the defect reproduces without the flag, and a misspelt flag is rejected - so the option is proven parsed, not decorative. Implementer recount: EIGHT divergent regions, not the brief's seven; it also swept the design status line whose amendment enumeration had gone stale at A2 (a reference over a set it was changing) and routed rather than touched a pre-existing nested-fence defect inside the frozen section.
Fix-round delta re-review: APPROVED. The reviewer re-ran the whole tamper matrix on its own instrumented shim, confirmed the AppImage paragraph true of all four formats it names, verified section 4 byte-identical across all seven fenced blocks with a mutation control, and ruled its own site count wrong for a sharper reason than the report gave (its "five hunks" reproduces at no context value against either tree; it had written down the count of ruled edits instead of reading the diff headers).
PROCESS FINDING, ledgered as reviewer-instruments-live-where-the-implementer-cannot-write: the reviewer's first re-run executed the IMPLEMENTER's shim, because both agents wrote to the same session scratchpad path minutes apart. Caught only because an error string changed. Independence of context is not independence of tooling.
Close-batch advisories from the delta, vehicle = the next design-touching change (same handling as F1): tighten A3's clause about the reviewer's figure and close the other half of its region mapping (descriptions 1 and 2 share a region, 5 spans two); two report-internal citations that do not reproduce (:91 should be :92; the 4.4 fence size). Recorded baseline: all four remaining `-c SHA256SUMS` instruction sites now carry the flag.
PLAN 8 CLOSED 2026-07-27 (session 23). This is the last .superpowers write for plan 8; the salvage follows it, per salvage-is-the-last-close-write. Owner steps R8 (inspect the draft release, incl. the license-dialog rendering addendum) and R10 (delete the draft) remain OPEN and are his by design - recorded as open user actions, not booked as done.
