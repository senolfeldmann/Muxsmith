# SDD ledger — plan: docs/superpowers/plans/2026-07-27-plan-8.5-macos-packaging-fixes.md

BASE (master at execution start): f627105
No worktrees by plan decision: tasks 1-4 are strictly serial and commit directly on master.

Plan authored four-eyes: author (top tier) c2514e7, review round 1 NEEDS FIXES (1 major + 5 minors), fix round ff05ac3, delta APPROVED. The major was a post-sweep absence check that claimed a neighbouring grep as its fire-verification: run with its own invocation it returned 4 of 5 sites, blind to two by construction, so an implementer skipping either would have reported green. The delta reviewer verified the fix by simulating each omission rather than reading the step.
Task 1: dispatched (cheap tier, BASE f627105)
Task 1: report DONE (commit 9460daf, 7 files - the tracker first said 8; corrected per the whole-branch verdict N2, which measured 7 by numstat and noted the Task-1 report itself said seven); no premise refuted, every content anchor matched. The three-part closing check worked as designed: (a) 4 -> 1 (the survivor is the ROADMAP blocker entry's own quoted-wording line, a legitimate keep), (b) README 1 -> 0, (c) ROADMAP presence 0 -> 1, each fired by its own invocation pre-edit. The config red-state was fire-verified (invalid signingIdentity -> exit 1 with the predicted error, restored alias-proof, re-verified green). Review dispatching.
Task 1 review: NEEDS FIXES, one major - the design's D75 decision body still carried "unsigned and not notarized" as LIVE prose (measured outside any fence). The plan's own Step-4 classification table NAMED that site and Step 5 gave it no edit: classified, never commissioned. It contradicted INSTALL.md in the same commit and falsified, by one site, the completeness sentence that same commit inserted seventeen lines above it. The implementer was right not to touch it unbidden; Step 4 should have returned NEEDS_CONTEXT. Reviewer's sweep accounting: 308 tracked hits bucketed to sum exactly, 52 on the live surface, exactly one uncovered.
Task 1 fix round: complete (commit 5060ef5, one file). Bullet corrected to INSTALL.md's substance, A4 extended to name the site, count recompute run and found no numeral over the affected set. All four report-figure corrections confirmed by own measurement, none pushed back. Unprompted: located the only other tree occurrence of the old wording, confirmed it sits inside a section-4.1 transcription fence already covered as superseded, and left it - so the completeness claim now rests on a measurement rather than a re-assertion. Delta re-review dispatching.
Task 1 delta re-review: NEEDS FIXES on one MINOR, explicitly a round-1 residue rather than a delta regression. All four routed items verified clean: the corrected bullet is byte-identical to INSTALL.md:52's substance minus the label the bullet subsumes (both comparisons carrying negative controls), the count-recompute absence holds for the set swept (the reviewer fired its instrument on five real counts in the same file before trusting the empty result), and the fenced occurrence is genuinely covered - A4 names it at line level, A3 forbade editing it, fence corpus 14/14 identical.
The residue: the design's own status line enumerates the amendment log as "A1, A2 and A3" while the log has carried four entries since 9460daf. Fired against the pre-edit blob, where it matched exactly - so it is a maintained reference that stopped tracking, not decoration. The reviewer missed it in round 1 too and says so. Its forward note is the valuable part: Task 2 appends A5 to the same log, so the identical staleness recurs at the next commit unless the line is named as a consuming site.
CONTROLLER RULING: do not maintain the enumeration - REMOVE it. The header states the log's location, and the log states its own contents; an enumeration in a header over a growing set is a dependency that must be visited on every append, and it has now gone stale twice in one day on this same line (the plan-8 wording round fixed its first instance). Replacing it with a pointer kills the class instead of scheduling its next maintenance. Verified before ruling: `git grep` finds no consumer of the enumeration anywhere in docs/.
Task 1: COMPLETE (commits 9460daf + 5060ef5, review clean after the fix round). The one open minor is ROUTED INTO TASK 2 as a binding instruction - same file, same amendment log, and Task 2's reviewer grades both. House precedent: a finding whose file belongs to a follow-up task travels verbatim into that task's dispatch rather than spawning its own cycle.
Task 2: report DONE (commit 50e08cd, 3 files). Experiment re-run on this machine with the repo-pinned CLI: baseline deb Recommends count 1, with a null-carrying platform config 0, Depends intact as the positive control, control-archive line count 12 -> 11 so exactly one line went. Third independent reproduction of the clear-branch result (plan author, plan reviewer, now the implementer); no NEEDS_CONTEXT trigger fired and the fallback RTF was never created. Edits: the macOS overlay carrying licenseFile null, the BUILDING.md paragraph, amendment A5, and the routed status-line cut. Nothing claims ruling 2 verified - RR4 is the machine half, O2 the acceptance.
CONTROLLER CLAIM CORRECTED by the implementer: my "git grep finds no consumer of the enumeration anywhere in docs/" was imprecise - there ARE hits under docs/process-journal/artifacts/**, all dated historical records narrating the earlier edit, which is the class the Task-1 sweep declares out of surface. None reads the enumeration as current, so the ruling stands, but the claim as phrased was wrong and the implementer said so rather than letting it pass.
Task 2 review: NEEDS FIXES - one major, two minors, plus three measurements the reviewer added that neither plan nor report carried. The most valuable: the clear SURVIVES D82's -c overlay. release.yml applies both the platform file and the -c flag file on the macOS leg, and had -c been computed against the base config rather than the merged one, the fix would have been void at release time while every local check stayed green. Measured: both -> Recommends 0, -c alone -> 1. Also: RFC 7396 delete semantics re-established without relying on A5's source citation (a non-nullable key rejects 123 and accepts null), and macOS-only selection verified in BOTH directions with a firing positive control, which the plan's own Step 8 could not give.
MAJOR: adding A5 to an enumerated set fires proc-normative-count-recomputed trigger 2, and the sweep ran over the routed line's phrasings rather than over the SET. Two live sites, both in the executing plan document: its Global Constraints line stating the governing set as "D75-D90 + A1-A3" (now A1-A5, and A5 is the amendment governing the file this very commit created) and one further site. A4 staled them first and Task 1's whole review chain missed them, so Task 2 is not the origin - but it was executing a ruling whose stated purpose was this exact class. Remedy is routing, not fixing: the plan file is outside Task 2's Files list.
CONTROLLER RULING, same cure as the design header: the plan's enumeration of the governing set is replaced by a POINTER, not maintained. Three instances of this class in one day on two files is enough evidence that maintaining an enumeration of a growing set loses to removing the dependency. Routed to the resumed plan author (the controller does not author plan content), delta judged by the resumed plan reviewer.
Status-line cut ruled sound: only lines 3-4 changed across 2174 shared lines, prefix and suffix byte-preserved, pointer target present exactly once - and the same commit appended A5, which would have staled the enumeration a third time, so the cut is vindicated inside its own diff. The controller's "no consumer" claim was false as phrased and true in substance; every hit is a past-tense evidentiary record, and re-pointing those is forbidden rather than merely unnecessary.
Task 2 minors: the overlay is five lines, not four; and `tauri inspect` does not validate platform overlays at all, so the report read stronger than its check.
Plan amendment (Task-2 review routing): commit 29ef17b. Both sites took the pointer; the author's sweep confirmed the reviewer's count of two exactly, and it went one step further than the ruling by dropping the D-range from the normative line as well, on the argument that the ruling's rationale covers ANY membership list of a growing governing set and that "its D-entries" loses nothing once the pointer names the file. Site 2 (Task 1's read list) took the pointer with a reason worth keeping: a reader there already has the design open, the log is one terminal section, and "read it in full" is operationally stronger than a member list precisely because that task is itself an appender.
INCIDENT, disclosed by the author and repaired: its first commit attempt (86bfd69) swept in the concurrently-running Task-3 implementer's staged file, because a bare `git commit` commits the whole shared index even when you staged only your own path. Repaired by soft reset plus a pathspec-scoped re-commit; controller verified 29ef17b touches one file, 86bfd69 is unreferenced, and Task 3 committed its own work afterwards (87c1dee). Ledgered as concurrent-writers-need-pathspec-scoped-commits, and folded into the doctrine's parallelize boundary as the MECHANISM reason that one-tree parallelism is not made safe by staging discipline alone.
Task 3: report DONE (commit 87c1dee); review dispatching.
Plan amendment review: APPROVED. Two sites measured at the parent and zero at HEAD, with no third stale enumeration left. Site 1 was judged not merely correct but STRONGER than what it replaced: it gained a temporal clause ("at the log's state at execution time") on exactly the axis the enumeration kept failing, and membership now resolves by opening one named file, which is observation rather than the inventable latitude the latitude rule bars. Site 2's three supporting arguments each checked out.
One honest nuance the reviewer raised without making it a finding: the author's extension to the D-range was RIGHT in outcome but over-broad in rationale - the D-set is not in fact growing, since this house lands changes as A-entries and D-numbering is fixed at approval. The defensible ground is different: naming the file loses nothing (measurably, "its D-entries" is exactly D75-D90 today), and one execution-time contract for both halves beats a frozen half beside a live one.
Also confirmed: the amendment cut exactly the membership-enumeration kind and left the member-citation kind alone - Task 2's own read-list naming A2 and A3 is a content pointer, not the ruled class.
Task 3 review: APPROVED (commit 87c1dee). Committed blob byte-identical to the plan's carried post-state, with four negative controls fired including two whitespace-only ones that a `diff -w` would have passed. Table byte-identical (same sha256), __VERSION__ 8/8 counted as occurrences rather than lines because grep -c would hide a doubled token, prose stream identical at 149 words both sides so the joins changed structure only. The reviewer also simulated the COMPOSED release body (banner plus version substitution, version recomputed from Cargo.toml) rather than judging the template alone, and reasoned the fix mechanism-neutrally: the joined line has neither an intra-region newline nor a line-initial pipe, so both candidate rendering mechanisms lose their trigger. It explicitly did not claim ruling 3 accepted.
Index-race end state closed by measurement: `git diff 86bfd69 87c1dee` is EMPTY, so the transient and final trees are identical and nothing was lost in the repair; 86bfd69 unreferenced with a fired control.
Task 3 minors, both report-prose only, DISPOSITION = recorded, not dispatched: the report's "12 lines removed" contradicts its own "3 insertions/14 deletions" (measured 14 removed, 3 added, net 11), and it promises a verbatim diff it then paraphrases. Neither touches the artifact, and the reviewer's verdict - salvaged alongside the report - carries the correct figures, so the durable record is self-correcting. A dispatch for two prose lines does not earn its round-trip at this point in the session.
Tasks 1-3 COMPLETE. Task 4 preconditions are controller work: ten-part gate, push, green ci including ledger-lint. Gate running.
Controller preconditions for Task 4: ten-part gate run in full - RED on first run at cargo test (one non-reproducible failure in dry_run_cli, green alone and in three further workspace runs, exit 0 / 0 FAILED / 39 suites with the FAILED pattern fire-controlled against the red log), then GREEN on a complete re-run of all ten parts. No Rust changed in this plan (measured: zero .rs/Cargo.* paths in f627105..HEAD) and CI had run the identical Rust green hours earlier, so the plan did not introduce it. Owner call: flakiness is not accepted as a standing condition - recorded as a ruled 1.x fix with its candidate named and explicitly labelled as removal of a known race class rather than a confirmed fix for an unestablished cause. Pushed 8ad4392, ci run 30312472265 SUCCESS including ledger-lint.
Task 4: report DONE, no commit by design. Run 30312889098 success, 6/6 jobs, one dispatch. RR1-RR6 all green with fired controls throughout: RR1's checksum control flipped a byte and watched that one file fail, RR2's two absence checks each fired against the pre-Task-3 template (2) and the committed template (8), RR4's planted-copy control fired at 1, RR6's ls-remote control fired.
The two DIFFERENTIAL measurements are what make this evidence rather than assertion: on the R8 defect dmg the same invocations return 0 CodeResources and 2 LPic, against the new artifact's present-seal and 0 - so the checks are shown to discriminate, and the plan's recorded LPic=2 measurement is reproduced exactly while that artifact's retention lasts.
Task 4 flags, neither a blocker: (a) local master was one commit ahead of origin at dispatch (61dc522, ROADMAP only) and the run's headSha came back 8ad4392, the exact SHA ci proved green - flagged so "master was ahead" is not later misread as an unverified base; (b) plan-text discrepancy - RR1's updater-artifact grep printed 8 where the plan expects 4, decomposed by two disjoint invocations into 4 runner echoes of the unexpanded script source plus 4 executed outputs, fully accounted, substantive condition holding per leg. Correctly not escalated: the check was evaluable and its cause measured. Worth correcting if that check is ever re-transcribed.
Nothing in Task 4 claims rulings 1, 2 or 3 verified. O1-O4 restated verbatim in the report and pending the owner.
Task 4 review dispatching.
OWNER ACCEPTANCE 2026-07-28: "passt nun alles auf dem mac". O1 (the install shows the unidentified-developer path with Open Anyway instead of "damaged") and O2 (the dmg mounts with no pre-mount license dialog) are ACCEPTED on real Apple-Silicon hardware by the owner. These are the acceptances the machine halves RR3 and RR4 could only half-carry; rulings 1 and 2 are therefore verified, and the two 1.0 blockers found by his 2026-07-27 walk-through are closed by observation rather than by configuration diff.
Owner also suspended the context-budget session cut for this session: the plan closes here.
STILL OPEN and NOT booked: O3 (the rendered draft body on github.com showing the three OS links on one line) - his wording scoped the acceptance to the Mac, and O3 is a browser observation on the release page, so it is asked rather than assumed. O4 (delete the draft) remains his. Also unconfirmed: whether INSTALL.md's macOS section matched what he actually clicked, which is the second half of O1's duty.
OWNER ACCEPTANCE COMPLETED 2026-07-28: "ja, passt beides" - O3 (the rendered draft body shows the three OS links on one line) ACCEPTED, and the second half of O1's duty is discharged: INSTALL.md's macOS section matches the flow he actually walked, so no owner wording correction rides the close. All three rulings are now accepted at their acceptance surface rather than at their machine half. Only O4 remains, and it is his by design: delete the rehearsal draft "REHEARSAL - not a release (run 30312889098)".
Task 4 review: APPROVED, no major. Instrument independence taken seriously: every measurement in a freshly minted mktemp dir, all assets re-downloaded from GitHub, nothing read from the implementer's scratch. RR3 differential 0 -> 1 CodeResources, RR4 differential 2 -> 0 LPic with STR# and TEXT moving the same way, and the reviewer's R8 numbers reproduce the plan-review round-1 record taken on a different day with a different instrument.
THE FINDING THAT VALIDATES THE PLAN'S CHOICE OF CHECK: the reviewer extracted the seal and read it (a genuine codesign plist with files/files2 hashes, cdhash and requirement), then parsed the Mach-O load commands of BOTH GUI binaries and found that both the new AND the R8 defect binary carry LC_CODE_SIGNATURE with a valid blob - because the arm64 linker always ad-hoc signs the binary. A binary-level signature check would therefore have PASSED on the broken artifact. The bundle seal is the only discriminator at this layer, which is exactly the check the plan picked. It also reconstructed the body's deterministic region locally and found it byte-identical to the live draft, direct evidence the body was not hand-edited.
FLAG 1 ruled a note, not a defect - but with a sharp qualification worth keeping: the safety was CONTINGENT on which file the ahead commit touched. The same state with release collateral ahead would have produced a silent mismatch between the dispatched ref and the ci-verified SHA. Cure named: a dispatch precondition requiring zero commits ahead of origin.
FLAG 2 ruled a REAL DEFECT, in the plan text rather than the artifact, and traced to its origin: plan 8's R5 asserts NO count - it is a per-leg presence check - and plan 8.5 converted it into an aggregate count of LEGS, then silently reused that as a count of LOG LINES, asserted from drafting intent and never measured. The runner echoes each step's script source into the log group, so 4 legs yield 4 echoes plus 4 outputs. Recommended fix is to restore the presence shape rather than patch 4 to 8, since a hardcoded 8 inherits the same fragility.
F2 (plan text): Step 1 prescribes a single foreground `timeout 5400` that this harness cannot execute at all (its Bash call caps below that). The implementer's disclosed two-chunk split was the only compliant execution. Two consequences for any future transcription: the stop rule ("a tripped timeout stops the task") is ambiguous under chunking and wants restating as a total elapsed budget, and the chunked form belongs in the text or the next implementer reaches for backgrounding - which the same constraint bans.
F1/F3/F4: report-prose only (O-steps described as "verbatim" while paraphrased, one borrowed gate phrasing stronger than its source, one backticked log line dropping literal padding). Disposition: recorded, not dispatched; the verdict carrying the corrections is salvaged beside the report.
WHOLE-BRANCH REVIEW (top tier, range f627105..61dc522, nine commits, 11 files): READY. No major, nothing blocking. All seven dimensions pass with measurements rather than assertions: cross-task integration measured on master, all three rulings and the INSTALL re-verify duty with named implementations and owner acceptances, ZERO surviving latitude (the stale-enumeration sweep returns 0 live hits and fired 3 at BASE), every no-work-needed premise reproduced, all cited house ids resolving, the supersede-never-overwrite rule applied correctly at all four supersession sites, and no live surface still calling the macOS app unsigned.
Roll-up funnel, nine items, all classified: the zero-commits-ahead precondition and the total-elapsed-budget rule became house entries; the presence-check-turned-count became an occurrence on proc-normative-count-recomputed with the recorded fix being to restore the presence shape rather than patch the number; the retired plan is NOT amended for either, since a retired plan is history and the durable home is the house files; report-prose minors across tasks 2, 3 and 4 are recorded, their verdicts salvaged beside the reports so the archive self-corrects; the controller's "no consumer" phrasing was already corrected in place; O4 routes to the owner; the flaky test needs no change (owner-ruled 1.x, candidate named, trigger registered); and the R8 artifact's retention expiry needs none, since the differentials are recorded.
Blocked-pool sweep across all four house files: 23 blocked entries (24 at the plan-8 close, minus ci-13 settled there). No blocked_on cleared and no deferred work landed regardless - the four Plan-9 items wait on a plan that has only been reconnoitred, not executed. ONE item surfaced for owner disposition: proc-subagent-commit-trailer-set is blocked on "the next commit-policy touch", and this session made two commit-policy-adjacent rulings (the standing-grant restatement and the pathspec-scoped commit rule), so its condition is arguably met.
Tracker correction per whole-branch N2: Task 1 touched 7 files, not the 8 this tracker first recorded.
PLAN 8.5 CLOSED 2026-07-28. This is the last .superpowers write for the plan; the salvage follows it, per salvage-is-the-last-close-write.
