# Task 3 verdict: D71 topic content - `editor-tracks-rules`, en + de

- Reviewer: independent, mid tier. Read-only on the tree; this file is the only write.
- Reviewed: branch `plan75-b`, commit `29952b9` (worktree `/home/senol/Git/Muxsmith/.worktrees/plan75-b`), base `1d82179`.
- Inputs: `task-3-brief.md` (the requirements), `task-3-report.md` (claims, graded as claims), `review-1d82179..29952b9.diff`, `implementer-preamble.md` (binding).
- Ground truth consulted directly: design `docs/superpowers/specs/2026-07-22-plan75-track-rule-add-remove-design.md` D65 (:217), D66 (:269), D67 (:329), D69 (:428), D71 (:556); D41 (`docs/superpowers/specs/2026-07-15-plan-6-design.md:122`); `docs/conventions.yaml` `help-topic-h1-scheme` (:1033), `code-comment-line-citations-drift` (:1012); `docs/product-boundaries.yaml` `core-83-zero-rule-keep-passthrough` (:389), `editor-generic-action-keys` (:404); `scripts/check-i18n.mjs`; both topic files and their siblings; `src/views/EditorView.vue`; `e2e/help-mode.spec.ts`.

## Verdict 1 - spec compliance: **APPROVED**

Every brief requirement met, nothing extra.

| Brief item | Result |
| - | - |
| Files: modify exactly `help/{en,de}/editor-tracks-rules.md` | Met. `git diff HEAD~1 --stat` = 2 files, 10 insertions, 2 deletions; `git status --porcelain help/` empty in the worktree and in the main tree. |
| Step 1 - en additions in "Editing a rule" and "When the list may be empty", exactly the enumerated claims | Met (per-sentence walk under Verdict 2). Landed in the two named sections; the brief's plan-authoring anchors (en :9/:17, de :9/:17) reconstruct correctly against the pre-edit files. |
| Step 1 - no keyboard-shortcut claims, no undo promises, no severity beyond "a warning" | Met. Added text contains no keyboard, focus, shortcut, undo or redo term; the only severity word is `a warning` / `eine Warnung`. |
| Step 2 - de additions, real prose in the file's register, de orthography | Met. du-imperative and du-address (`bis du ... ausgefüllt hast`, `wenn du speicherst`) matching the file's own `Ziehe eine Zeile` / `Klicke eine Zeile an` / `verenge`; file-local terminology (`Raster`, `Detailpanel`, `Match-Ausdruck`) rather than the sibling's `Tabelle`/`Detailbereich`; sentence structure diverges from the en (subordinate `wie jede andere Änderung` against the en coordinated clause), so it is authored, not transliterated. Umlauts intact; no ß occurs in the added text (correctly, none is owed). |
| Step 3 - h1 grep empty, fire-verified | Met, and reproduced (see ledger). |
| Step 3 - `pnpm check:i18n` green, fire-verified | Met, and reproduced byte-identically. Independently corroborated structurally. |
| Step 4 - `pnpm lint && pnpm build && pnpm check:i18n && pnpm test:e2e` foreground | Met as reported; `check:i18n` reproduced, the other three corroborated but not re-run (ledger states why and bounds the residual). |
| Step 5 - one commit, both locales, exact message, unsigned, trailer, explicit staging | Met. Subject byte-identical to the brief's prescribed string; `%G? = N`; trailer present; two paths staged, no `git add -A` evidence in the tree state. |
| Zero new surface (preamble) | Met. No catalog id or Fluent attribute touched (`gui-editor.ftl` = 46 ids per locale, measured, budget intact); no help-id, no `data-help-id`, no new topic file; no registry, component, Rust, `DiagCode`, eslint or dependency change. The diff is two markdown files. |
| No design-latitude clause added, no settled decision reopened | Met. The three routes D71 rejected (both help-id routes, the second-host-element route) stay closed by construction - nothing in the diff creates an id or a host element. |

Nothing extra: the added text carries five sentences, all mapped below. The one element outside D71's transcribed enumeration is the `(see the Editor topic)` pointer, which is navigation rather than a behavioral claim, and it matches the dominant house form (21 pre-existing cross-reference lines in `help/en/`, all of the `<label> topic` family). Treating a closed CLAIM enumeration as a wording freeze would be a misread; see the over-restriction flags.

## Verdict 2 - task quality: **APPROVED**

Content accuracy holds against D71's closed enumeration; register, structure, typography and the fire-verified checks all stand up. Findings are three LOW items and one nit, none blocking; two of them are explicitly the owner's rendered-surface pass by the preamble's own routing, and one is a report artifact, not a product artifact.

### Sentence-by-sentence walk to the anchor

| Added sentence | D71 bullet | Anchor, checked at source |
| - | - | - |
| en :13 / de :13 `The Add button appends a new empty rule at the end of the list, selects it, and opens its detail panel.` | 1, first half | D67 (:331-336): appends the skeleton at the end of `tracks.rules`, auto-selected, detail panel opens. `its detail panel` is D67's own phrasing. No focus claim, matching D67's explicit focus ruling. |
| en :13 / de :13 `A warning flags that new rule until you fill in its match expression.` | 1, warning clause | D65 (:236-241): exactly one config-time diagnostic, `EmptyMatchExpression`, warning severity, at `tracks[i].match`, until filled. Severity word matches; no diagnostic identity, no config path, no rendered text quoted. See LOW-2 for the one under-specification. |
| en :15 / de :15 `The Remove button deletes the selected rule; it stays unavailable until a row is selected.` | 1, second half | D66 (:283-285): disabled exactly when `selectedIndex === null`, no other disable condition. `unavailable` is the enumeration's own word, so no `disabled`/`gesperrt` mechanism claim is smuggled in. |
| en :15 / de :15 `Removing asks no confirmation - like every other change in the editor it touches the model only, and the file on disk changes when you save (see the Editor topic).` | 1, tail | D66 (:271-275) no confirmation, explicit save bounds the loss; D41 save-note surface via `editor-save-note` (`locales/en/gui-editor.ftl:134`, `locales/de/gui-editor.ftl:138`) and `help/{en,de}/view-editor.md:13-15`. The universal quantifier verified against code, not assumed: `src/views/EditorView.vue` has exactly one profile-file write path (`doSave` -> `saveProfile`, :266-281); every other mutation is an in-memory model swap. (`rememberRecentProfile` :238 writes settings, not the profile, and is not a change to what the editor edits - checked, immaterial.) |
| en :23 / de :23 `Removing the last rule is allowed; the empty list it leaves behind is exactly the state described here.` | 2 | D69 (:430-434) + `core-83-zero-rule-keep-passthrough` (`docs/product-boundaries.yaml:389`). Correctly stops at legality: no Save-disabling claim, no info-notice claim, no error claim, all of which D69 carries and D71 does not license. |

Deliberate omissions verified absent, all of them true per the design but outside D71's closed enumeration: selection clearing and panel closing after removal (D66), button placement and order (D70), the diagnostic's identity and config path (D65), the zero-rule save gate and info notice (D69), tooltips (D72), undo/redo in any form (D66). The discipline the enumeration demands was actually applied.

Consistency ripple checked and clear: no sibling topic contradicts the new text. `help/{en,de}/editor-attachments-rules.md:7` uses backticked `` `Add` `` / `` `Hinzufügen` `` for the attachment rule KIND; the new text uses the unbackticked button form, which keeps the two apart. `help/en/editor-tracks-unmatched.md:11` already documents the info notice for the empty-list-under-`keep` case, so the new clause's back-pointer to "the state described here" resolves without duplication or conflict.

### Findings

**LOW-1 - referential ambiguity of the pronoun in the Remove sentence, both locales.**
`help/en/editor-tracks-rules.md:15`, `help/de/editor-tracks-rules.md:15`. In `The Remove button deletes the selected rule; it stays unavailable until a row is selected` the nearest antecedent of `it` is `the selected rule`; the intended referent is the button. Identical in de, where `sie` sits after two feminine candidates (`die Schaltfläche Entfernen`, `die ausgewählte Regel`). Self-correcting semantically (a rule cannot be unavailable until a row is selected), so no false claim reaches the reader, but the reader does the disambiguation. Owner-pass wording, not a task fix: `... ; the button stays unavailable until a row is selected` / `... ; die Schaltfläche bleibt gesperrt, solange keine Zeile ausgewählt ist`.

**LOW-2 - the warning sentence does not localize the warning, and the place a reader will look is the one place it does not appear.**
`help/en/editor-tracks-rules.md:13`, `help/de/editor-tracks-rules.md:13`. D65 (:239-241) is explicit that the marker lands at the Match section legend inside the auto-opened detail panel and that the grid row marker deliberately does not fire. The topic's own first paragraph establishes the row as a rule's visible identity (`Each grid row summarizes one rule`), so `A warning flags that new rule` invites a look at the row. The de verb `markiert` leans harder toward a visible marker than the en `flags`. Not an overreach - the sentence asserts no location, and the ambiguity is inherited verbatim from D71's own enumeration wording (`the empty rule is announced by a warning`), so the implementer carried the contract faithfully. Owner-pass candidate: naming the panel stays inside D65, which is D71's own anchor for this clause.

**LOW-3 - report artifact: a quote attributed to the decision that is quoted about it, not by it.**
`task-3-report.md:146` writes: `restates D41's own sentence ("an unsaved model mutation touches no file")`. The string occurs exactly once in `docs/` - at the plan-7.5 design `:274`, inside D66, where it is D66's parenthetical gloss ON D41. D41 itself (`docs/superpowers/specs/2026-07-15-plan-6-design.md:122-175`) carries the canonical-rewrite semantics, the core writer location and the `SaveError` currency; it contains no such sentence. The wording of the quote is accurate; the attribution is not. Product content unaffected - the claim is independently licensed (D71's tail anchor names D66 and the D41 save-note surface; D66 :271-275 states the substance; `editor-save-note` and `help/{en,de}/view-editor.md:13-15` carry the surface). Class: `code-comment-line-citations-drift` (`docs/conventions.yaml:1012`), handled as its precedents were - the s22-h1-derivation-fix HARVEST occurrence took the same shape under an APPROVED verdict. Correction owed in the report line, or simply carried as the harvest occurrence below.

**NIT-1 - possessive where the file uses the definite article.**
`help/en/editor-tracks-rules.md:13` `opens its detail panel` / de `öffnet ihr Detailpanel`, against the file's existing `the detail panel below the grid` / `das Detailpanel unter dem Raster`, which establishes one shared panel. D67 writes `its detail panel opens`, so the wording is anchor-faithful and this is a style observation only.

### Verification ledger

Reproduced independently:

- **h1 grep.** `git diff help/ | grep -E '^[+-]# '` -> no output, `exit=1`. Negative controlled twice **without mutating the tree**: a synthetic `-# Rules (tracks)` / `+# FIREVERIFY ...` pair piped through the same pattern fires (`exit=0`), and `git diff --no-index /dev/null help/en/editor-tracks-rules.md | grep -E '^[+-]# '` prints `+# Rules (tracks)` (`exit=0`). The empty result is a measured absence. Section headings likewise unchanged (no `^[+-]## ` lines in the diff).
- **Typography.** `grep -nP '[\x{2010}-\x{2015}\x{2018}\x{2019}\x{201C}\x{201D}\x{2026}\x{00A0}\x{2212}]'` over both files -> no output, `exit=1`; positive control `printf 'a \xe2\x80\x94 b'` through the same class fires. Report's result and its control both reproduce.
- **`pnpm check:i18n`.** Green; output line byte-identical to the report's, including the `22 help id(s) x 2 help locale(s)` figure, confirming the help-id set did not grow.
- **Gate reach, structurally (stronger than the single fire-test).** `scripts/check-i18n.mjs:621-636` loops every `.md` in every `help/<locale>/` and applies the URL, pipe and raw-HTML tests unconditionally per file. So all three D62 content bans demonstrably reach both edited files, where the report's fire-test proved one ban on one file. The six help checks are existence forward (:587-591), orphans (:596-600), locale lockstep (:605) and the three content bans; **no paragraph- or section-parity constraint exists**, so the report's claim that added-paragraph counts are a register choice rather than a gate is correct (moot here - both locales added the same shape).
- **Catalog facts and line refs.** `locales/en/gui-editor.ftl:138-139` = `editor-action-add = Add`, `editor-action-remove = Remove`; `locales/de/gui-editor.ftl:142-143` = `Hinzufügen`, `Entfernen`; `editor-save-note` en :134 / de :138. All four report line refs correct; prose labels byte-equal to the catalog values. Id count measured at 46 per locale.
- **House phrasing precedents the report cites.** `help/en/view-editor.md:17` `The Apply button ...` and `help/de/view-editor.md:17` `Die Schaltfläche Anwenden ...` exist as quoted. The bold-label form is indeed reserved for a label that leads its own paragraph or bullet (`help/en/batch-suggestion-card.md:9` `**Apply** performs ...`, `help/en/editor-track-rule-match-expr.md:5` `- **`exact`**: ...`). `gesperrt` precedent exact, including the frame: `help/de/view-editor.md:11` `Speichern ist gesperrt, solange ...`. `Bestätigung` precedent is the compound `Bestätigungs- oder Speicherschritt` (`help/de/batch-suggestion-card.md:9`), so the report's "the term ... uses" is true of the stem - mild, no finding.
- **Git facts.** HEAD `29952b9be07d1b83fb658f30c7e27e61d1af276b`, `%G? = N`, subject byte-identical to the brief's message, `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` present, stat as quoted, both trees clean under `help/`, base `1d82179` as stated.
- **The e2e derive-from-file premise (brief's and report's shared load-bearing claim).** True: `e2e/help-mode.spec.ts:36` reads `help/en/${helpId}.md` and parses it with `marked` at runtime to build the expected sidebar HTML; `e2e/help-topics.spec.ts:11-12` does the same for `view-batch`. A content edit therefore cannot desynchronize the expectations. A static count of `test(` occurrences across `e2e/*.spec.ts` sums to 53, matching the report's `53 passed` exactly.

Taken as reported, not re-run: `pnpm lint`, `pnpm build`, `pnpm test:e2e`. Re-running them writes build output into the worktree, which my read-only constraint forbids. Residual risk low and structurally bounded: the diff contains two markdown files under `help/`, which neither eslint nor `vue-tsc` reads; the only runtime consumer is `src/help/topics.ts` via `marked`, and the specs that assert over it derive their expectations from the same files. The reproduced `check:i18n` and the matching 53-test count corroborate that the reported run was real.

## Adjudication questions

### Q1 - in-tree cross-reference form vs the label-leading form

**Verdict: the in-tree form is correct here. The normalization question is an owner-pass item, not a fix in this task.**

Reasoning:

- `help-topic-h1-scheme` (`docs/conventions.yaml:1033`) has exactly one prose-reference clause: *"Prose cross-references retain the parenthetical only where the bare label collides across sections; a unique label leads bare."* It governs the **parenthetical**, not which label token a reference uses. `Editor` collides with nothing across sections, so neither candidate violates the scheme. The byte-equality duty in that statement attaches to the **h1** against its gui-editor catalog value; `Editor view` / `Editor-Ansicht` is not a catalog value at all (the three view topics sit in the scheme's closed parenthetical exemption list), so no byte-equality obligation reaches a prose reference to it. The report's phrase "the label-leading rule" is a defensible reading of "a unique label leads bare", but it should not be read as requiring the reference to reproduce the h1.
- In **en** the chosen wording is not merely in-tree precedent, it is the dominant house form outright: every one of the 21 pre-existing cross-reference lines in `help/en/` is `the <label> topic` (or `that topic` / `its own topic`). Both Q1 candidates are label-leading; the only difference is whether the label token is `Editor` or `Editor view`, and the sole existing reference to that target - `help/en/batch-suggestion-card.md:9` `(see the Editor topic)` - uses the former.
- In **de** the real house-pattern question does sit here, and it is narrower than the question as posed. The dominant de form is `siehe das Thema <Label>` (15 sites carry `Thema <Label>` / `im Thema <Label>`), with three outliers: `das Thema zur Vorschlagskarte` (`view-editor.md:17`, `view-batch.md:16`) and `das Editor-Thema` (`batch-suggestion-card.md:9`). So `siehe das Editor-Thema` is a one-site compound outlier, and normalizing it to `siehe das Thema Editor-Ansicht` is a legitimate argument - as a **tree-wide** normalization of that outlier, not as a defect in this task's sentence.
- Why it is an owner-pass item and not a fix here, on three independent grounds: (a) the preamble routes final user-facing wording to the owner's rendered-surface pass at plan close (the standing D54 route), and this is wording; (b) the brief's file set is exactly two files, and a consistent flip must also touch `help/{en,de}/batch-suggestion-card.md`, so doing it here either breaches the file set or leaves a two-site inconsistency, which is strictly worse than the status quo; (c) no D71 claim depends on it, so it is not a design fork owing NEEDS_CONTEXT.
- The implementer's handling - choose the in-tree precedent, spell out both candidates in a flagged concern, decide nothing - is exactly what the concern channel is for. Route concern 1 to the owner pass as a **three-site de normalization item** (the two `Thema zur Vorschlagskarte` sites and the one `Editor-Thema` site), which is the shape the question actually has.

### Q2 - anchoring to the design's verified-at-authoring enumeration rather than landed code

**Verdict: correct anchoring per the plan. No sentence overreaches what D71 licenses.**

Reasoning:

- The plan closes it in three places: the brief's Interfaces line (*"consumes nothing from stream A (the claims are closed below)"*), the preamble's first constraint (*"The design document is the contract"*), and D71 itself, which states the claims are closed there *"so nothing is invented at the keyboard"*. Stream A's code is absent from `plan75-b` by construction, so code-anchoring was neither available nor owed. Anchoring to landed code would in fact have been the deviation.
- The five-sentence walk above traces every claim to D65/D66/D67/D69 and finds no extrapolation past an anchor. The two elements that go beyond a bare transcription both hold up: the `(see the Editor topic)` pointer is navigation whose target already carries the save semantics, and the generalizer `like every other change in the editor` I verified against `src/views/EditorView.vue` rather than accepting it - one profile-file write path (`doSave` -> `saveProfile`, :266-281), everything else an in-memory swap. That quantifier is true, not merely plausible.
- The residual the report names in its concern 2 is real and correctly scoped: if stream A's landed code diverges from D65's single warning emission, D66's disable condition or D67's auto-select, these sentences inherit the divergence. That is not a task defect - the enumeration is what the task was given - and the plan-close rendered-surface pass over the merged branch is the right place to catch it. Recommend the controller carry it as an explicit item into that pass rather than letting it live only in a task report.

### Q3 - the five cargo gate parts not run

**Verdict: sanctioned by the task's own steps. Not a gate-cut finding.**

Reasoning:

- Step 4 enumerates exactly four commands. The preamble's nine-part duty attaches to *"before any push"* and *"Full gate after every merge"*; a task commit on a stream worktree is neither. Its *"no subsets"* clause governs the nine-part run when that run is owed, not a per-task step, and its own parenthetical states the cargo parts' purpose for this plan: they *"guard against accidental drift"*.
- That purpose is independently satisfied, and more directly than cargo satisfies it: `git diff HEAD~1 --stat` shows two `.md` files under `help/` and `git status --porcelain help/` is empty in both trees. A diff containing no Rust proves the absence of Rust drift; a green `cargo test` would only have failed to detect its presence.
- The implementer flagged the omission with its ground rather than passing silently (`task-3-report.md:115`), which is the correct handling of a residual it does not own.
- The nine-part duty at merge and push is undischarged and stays with the controller; this verdict does not touch it.
- One ambiguity worth a documentation fix, not a rule: read in isolation, *"no subsets"* plus *"they do not become optional"* sits adjacent to task steps that name four parts, and both stream implementers hit that fork. See HARVEST.

## HARVEST

### Observed patterns

1. **A quote lifted from a neighbouring decision that cites the target decision reads as the target's own words.** LOW-3 is the plan-family's next instance of `code-comment-line-citations-drift`: the plan-7.5 design review F1 (a D54 quote whose location was copied without reopening the host) and the s22-h1-derivation-fix HARVEST (a symbol claim in a fix report) are the same shape. What is new and worth naming is the **second observable trigger**: the entry already keys on "you are typing a quotation mark"; add "the line you copied contains a parenthetical `(Dxx - ...)`" - that parenthetical is the *citing* decision speaking, not `Dxx`. Proposed occurrence line for `docs/conventions.yaml` `code-comment-line-citations-drift` (count 3 -> 4):
   `{date: "2026-07-27", kind: violated-corrected, ref: "plan-7.5 T3 verdict LOW-3 (task-3-verdict.md): the task report attributed the sentence 'an unsaved model mutation touches no file' to D41; the string exists once in docs/, at the plan-7.5 design :274 inside D66's parenthetical gloss ON D41 - a quote credited to the decision it paraphrases, in a durable process artifact. Product content unaffected (the claim is independently licensed by D71's tail anchor). Second trigger recorded: a copied line whose parenthetical reads (Dxx - ...) is the citing decision speaking, not Dxx"}`

2. **A read-only reviewer controls the pattern, not the artifact - the reviewer-side counterpart of fire-verification.** The implementer correctly fire-verified its two negatives by mutating the tree and reverting (`proc-verification-step-must-be-falsifiable`). A reviewer cannot mutate the tree, and "I could not fire-verify, so I trusted it" is how a negative goes unchecked. Two cheap side-effect-free substitutes worked here: pipe a synthetic input through the exact same pattern, and force the positive case out of git itself (`git diff --no-index /dev/null <file>` makes the `^[+-]# ` pattern fire on the unmodified file). Worth naming in `docs/process-conventions.yaml` alongside the falsifiability entry as the read-only variant, since every review round in this process faces it.

3. **Structural gate reach beats a single fire-test, and costs less.** Reading `scripts/check-i18n.mjs:621-636` and seeing the content bans loop over every `.md` in every help locale establishes that all three D62 content checks reach both edited files at once; the fire-test establishes one check on one file. Both are worth having, but the structural read is the stronger evidence and the cheaper one, and it also answered the paragraph-parity question as a by-product. Candidate as a review-side heuristic: when a gate's passing result is an absence, read the gate's iteration domain, not only its output.

4. **The task got the enumeration/wording boundary right, and it is worth stating explicitly in future content contracts.** D71 closes CLAIMS. The implementer added a cross-reference and chose sentence structure, verb choice and locale-local terminology freely, and all of that is correct. A literal reading of "the sentences carry exactly these claims, nothing more" would have condemned the `(see the Editor topic)` pointer. Future D-level content contracts should say "the claim set is closed; wording, structure and cross-references ride the owner surface pass" so the closure is not misread as a wording freeze.

### Repeated rejections

None in this task. The implementer rejected nothing and decided nothing at the keyboard: both its concerns were routed as concerns, which is the ruled channel. The design's three rejected routes (both help-id routes, the second-host-element route) were not reopened, verified by the diff introducing no help-id, no `data-help-id` and no topic file.

### Over-restriction flags

- **Do not convert Q3 into "every task runs the nine-part gate."** This diff is two markdown files; a mandated `cargo test --workspace` here is ceremony, and mandated ceremony trains implementers to treat the gate as ritual rather than as a check. The drift it guards against is already proven absent by the zero-surface diff. The right fix is one disambiguating line in future preambles - "task steps run the named subset; the nine-part gate is owed at merge and push; the zero-surface diff check is the task-level drift guard" - which closes the fork without adding an obligation.
- **Do not generalize LOW-1 or LOW-2 into house rules about pronouns or warning-location phrasing.** Two sentences in one topic is a sample of two, and the preamble already owns the channel that fixes them (the owner's rendered-surface pass). A convention entry here would be an unearned abstraction.
- **Do not tighten the claim enumeration into a wording enumeration.** Pattern 4 above is the flag: a stricter reading of D71's closure would have forced out a cross-reference that matches the dominant house form at 21 sites and helps the reader. Keep the closure on claims.
- **Do not normalize the de `Editor-Thema` outlier from inside a task whose file set excludes the other sites.** The correct move is the tree-wide owner-pass item named in Q1; a single-file flip would manufacture the inconsistency it means to remove.
