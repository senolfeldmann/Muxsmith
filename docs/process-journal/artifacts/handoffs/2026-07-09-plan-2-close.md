<!-- Recovered verbatim in session "HANDOFF-Bergung + ROADMAP-Sweep" (2026-07-10)
     from the transcript of "Done: Muxsmith Plan 2" (2fdfe9f4), Write tool call
     2026-07-09T08:52:16Z (the session-close HANDOFF write). Cross-verified
     byte-identical against the first Read of HANDOFF.md in the following session
     "Done: Muxsmith Plan 3" (2b4312c5). Body below is byte-faithful except for
     this provenance comment. NOT COMMITTED until Şenol reviews the collection. -->
# Handoff

> Self-contained context transfer. A fresh session should be able to resume from this file alone. Generated by the write-handoff skill; supersede in place rather than appending.

**Date:** 2026-07-09
**Active agent:** Peter (~/agents/peter)
**Working directory / repo:** ~/Git/Muxsmith (github.com:senolfeldmann/Muxsmith, private, master tracks origin/master)

## STANDING INSTRUCTIONS - reproduce this ENTIRE section verbatim in every future HANDOFF, including this reproduction rule itself, so neither instruction below ever lapses across sessions or compactions

### SI-1. Execution method: superpowers subagent-driven-development, never inline

Execute ALL implementation work on Muxsmith via superpowers subagent-driven-development, never inline in the controller context. Per task: dispatch a FRESH implementer subagent (TDD - failing test reproducing the target, minimal fix, green, commit) AND a separate INDEPENDENT reviewer subagent; the controller re-runs suites itself and never trusts a report's arithmetic. Controller-only work is the plan, the spec, task briefs, triage, and commit hygiene - the controller does NOT author the implementation. Close a plan with a whole-branch review on the most capable model.

Why pinned: Plan 2 was executed inline (one context wrote plan + code + tests, zero independent review). A retrofit review found ~11 real bugs the self-verification missed; the corrective fix pass (done via SDD) then had independent reviews catch real defects at 5 more gates (docs/process-journal/artifacts/plan-2-fixes-sdd/, docs/process-journal.md). Şenol's directive (2026-07-09): use superpowers properly from now on.

### SI-2. Process journal duty at every plan boundary and session close

At every plan/phase completion AND every session close or handoff, append one entry to docs/process-journal.md following docs/process-journal/PROMPT.md verbatim. PROMPT.md (git-tracked, 81 lines) is the authoritative entry prompt: file/hygiene rules, the 8 sections an entry captures (scope, decisions+why, what-the-process-caught, mechanics/metrics, friction/failure, moments, deltas, open threads), anti-goals, and trigger discipline. READ and FOLLOW PROMPT.md each time; do not reconstruct the format from memory. The journal is the primary source for the planned 1.0 process blog, written while context is hot because transcripts compact and scratch dirs die on reboot. Never rewrite or reorder old entries; append only; commit `journal: <phase> entry`. If an SDD pass produced `.superpowers/sdd/` artifacts, salvage them to docs/process-journal/artifacts/<phase>-sdd/ and verify the file count in the COMMIT (a copied `.gitignore` once silently excluded all of them).

(Both SI-1 and SI-2 are carried forward by the reproduction rule in this section's heading.)

## Objective

Build Muxsmith v1: a rule-based bulk MKV muxing tool (Rust core + CLI now, Tauri 2 GUI later) that applies a declarative profile to a source tree and generates/executes mkvmerge commands. Plan 1 (foundations + validate CLI) COMPLETE. Plan 2 (identify, matcher, planner, suggestion engine, dry-run CLI) COMPLETE: implemented, independently reviewed (retrofit), and a full corrective fix pass (F1-F9) applied via SDD. NEXT: Plan 3.

## Constraints and conventions

- Authoritative spec: `docs/superpowers/specs/2026-07-08-muxsmith-v1-design.md` (amended 2026-07-09 for the fix-pass decisions). Design decisions + suggestion-engine algorithm: `docs/superpowers/specs/2026-07-09-plan-2-design-decisions.md` (D1-D6). On any conflict the spec wins.
- Core stays prose-free: diagnostics are `code` + `params` only; all human text in `locales/*/*.ftl` (Fluent). `#![deny(missing_docs)]` on lib crates.
- Per-commit gate (do NOT skip fmt - Plan 2 pushed fmt-dirty once and reddened CI): `cargo test --workspace` AND `cargo fmt --all --check` AND `cargo clippy --workspace --all-targets -- -D warnings` AND `cargo deny check`.
- Git commits/pushes AUTHORIZED for this repo. Trailer final line: `Co-Authored-By: <session model> <noreply@anthropic.com>`. GPG blocks agent commits; use `git -c commit.gpgsign=false commit ...`. Every push logged in `gh-log.md` (git-ignored).
- CI: `.github/workflows/ci.yml` runs `test` (Linux-only on branch pushes, full 3-OS on PRs/tags) + a `deny` job. mkvmerge-gated tests self-skip in CI (mkvtoolnix not installed there yet - see follow-ups).
- mkvmerge is external (installed v99/v100, pinned identification schema v20). Full source at `~/Downloads/mkvtoolnix/` (Peter memory `reference_mkvtoolnix_source`). Confirm mkvmerge behavior by running the binary, not from memory.
- Typography everywhere: ASCII punctuation, umlauts and Ş intact.

## Decisions (and where the rationale lives)

- Plan 2 design D1-D6 (memo): strict independent uniqueness; batch-validated suggestions ("an applied suggestion survives the next dry run"); full MKV-structure scope in v1; Tauri 2 + Rust + React/TS; capability model generated at build time; codec_kind exact-only; value-domain validation; PathBuf locator; rendered-name invariants; suggestion-engine algorithm.
- Fix-pass decisions (Şenol, 2026-07-09, folded into the spec): #1 absent boolean matchable property compares false for exact (mirror mkvmerge, 4.4); #2 empty `any`/`not` is a config error `EmptyMatchList` (4.3); #3 two-planned output collision always errors, `on_collision` governs on-disk files only (4.8); dry-run is a strict superset of validate (5.5).

## Current state

- HEAD 042a2c0, clean (only this HANDOFF.md untracked, by convention), pushed. CI green (test + deny) at 59d24c8/042a2c0. 164 workspace tests, fmt/clippy/deny clean.
- Plan 2 code (crates/muxsmith-core, crates/muxsmith-cli, crates/xtask, locales/, deny.toml, ci.yml): identify (-J wrapper + cache + Identify trait), matcher, discovery, planner (resolution + batch-wide SourceOverwrite + collision semantics + suggestion engine), dry-run + identify CLI, all with the fix-pass corrections. `muxsmith dry-run <profile> --source DIR --output DIR [--json]` and `muxsmith identify <file>` work against real mkvmerge.
- Fix pass (F1-F9 + final-review catch): commits 847b476..59d24c8. Ledger `.superpowers/sdd/plan2-fixes-progress.md` (all 9 [x]). Independent-review trail archived at `docs/process-journal/artifacts/plan-2-fixes-sdd/` (23 files). Journal entries for Plan 2 and the fix pass are in `docs/process-journal.md`.

## Next steps

1. Plan 3: attachments/chapters/tags/title resolution (identify already parses tracks only - extend to attachments/chapters where consumed), command generation (`command: Plan -> Vec<String>`), the `executor` (process spawn, `--gui-mode` progress parse, cancellation, job states), the `run` subcommand + FIFO job queue. Author a plan (writing-plans), execute via SDD per SI-1, journal per SI-2.
2. Not-blocking follow-ups (triage into Plan 3 or a cleanup pass): 6 Minor items from the final review (`docs/process-journal/artifacts/plan-2-fixes-sdd/FINAL-review.md`); the `mkvmerge-query-failed` CLI path drops config diagnostics (same class as the fixed F1 `locate()` path); OverlappingRules names only 2 of >=3 claimants; lint (`0`) vs planner (`tracks[0]`) rule-ref formatting; regex recompiled per `matches()` call; no proptest coverage yet (spec 10 wants it); install mkvtoolnix in CI so the gated integration tests actually run there.

## Open questions / risks

- Go-public timing (flips CI matrix back to 3-OS on push; Actions then free). Dependabot not enabled. Both Şenol's call.
- Plan 4 (Tauri GUI) parked deps: webkit2gtk4.1-devel, librsvg2-devel, libappindicator-gtk3-devel, pnpm via corepack; platform-standard mkvmerge install-location probing (GUI first-run).
