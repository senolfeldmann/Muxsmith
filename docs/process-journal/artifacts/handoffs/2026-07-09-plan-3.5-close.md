<!-- Recovered verbatim in session "HANDOFF-Bergung + ROADMAP-Sweep" (2026-07-10)
     from the transcript of "Done: Muxsmith: Plan 3.5" (ffc7c915), Write tool
     call 2026-07-09T19:25:35Z plus the one subsequent Edit 2026-07-09T19:26:45Z
     (together the session-close state). Cross-verified byte-identical against
     the first Read of HANDOFF.md in the following session "Done: Muxsmith
     Plan 4" (f6ee0efc). Body below is byte-faithful except for this provenance
     comment. NOT COMMITTED until Şenol reviews the collection. -->
# Handoff

> Self-contained context transfer. A fresh session should be able to resume from this file alone. Generated by the write-handoff skill; supersede in place rather than appending.

**Date:** 2026-07-09
**Active agent:** Peter (~/agents/peter)
**Working directory / repo:** ~/Git/Muxsmith (github.com:senolfeldmann/Muxsmith, private, master tracks origin/master)

## STANDING INSTRUCTIONS - reproduce this ENTIRE section verbatim in every future HANDOFF, including this reproduction rule itself, so no instruction below ever lapses across sessions or compactions

### SI-1. Superpowers plugin throughout, and parallelize independent work

Use the Superpowers plugin to its fullest, every session, wherever a skill's trigger fits - not only for execution (Şenol's directive 2026-07-09: use it persistently where it makes sense). The workflow spine: **brainstorming** before any design/feature work -> **writing-plans** to turn a spec into a staged plan -> execution (below) -> **requesting-code-review** at task/feature/merge boundaries -> **finishing-a-development-branch** to integrate. Plus **systematic-debugging** for any bug/failure, **test-driven-development** for every feature/fix, **verification-before-completion** before claiming done, **using-git-worktrees** for isolation.

Execution = **subagent-driven-development** (or **executing-plans** for a batch/parallel-session run), NEVER inline in the controller context: a FRESH implementer subagent per task (TDD - failing test, minimal fix, green, commit) AND a separate INDEPENDENT reviewer subagent; the controller re-runs suites itself and never trusts a report's arithmetic; the controller authors plan/spec/briefs/triage, NOT the implementation; close a plan with a whole-branch review on the most capable model.

**Parallelize independent work.** Derive the task dependency graph first; run streams that do not depend on each other CONCURRENTLY in separate git worktrees (using-git-worktrees + dispatching-parallel-agents), or via the Workflow tool for deterministic fan-out, then merge and re-run the gate. Only a genuine dependency (a task consumes another's output, or two tasks edit the same code region) forces sequential order. Serial SDD in one working tree is the fallback for tightly-coupled tasks, not the global default.

Why pinned: Plan 2 inline execution -> ~11 bugs the self-verification missed (retrofit review + 5 corrective gates; docs/process-journal/artifacts/plan-2-fixes-sdd/). Plan 3.5 -> executed correctly via SDD but STRICTLY SERIALLY; after the foundational restructure task (Task 1) the D19/D20/D21 streams were independent (disjoint planner.rs regions) and could have run as parallel worktree streams for a moderate wall-clock win, left on the table. Şenol's directive (2026-07-09): Superpowers to its fullest, persistently, parallel where independent.

### SI-2. Process journal duty at every plan boundary and session close

At every plan/phase completion AND every session close or handoff, append one entry to docs/process-journal.md following docs/process-journal/PROMPT.md verbatim. PROMPT.md (git-tracked, 81 lines) is the authoritative entry prompt: file/hygiene rules, the 8 sections an entry captures (scope, decisions+why, what-the-process-caught, mechanics/metrics, friction/failure, moments, deltas, open threads), anti-goals, and trigger discipline. READ and FOLLOW PROMPT.md each time; do not reconstruct the format from memory. The journal is the primary source for the planned 1.0 process blog, written while context is hot because transcripts compact and scratch dirs die on reboot. Never rewrite or reorder old entries; append only; commit `journal: <phase> entry`. If an SDD pass produced `.superpowers/sdd/` artifacts, salvage them to docs/process-journal/artifacts/<phase>-sdd/ and verify the file count in the COMMIT (a copied `.gitignore` once silently excluded all of them).

### SI-3. mkvtoolnix parity audit in all planning and decision-making

When authoring plans or design-decision memos, or resolving ANY behavioral
question (a default, a semantic, a policy), compare the proposed behavior
against mkvtoolnix-gui / mkvmerge wherever the comparison is meaningful.
Muxsmith's audience knows mkvtoolnix and expects matching behavior where it
applies; a silent divergence is a surprise to that audience.

The load-bearing distinction: mkvtoolnix is INTERACTIVE (it pre-fills smart
guesses the user reviews and corrects per file), Muxsmith is DECLARATIVE BATCH
(the profile IS the spec, no per-file review loop). So:
- mkvtoolnix's MUXING SEMANTICS and OUTPUT are parity targets to match.
- mkvtoolnix's INPUT-TIME CONVENIENCE GUESSES are NOT targets: filename-derived
  language/flags, auto-title-from-source, unique-name suffixing, and sequence
  auto-append are magic guesses that are right for an interactive tool with
  review and wrong as unseen defaults in batch (see docs/IDEAS.md 1-4).

Method: classify each comparable decision as match / justified divergence /
genuine gap; read the mkvtoolnix source at ~/Downloads/mkvtoolnix (cite
file:line) and confirm mkvmerge behavior by running the binary, not from
memory; surface genuine gaps and deliberate divergences for Şenol's decision
rather than silently choosing; record deliberate divergences in the design memo
with rationale. The Plan 1-4 audit (2026-07-09) set the depth/style bar.

### SI-4. Git commits and pushes are STANDING-authorized for this repo - never re-request

Şenol's directive (2026-07-09, "persist indefinitely"): commits AND pushes on
~/Git/Muxsmith are authorized as a standing grant. Do NOT ask for per-action
or per-session confirmation, and do NOT treat the global never-commit default
as applying here - this repo overrides it ("commit like a real dev").
Mechanics: `git -c commit.gpgsign=false commit ...` (GPG blocks agent
commits); trailer final line `Co-Authored-By: <session model>
<noreply@anthropic.com>`; log every push in `gh-log.md`. If the HARNESS
permission classifier still blocks a git command despite this grant (it reads
settings, not this file - it did so once on 2026-07-09), state in one line
what was blocked and continue with other work; the durable mechanical fix is
a `.claude/settings.json` allow-rule for git in this repo, which Şenol can
approve once. Never interpret a classifier block as Şenol revoking this grant.

(SI-1 through SI-4 are carried forward by the reproduction rule in this
section's heading.)

## Objective

Build Muxsmith v1: a rule-based bulk MKV muxing tool (Rust core + CLI now, Tauri 2 GUI later) that applies a declarative profile to a source tree and generates/executes mkvmerge commands. Plans 1, 2, 3, and 3.5 are COMPLETE and pushed. Plan 4 (executor + run + job queue) is fully designed AND planned - memo and implementation plan both Şenol-approved and committed. **The next session's job: EXECUTE Plan 4**, wave-parallel per SI-1.

## Constraints and conventions

- Authoritative spec: `docs/superpowers/specs/2026-07-08-muxsmith-v1-design.md`. Memos: plan-2 (D1-D6), plan-3 (D7-D12), plan-3.5 (D19-D21), plan-4 (D13-D18). Spec wins on conflict.
- Plan 4 execution plan: `docs/superpowers/plans/2026-07-09-plan-4-executor-run-queue.md` - 11 tasks, 7 waves, dependency graph in the plan. Its Global Constraints section binds every task.
- Per-commit gate (do NOT skip fmt): `cargo test --workspace` AND `cargo fmt --all --check` AND `cargo clippy --workspace --all-targets -- -D warnings` AND `cargo deny check`.
- Core stays prose-free (codes + params; human text in `locales/en/*.ftl`); `#![deny(missing_docs)]` on lib crates; ASCII punctuation, umlauts and Ş intact.
- mkvmerge v100 installed; confirm its behavior by RUNNING it (SI-3). Gated tests self-skip without it. mkvtoolnix source at `~/Downloads/mkvtoolnix`.
- CI: Linux-only on branch pushes, 3-OS on PRs/tags; deny job always. Plan 4 Task 6 adds mkvtoolnix install on Linux.
- Şenol values wall-clock: wave 1 of Plan 4 (T1, T4, T5, T6, T7) MUST run as parallel worktree streams, not serially (SI-1; the serial Plan 3.5 run drew his direct criticism).

## Decisions made (and why)

- Plan 4 design (D13-D18, memo approved by Şenol): core-owned queue emitting a serializable `JobEvent` over mpsc (one enum serves CLI renderer, Plan 5 Tauri events, future NDJSON) -> D13. Soft `--fail-fast` (stop dequeuing, in-flight finish; mkvtoolnix-gui has NO fail-fast at all, so soft is the smaller departure) -> D14. Milestone lines + `--json` final document + worst-of exit fold + 130 on SIGINT + `--on-collision` flag -> D15. `ctrlc` crate, single-level handling -> D16. Delete-partial on failure/cancel KEPT as deliberate anti-parity (interactive GUI keeps partials for diagnosis; bulk contract is "file in tree = valid output", and a kept partial + `on_collision: skip` would silently skip the broken file on rerun) -> D17. D18 riders folded into Plan 4: tests/support consolidation, tempdir-leak fix, CI mkvtoolnix, richer gated live test.
- Plan 3.5 landed the parity fixes: BCP-47 accept-liberal/match-canonical (D19; `exact` = typed value-equality, spec 4.3, README-flagged for 1.0); `tracks` as `{ unmatched, rules }` with `keep|drop` default drop, and keep track-order = ALL primary tracks first in source order then donors (D20 decision B: "keep = match what is already there" - Şenol reversed the built assumption after the whole-branch review flagged donor-first as unintuitive); `UnsupportedSource` diagnostic (D21, trigger `!container_recognized || !container_supported`, zero-track stays MissingTrack).
- Deferred by decision (do NOT build): NDJSON `--json-events` (v1.x), persisted job logs (Plan 5), `--fail-fast=now` (v1.x), zero-track empty-plan warning (cleanup pass), everything in `docs/IDEAS.md` (4 shelved parity features).

## Current state

- HEAD 7aec492 (session-close journal), master IN SYNC with origin/master. Working tree clean; only this HANDOFF.md untracked (git-ignored, by convention). Suite ~214 tests, full gate green at aa75025 (controller-verified); commits since are docs-only.
- Plan 3.5 record: journal session-4 entry appended (d169c51); SDD artifacts archived at `docs/process-journal/artifacts/plan-3.5-sdd/` (23 files, b41edc4); pushes logged in gh-log.md.
- Plan 4 docs committed and pushed: memo 7ba90ee, implementation plan c0c0ef7. NOTHING of Plan 4 is implemented yet - no executor module, no run subcommand, no ctrlc dep, CI unchanged.
- Peter-side persistence (agents repo, ~/agents): eager memory `feedback_superpowers_throughout` (Superpowers + parallelism doctrine); `project_muxsmith` memory carries the repo authorizations and resume pointers. Uncommitted in that repo (Şenol handles its git himself unless asked).

## Next steps

1. Execute Plan 4 per `docs/superpowers/plans/2026-07-09-plan-4-executor-run-queue.md`: start a fresh SDD ledger (`.superpowers/sdd/progress.md`; archive note: the current file still holds Plan 3.5's completed ledger - its archived copy already exists in plan-3.5-sdd/, so overwrite freely), then launch WAVE 1 = T1 (executor seam), T4 (--on-collision), T5 (tests/support + tempdir), T6 (CI mkvtoolnix), T7 (richer gated test) as five parallel worktree implementers; review each; merge sequentially (suggested order T5 -> T7 -> T4 -> T6 -> T1, trivial-conflict candidates T5/T7 both touch command_integration.rs) with the full gate re-run per merge.
2. Then the dependent chain: T2 -> T3 -> T8 -> T9 -> T10 -> T11 per the plan's waves (T2 REQUIRES T1's observed gui-mode grammar; T8 requires T3+T4).
3. Close-out: whole-branch review on the most capable model, SI-2 journal entry, salvage plan-4 SDD artifacts, refresh this HANDOFF, push; then verify in the Actions log that T6 made the gated tests RUN in CI (not skip) - that verification is deferred-to-post-push by design.
4. After Plan 4: Plan 5 is the Tauri GUI (deps parked in Open questions below).

## Open questions / risks

- The `--gui-mode` line grammar (warning/error tag format) is deliberately UNVERIFIED until Plan 4 T1 Step 1 probes the real binary; T2's parser encodes whatever T1 observes. Do not shortcut this with assumed strings.
- Wave-1 worktree merges are a new process for this repo (first parallel run); if merge friction exceeds the win, fall back to serial for the remainder and note it in the journal - but do not skip the attempt.
- Go-public timing (flips CI to 3-OS on push; Actions then free) and Dependabot: both still Şenol's call, unchanged.
- Plan 5 (Tauri GUI) parked deps: webkit2gtk4.1-devel, librsvg2-devel, libappindicator-gtk3-devel, pnpm via corepack; platform-standard mkvmerge install-location probing (GUI first-run); persisted per-job logs (spec 6) live there.
