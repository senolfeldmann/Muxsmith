<!-- Recovered verbatim in session "HANDOFF-Bergung + ROADMAP-Sweep" (2026-07-10)
     from the transcript of "Done: Muxsmith Plan 2" (2fdfe9f4), MID-SESSION Write
     tool call 2026-07-08T22:54:51Z (2026-07-09 00:54 local). State: Plan 2 fully
     WRITTEN, execution not yet started. Superseded later the same session; no
     independent cross-check source exists for mid-session states, content taken
     directly from the Write tool call. Byte-faithful except for this provenance
     comment. NOT COMMITTED until Şenol reviews the collection. -->
# Handoff

> Self-contained context transfer. A fresh session should be able to resume from this file alone. Generated by the write-handoff skill; supersede in place rather than appending.

**Date:** 2026-07-09
**Active agent:** Peter (~/agents/peter)
**Working directory / repo:** ~/Git/Muxsmith (github.com:senolfeldmann/Muxsmith, private, master tracks origin/master)

## Objective

Build Muxsmith v1: a rule-based bulk MKV muxing tool (Rust core + CLI now, Tauri 2 GUI later) that applies a declarative profile to a source tree and generates/executes mkvmerge commands. Plan 1 (core foundations + validate CLI) is COMPLETE, reviewed, pushed, CI green. Plan 2 (identify, matcher, planner, suggestion engine, dry-run CLI) is now fully WRITTEN and pushed. The next increment is EXECUTING Plan 2 via subagent-driven-development.

**No gate. Şenol lifted the wait (2026-07-09): no additional context is coming; commits and pushes are authorized; proceed autonomously.**

## Constraints and conventions

- Authoritative spec: `docs/superpowers/specs/2026-07-08-muxsmith-v1-design.md`. On any conflict, the spec wins. The Plan 2 design decisions + suggestion-engine algorithm are `docs/superpowers/specs/2026-07-09-plan-2-design-decisions.md` (D1-D6, FINAL); D1-D5 are already folded into the spec, so the spec stays the single source of truth.
- Plan 2 to execute: `docs/superpowers/plans/2026-07-09-plan-2-identify-matcher-planner.md` (12 TDD tasks, complete code, Plan-1 style). Execute via superpowers:subagent-driven-development: fresh implementer per task, two-stage review, fix waves, final whole-branch review. Model split: haiku for transcription tasks, sonnet for judgment tasks (matcher, planner, suggestion engine) and reviewers, strongest model for the final review.
- Controller verifies independently: haiku implementers mis-totaled test counts in 5/13 Plan 1 reports (code was fine). Re-run suites yourself; never trust report arithmetic.
- Core stays prose-free: diagnostics are `code` + `params` only; all human text in `locales/*/*.ftl` (Fluent). `#![deny(missing_docs)]` on lib crates; new public items need real rustdoc.
- Git: commits and pushes AUTHORIZED for this repo. Trailer on every commit: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>` (use the model actually running the session). GPG blocks agent commits; use `git -c commit.gpgsign=false commit ...`.
- GitHub: every interaction (gh, API, push) gets an entry in `gh-log.md` (git-ignored). Nothing that costs money; repo private, Actions macOS bills 10x so branch pushes run Linux-only CI by design. See Peter memory `feedback_github_interaction_rules`.
- Process journal: append an entry per `docs/process-journal/PROMPT.md` at plan completion and session close. Journal is `docs/process-journal.md`.
- Typography everywhere: ASCII punctuation (no em-dashes, no curly quotes), umlauts and Ş intact.
- mkvmerge facts: installed binary is v99, schema v20. Full source at `~/Downloads/mkvtoolnix/` (Peter memory `reference_mkvtoolnix_source`). The `-J` shape and list-* formats were confirmed empirically and are baked into Plan 2.

## Decisions made (and why)

Plan 1 decisions stand (strict independent uniqueness; suggestion contract; full MKV-structure scope in v1; Tauri 2 + Rust + React/TS; capability model generated at build time; YAML crate `yaml_serde`). Plan 2 decisions (memo D1-D6, FINAL):

- D1: `codec_kind` is `exact`-only; substring/regex on it is `CodecKindExactOnly`.
- D2: closed-domain value validation; new `InvalidPropertyValue`. `type`/`codec_kind` config-time (curated domains in capability, NOT generated: upstream schema has no `type` enum), `language` plan-time (runtime list).
- D3: `Locator.path` String -> PathBuf.
- D4: rendered-filename invariants: `PathSeparatorInRenderedName`, `EmptyRenderedName`.
- D5: template brace semantics locked with a test (no code change expected).
- D6: suggestion engine (closed edit grammar, batch simulation via the real planner, acceptance = conflict gone everywhere + no new diagnostic, deterministic ranking, cap 3). Plan 2 implements the AmbiguousRule path fully; OverlappingRules auto-suggestions and the no-single-fix partition report are DEFERRED (flagged in Task 11).

## Current state

- HEAD 05899c3, pushed. Tree clean except this HANDOFF.md (untracked by convention) and gh-log.md (git-ignored).
- Commits this session (all pushed): 3b71a71 (design memo), d4390d7 (spec fold-in), 05899c3 (Plan 2 doc).
- Plan 1 code as at 33d6587: `crates/muxsmith-core` (report, profile{model,load,match_expr,validate,lint}, capability{mod,generated,runtime-pending}, template), `crates/muxsmith-cli` (validate + schema), `crates/xtask` (codegen), `locales/en/*.ftl`, CI. 81 tests green, fmt/clippy clean, CI green.
- Tooling installed: mkvtoolnix v99, gh (auth), cargo-insta, cargo-deny 0.19.9, cargo-nextest, rust-analyzer, ffmpeg, python3. Parked for Plan 4: Tauri system deps, pnpm.

## Next steps

1. Execute Plan 2 task by task via subagent-driven-development (start Task 1: cargo-deny). Commit each task with the trailer; push in batches; log pushes in gh-log.md.
2. After Task 12: whole-branch review (strongest model), CI green (incl. new `deny` job), journal entry + archive SDD artifacts under `docs/process-journal/artifacts/plan-2-sdd/`.
3. Record Plan 2 deferrals so they are not lost, then distill durable residue into `project_muxsmith` memory (or an ARCHITECTURE note) and supersede this HANDOFF.

## Open questions / risks

- Plan 2's implementation code is written blind (not compiled). Expect the SDD implementer/reviewer/fix-wave loop to catch compile and edge-case issues; the tests are the executable spec. Known flagged spots in the plan: the `candidates_for_rule` iterator-chain borrow (materialize to an owned Vec), and `Diagnostic.severity` field visibility (add a `with_severity` builder if private).
- Deferred within Plan 2 (flagged, not silent): OverlappingRules auto-suggestions; no-single-fix partition report; extension validation against `--list-types` (no diagnostic code exists yet); attachments/chapters/tags/title resolution + command generation + `run` executor (Plan 3); platform-standard mkvmerge install-location probing (Plan 4).
- Go-public timing (flips CI matrix back to 3-OS). Dependabot not enabled. Both Şenol's call.
