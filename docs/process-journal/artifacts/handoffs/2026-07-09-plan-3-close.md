<!-- Recovered verbatim in session "HANDOFF-Bergung + ROADMAP-Sweep" (2026-07-10)
     from the transcript of "Done: Muxsmith Plan 3" (2b4312c5), Write tool call
     2026-07-09T13:37:29Z plus the one subsequent Edit 2026-07-09T13:42:36Z
     (together the session-close state). Cross-verified byte-identical against
     the first Read of HANDOFF.md in the following session "Done: Muxsmith:
     Plan 3.5" (ffc7c915). Body below is byte-faithful except for this
     provenance comment. NOT COMMITTED until Şenol reviews the collection. -->
# Handoff

> Self-contained context transfer. A fresh session should be able to resume from this file alone. Generated by the write-handoff skill; supersede in place rather than appending.

**Date:** 2026-07-09
**Active agent:** Peter (~/agents/peter)
**Working directory / repo:** ~/Git/Muxsmith (github.com:senolfeldmann/Muxsmith, private, master tracks origin/master)

## STANDING INSTRUCTIONS - reproduce this ENTIRE section verbatim in every future HANDOFF, including this reproduction rule itself, so neither instruction below ever lapses across sessions or compactions

### SI-1. Execution method: superpowers subagent-driven-development, never inline

Execute ALL implementation work on Muxsmith via superpowers subagent-driven-development, never inline in the controller context. Per task: dispatch a FRESH implementer subagent (TDD - failing test reproducing the target, minimal fix, green, commit) AND a separate INDEPENDENT reviewer subagent; the controller re-runs suites itself and never trusts a report's arithmetic. Controller-only work is the plan, the spec, task briefs, triage, and commit hygiene - the controller does NOT author the implementation. Close a plan with a whole-branch review on the most capable model.

Why pinned: Plan 2 was executed inline (one context wrote plan + code + tests, zero independent review). A retrofit review found ~11 real bugs the self-verification missed; the corrective fix pass (done via SDD) then had independent reviews catch real defects at 5 more gates (docs/process-journal/artifacts/plan-2-fixes-sdd/, docs/process-journal.md). Şenol's directive (2026-07-09): use superpowers properly from now on.

### SI-2. Process journal duty at every plan boundary and session close

At every plan/phase completion AND every session close or handoff, append one entry to docs/process-journal.md following docs/process-journal/PROMPT.md verbatim. PROMPT.md (git-tracked, 81 lines) is the authoritative entry prompt: file/hygiene rules, the 8 sections an entry captures (scope, decisions+why, what-the-process-caught, mechanics/metrics, friction/failure, moments, deltas, open threads), anti-goals, and trigger discipline. READ and FOLLOW PROMPT.md each time; do not reconstruct the format from memory. The journal is the primary source for the planned 1.0 process blog, written while context is hot because transcripts compact and scratch dirs die on reboot. Never rewrite or reorder old entries; append only; commit `journal: <phase> entry`. If an SDD pass produced `.superpowers/sdd/` artifacts, salvage them to docs/process-journal/artifacts/<phase>-sdd/ and verify the file count in the COMMIT (a copied `.gitignore` once silently excluded all of them).

(Both SI-1 and SI-2 are carried forward by the reproduction rule in this section's heading.)

## Objective

Build Muxsmith v1: a rule-based bulk MKV muxing tool (Rust core + CLI now, Tauri 2 GUI later) that applies a declarative profile to a source tree and generates/executes mkvmerge commands. Plan 1 (foundations + validate CLI), Plan 2 (identify/matcher/planner/suggestions/dry-run), and Plan 3 (full resolution + pure command generation) all COMPLETE. NEXT: Plan 4 (executor + run + job queue).

## Constraints and conventions

- Authoritative spec: `docs/superpowers/specs/2026-07-08-muxsmith-v1-design.md`. Design-decision memos: `2026-07-09-plan-2-design-decisions.md` (D1-D6) and `2026-07-09-plan-3-design-decisions.md` (D7-D12). On any conflict the spec wins.
- Core stays prose-free: diagnostics are `code` + `params` only; all human text in `locales/*/*.ftl` (Fluent). `#![deny(missing_docs)]` on lib crates.
- Per-commit gate (do NOT skip fmt): `cargo test --workspace` AND `cargo fmt --all --check` AND `cargo clippy --workspace --all-targets -- -D warnings` AND `cargo deny check`.
- Git commits/pushes AUTHORIZED for this repo. Trailer final line: `Co-Authored-By: <session model> <noreply@anthropic.com>`. GPG blocks agent commits; use `git -c commit.gpgsign=false commit ...`. Every push logged in `gh-log.md` (git-ignored).
- CI: `.github/workflows/ci.yml` runs `test` (Linux-only on branch pushes, full 3-OS on PRs/tags) + a `deny` job. mkvmerge-gated tests self-skip in CI (mkvtoolnix not installed there yet).
- mkvmerge is external (installed v100, pinned identification schema v20). Full source at `~/Downloads/mkvtoolnix/` (Peter memory `reference_mkvtoolnix_source`). Confirm mkvmerge behavior by running the binary, not from memory.
- Typography everywhere: ASCII punctuation, umlauts and Ş intact.

## Current state

- HEAD a1283c4, master IN SYNC with origin/master (Plan 3 pushed, range 62d4956..a1283c4). Only this HANDOFF.md is untracked (git-ignored, operational, by convention). Working tree otherwise clean.
- 204 workspace tests, fmt/clippy(-D warnings)/deny all clean at HEAD (controller-verified). CI run 29022082442 green (test ubuntu-latest + deny). Non-blocking CI annotation: actions/checkout@v4 pins deprecated Node 20 - trivial bump for a future CI-maintenance pass.
- Plan 3 code (all in crates/muxsmith-core): `identify` parses attachments (`Attachment` struct) + chapters count; `matcher` generalized over a `Matchable` trait (Track + Attachment); `planner` produces a self-contained enriched `Plan` (resolved per-assignment `changes` with `track_kind`, `AttachmentPlan`, `ChapterSource`, `TagFlags`, `TitleAction`); new `command` module = pure `command(&Plan) -> Vec<String>`. `crates/muxsmith-core/tests/command.rs` (golden argv) and `command_integration.rs` (full 4.1 reference golden + gated live mkvmerge round trip).
- Plan 3 executed via SDD: 12 tasks, sonnet implementers/reviewers, opus whole-branch review (verdict: ready to merge, no Critical/Important; it drove real mkvmerge v100 to confirm the `-J` attachment id == `--attachments` selector id). One fix wave (Task 9 empty-donor-group, Important) + one minor cleanup. Per-task record: `docs/process-journal/artifacts/plan-3-sdd/` (39 files). Journal entry appended.

## Decisions (and where the rationale lives)

- Plan 3 decisions D7-D12 (memo `2026-07-09-plan-3-design-decisions.md`): plan 3/4/5 split; self-contained `Plan` + pure format-neutral `command`; identify attachment/chapter parse; resolution semantics (attachment scope primary-only D10; chapters/title reuse; per-file settable-language validation); command argv structure; and D12 = `add` is a collection-populator (all matched files, dedup by path, zero-match warning), justified by the slot-vs-collection invariant (uniqueness is for slot-fillers: track/chapters donors; collections are multi: attachment select/drop/add).

## Next steps

1. Plan 4 (process layer): the `executor` (process spawn behind a trait mirroring `Identify` for testability, `--gui-mode` progress parse `#GUI#progress NN%`, exit-code mapping 0/1/2, kill + delete-partial, job states); the `run` subcommand + FIFO job queue (`--jobs N` via a bounded std thread pool, NO async runtime; `--fail-fast`; failures do not abort the batch by default); Ctrl-C (SIGINT) cleanup wired to the executor kill primitive. Author a plan (writing-plans), execute via SDD per SI-1, journal per SI-2. Plan 4 forward decisions were pre-taken in D7 - revisit only if Plan 4's brainstorm surfaces a reason.
2. Plan 3 deferred follow-ups (triage into Plan 4 or a cleanup pass): richer gated live test (attachment + `changes`) - highest value, converts the one-off manual v100 validation into a standing guard; zero-track plan renders an empty MKV with no diagnostic (planner "empty plan" warning?); `FakeIdent`+`lang()` test helper duplicated 3x -> `tests/support.rs`; tests use `std::mem::forget(tempdir)` leaking temp dirs; `with-attachments.json` fixture uses 0-based attachment ids (real mkvmerge numbers from 1; code is id-agnostic so harmless); optional batch-level settable-language check. Plus the standing Plan 1/2 follow-ups (FINAL-review minors, regex recompiled per `matches`, install mkvtoolnix in CI so gated integration tests run there).

## Open questions / risks

- Go-public timing (flips CI matrix back to 3-OS on push; Actions then free). Dependabot not enabled. Both Şenol's call.
- Plan 5 (Tauri GUI, was Plan 4 in the spec numbering) parked deps: webkit2gtk4.1-devel, librsvg2-devel, libappindicator-gtk3-devel, pnpm via corepack; platform-standard mkvmerge install-location probing (GUI first-run). Persisted per-job logs in the app-data dir (spec 6) are deferred here (the GUI job-queue view is their consumer).
