<!-- Recovered verbatim in session "HANDOFF-Bergung + ROADMAP-Sweep" (2026-07-10)
     from the transcript of "Done: Muxsmith Plan 2" (2fdfe9f4), MID-SESSION Write
     tool call 2026-07-08T23:21:09Z (2026-07-09 01:21 local). State: Plan 2 fully
     IMPLEMENTED (125 tests green, HEAD e1bfba7), final whole-branch review still
     open. Superseded later the same session; no independent cross-check source
     exists for mid-session states, content taken directly from the Write tool
     call. Byte-faithful except for this provenance comment. NOT COMMITTED until
     Şenol reviews the collection. -->
# Handoff

> Self-contained context transfer. A fresh session should be able to resume from this file alone. Generated by the write-handoff skill; supersede in place rather than appending.

**Date:** 2026-07-09
**Active agent:** Peter (~/agents/peter)
**Working directory / repo:** ~/Git/Muxsmith (github.com:senolfeldmann/Muxsmith, private, master tracks origin/master)

## Objective

Build Muxsmith v1: a rule-based bulk MKV muxing tool (Rust core + CLI now, Tauri 2 GUI later). Plan 1 (foundations + validate CLI) COMPLETE. Plan 2 (identify, matcher, planner, suggestion engine, dry-run CLI) is now fully IMPLEMENTED and pushed: all 12 tasks done, 125 workspace tests green, fmt/clippy/deny clean locally, and a live `dry-run` against real mkvmerge produces a batch report with validated suggestions. Remaining before Plan 2 is "closed": a final whole-branch review, CI-green confirmation, and the process-journal entry.

## What is built (Plan 2, HEAD e1bfba7)

- `capability`: curated value domains (`matchable_domain` for `type`/`codec_kind`), `runtime` module (`Mkvmerge` locate/version/list_types/list_languages + `identify_json`; `LanguageIndex` normalizer), `PINNED_IDENTIFICATION_FORMAT_VERSION = 20`.
- `identify`: `-J` wrapper, `Track`/`PropValue`/`Identification` model, in-memory cache keyed path+mtime+size, `Identify` trait + `LiveIdentifier` (planner injection seam).
- `matcher`: pure `matches(expr, track, lang)` (exact/substring/regex/any/not, language normalization against language + language_ietf, codec_kind codec_id-prefix).
- `discovery`: `scan_primaries` (IgnoredFile/MultipleIdentifierMatches/DuplicateIdentifier), `resolve_locator` (match_to_source/match_pattern).
- `planner`: `plan_core`/`plan_batch`, per-file resolution under strict uniqueness (Ambiguous/Missing/Overlapping/External diagnostics), rendered-name invariants (D4), collision + SourceOverwrite, plan-time language validation, and the AmbiguousRule suggestion engine (D6).
- CLI: `dry-run` + `identify` subcommands, Fluent rendering, exit codes 0/1/2.
- New diagnostic codes wired (D1/D2/D4); cargo-deny gate + Linux `deny` CI job.

## Constraints and conventions

- Authoritative spec: `docs/superpowers/specs/2026-07-08-muxsmith-v1-design.md`; decisions/algorithm in `docs/superpowers/specs/2026-07-09-plan-2-design-decisions.md` (D1-D6). Plan doc: `docs/superpowers/plans/2026-07-09-plan-2-identify-matcher-planner.md`.
- Core stays prose-free (code+params); text in `locales/*/*.ftl`. `#![deny(missing_docs)]` on lib crates. ASCII in code/comments; no em-dashes/curly quotes anywhere; umlauts/Ş intact.
- **Verification gate per commit: run `cargo fmt --all --check` AND clippy AND tests. This session skipped fmt on tasks 3/5 and pushed fmt-dirty; the intermediate CI test job failed on fmt (fixed by rustfmt commit 72c59d2). Do not repeat: fmt-check before every commit.**
- Git commits/pushes AUTHORIZED. Trailer: `Co-Authored-By: <session model> <noreply@anthropic.com>`; GPG off via `git -c commit.gpgsign=false`. Every push logged in `gh-log.md` (git-ignored).
- mkvmerge is external (v99, schema v20). Full source at `~/Downloads/mkvtoolnix/` (Peter memory `reference_mkvtoolnix_source`).

## Next steps

1. Confirm CI green at HEAD e1bfba7 (`test (ubuntu-latest)` + new `deny` job). The prior push 0e64c1e failed only on fmt; e1bfba7 is fmt-clean.
2. Whole-branch review (strongest model) over the Plan 2 diff: matcher edge cases, planner collision-severity mapping and finalize ordering, suggestion-engine acceptance invariant, prose-free core. Apply fix waves.
3. Process-journal entry per `docs/process-journal/PROMPT.md`; note the direct-execution deviation from Plan 1's subagent-driven-development (executed inline by the controller with live cargo verification, since the tasks were well-specified and locally verifiable).
4. Distill durable residue into an ARCHITECTURE note or the `project_muxsmith` memory, then this HANDOFF can be deleted.

## Deferred (flagged, not silent) - carry forward

- Suggestion engine: OverlappingRules auto-suggestions; no-single-fix partition report (spec 5.3). The simulate/accept machinery generalizes to both.
- Plan 3: attachments/chapters/tags/title resolution, command generation (`Plan -> Vec<String>`), executor (`run`), job queue.
- Extension validation against `--list-types` (no diagnostic code exists yet; decide whether to add one).
- Platform-standard mkvmerge install-location probing (Plan 4, GUI first-run); v1 CLI uses PATH + explicit override.
- CI does not yet install mkvtoolnix, so the mkvmerge-gated tests self-skip there (spec 10 wants CI to mux fixtures); add an mkvtoolnix install step to the CI test job when convenient.

## Open questions / risks

- Plan 2 code was authored blind in the plan then executed with live verification; 125 tests + a live end-to-end dry-run pass, but a fresh adversarial review is still owed (step 2).
- Go-public timing (flips CI matrix to 3-OS). Dependabot off. Both Şenol's call.
