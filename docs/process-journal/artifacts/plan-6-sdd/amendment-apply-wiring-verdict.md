# Verdict (extracted from the reviewer transcript)

All load-bearing claims verify against the trees. I have everything needed to render the verdict.

## Verdict
**Amendment:** APPROVED

The amendment correctly encodes every fact the brief required, verified independently against both trees. Minor consistency notes only; none gate the commit.

## Executability checks

All premises confirmed in the wave-3 base worktree (`/home/senol/Git/Muxsmith/.worktrees/plan6-e`, `a91e56f`) unless noted:

- **Sibling topology** — `src/views/BatchView.vue:405` renders `<DiagnosticsPanel :diagnostics="generalDiagnostics" />` inside the diagnostics `<section>` (`aria-labelledby="batch-diagnostics-heading"`, :392), `:429` renders `<SuggestionCard v-for=...>` inside a separate suggestions `<section>` (:424). Two siblings, not parent/child. The amendment's `:405`/`:429` are exact.
- **Picked FILE path** — `const selectedProfile = ref<string | null>(null);` at `BatchView.vue:27`. Exact.
- **BatchView's IPC/orchestration idiom** — `dryRunning` (:32), `busy` (:36), and `runDryRun` (:176-195) establish precisely the `flag=true; ipcErrorCode=null; try{...}catch(e){const err=e as IpcError; ipcErrorCode=err.code; ipcErrorParams=err.params}finally{flag=false}` pattern the amendment's orchestration mirrors. The shared alert line the amendment cites at `:373-378` is the `<p v-if="ipcErrorCode" role="alert">{{ $t(ipcErrorCode, ipcErrorParams) }}</p>` block — exact.
- **IPC surface present at base** — `ipc.ts`: `loadProfile(path):Promise<LoadProfileDocument>` (:274), `saveProfile(path, profile):Promise<void>` (:278), `applySuggestion(profile, configPath, edit):Promise<Profile>` (:296-301), `LoadProfileDocument` (:270), `Suggestion.edit: unknown` (:134). All exist.
- **`config_path` is a locator, not a path** — `rule_index_of` definition at `crates/muxsmith-core/src/planner.rs:2117` (amendment's cite, correct for the current tree); the locator format `tracks[{ri}].match` is produced at :1447/:1526 and the doc at :1934 states config_path "is not of the `tracks[<N>].match` form" as the error case. Solidly confirmed.
- **Design citation for post-apply — supports the no-auto-validate half, silent on refresh (ruling below).** `docs/superpowers/specs/2026-07-15-plan-6-design.md:509-515` (D43): "The applied result is validated through the normal path, not by this command. `apply_suggestion` returns the mutated model; the editor then runs its existing `validate_profile_model` round-trip." `:503-504` (core-03): the guarantee is "an applied suggestion survives the next dry run." Both exact.
- **Two-distinct-values echo assertion is concrete enough to defeat a swap.** Named values: `PROFILE_PATH = "/profiles/demo.yaml"` (matches the real constant at `smoke.spec.ts:141`) vs locator `"tracks[0].match"`. Assertions in both directions: `apply_suggestion.configPath == "tracks[0].match"`; `load_profile`/`save_profile` `path == PROFILE_PATH`. Loading/saving by the locator fails the path assertion; passing the path as the apply locator fails the configPath assertion. This genuinely defeats the swap that the draft hid — the draft's fixture set `config_path: PROFILE_PATH` (`smoke.spec.ts` draft) and asserted `configPath).toBe(PROFILE_PATH)`, so load/save-by-`config_path` was indistinguishable from load/save-by-path.
- **Draft compatibility (refit, not restart).** The draft (5 files, no BatchView.vue) carries the salvageable pieces the amendment names: the apply button with `data-testid="batch-suggestion-apply"`, `:aria-busy`, `@click` (draft SuggestionCard.vue template); the two `.ftl` keys (`batch-suggestion-apply`/`-tooltip`, en+de); the rewritten D22 comment; the no-fix paired control (`getByRole("button", name("batch-suggestion-apply"))` count 0 in the `batch-diagnostics-heading` region, present on the article). The refit is real but bounded: relocate the `apply()` orchestration + error state from SuggestionCard into BatchView, switch the button to emit, retarget the fixture's `config_path` to the locator and add the `load_profile` assertion. The amended text describes the correct end state for each. The draft's misuse — `loadProfile(props.suggestion.config_path)` / `saveProfile(props.suggestion.config_path, updated)` — is exactly what the amendment corrects.

**Ruling on post-apply behavior (requested explicitly):** The design *supports* the no-auto-validate decision directly — D43 :509-515 places validation in the editor's `validate_profile_model` round-trip, which the batch view does not possess, so "batch view does not auto-validate" follows from the design, not from the author. The auto-refresh / auto-re-run-dry-run decision is genuinely *design-silent*; core-03 :503-504 guarantees survival of the *next* (user-initiated) dry run, not that apply triggers one. The amendment handles this correctly: it does not dress the no-refresh choice as a design mandate — it states "Auto-refreshing the batch view's suggestion list is a user-visible behavior the design does not record, so it is deferred" and routes it to the out-of-scope note. Task 14 ships the null option (introduce no new user-visible behavior), which is the legitimate keyboard-safe default; the product question of *adding* refresh is routed, not decided. Correct.

## Coverage

- **Files list corrected** — `src/views/BatchView.vue` added (plan :1601, parent/handler/imports), SuggestionCard reduced to "renders + EMITS, does NOT orchestrate" (:1600), DiagnosticsPanel role stated as one explicit choice: documentation-only comment, not in chain (:1602). Files-list header carries the marker.
- **Interfaces updated** — "Consumes" now names `load_profile`/`apply_suggestion`/`save_profile` via the three IPC fns, marked (plan :1608).
- **Both wrong-topology ripple sites fixed** — I grepped the amended plan for `DiagnosticsPanel` myself. Only mentions: :47 (the quoted-refuted original inside the Amendment 4 block, intended), :1601/:1602/:1614/:1642 all in correct positions (parent-of-both, out-of-chain, documentation-only). No surviving `DiagnosticsPanel` in a wiring position; grep for `wire the emit through` / `SuggestionCard -> DiagnosticsPanel -> command` returns nothing outside the quoted premise. Both defect sites (old Files-list "wire the emit through" and old Step 3) corrected.
- **`config_path`-as-path** — grepped: every plan occurrence treats it as an opaque locator forwarded to `apply_suggestion`. The misuse lived only in draft code, never in plan text; nothing to correct there.
- **Both hazards routed as controller notes, not TODOs** — the "Out of scope, routed by the controller (not implementer TODOs)" block carries apply-vs-editor concurrency ("Whole-branch/ROADMAP candidate; the controller routes it, Task 14 does not solve it") and auto-refresh ("Deferred to the controller (ROADMAP candidate), not decided at the keyboard"). Both are pointers to the controller, explicitly not implementer work.
- **Fourth amendment block within five sentences** — four sentences (refuted premise / verified facts / corrected wiring / marker convention), consistent with the amendment-block convention.
- **Falsifiability** — the one new negative check (no-fix "no apply button") carries a paired present/absent control on the identical selector, and the amendment names it as the control. The swap-detection rests on two distinct asserted values, not on an absence.
- **Scope** — one file (`docs/superpowers/plans/2026-07-16-plan-6-profile-editor.md`), confirmed by the diff and `git status`. No reopened settled decision (D22-comment update, no-fix case, 2-key budget, 45-key editor budget, Task-13b sequencing all untouched). No contradiction with amendments 1-3.

## Issues

#### Critical (Must Fix)
None.

#### Important (Should Fix)
None.

#### Minor (Nice to Have)
- **Marker omission on the routed-notes block.** The "Out of scope, routed by the controller" block (diff lines 44-46) is amendment-introduced text but lacks the `(amended 2026-07-16, apply-wiring routing)` marker, whereas the amendment marked its other non-step additions (Files header, Interfaces, both binding points). The amendment's own stated convention is "Steps changed by this routing carry the marker," and this block is a subsection rather than a step, so it is defensible — but it is the one new block without a marker while its neighbours have them. Adding the marker (or a parenthetical provenance) would make traceability uniform. Does not affect executability.
- **Routed notes are "candidates," not yet captured.** Both hazards are labeled "ROADMAP candidate." The plan-side requirement (record as routed notes, not TODOs) is met, but "candidate" means the controller still owes the durable capture in the whole-branch/ROADMAP artifact. Follow-up for the controller, outside this amendment.
- **Report/plan tension on busy-state granularity (not a plan defect).** The report's Concern #1 frames per-card vs app-wide busy as free implementer choice, while the plan's Wiring binding point mandates "the clicked card's control via `aria-busy`." Both readings satisfy the e2e (which only observes the clicked card's button) and the plan wording is satisfiable either way, so no fork is actually open; noting only that the report's framing is looser than the plan text.
- **Line-drift, self-flagged and inert.** `rule_index_of` @ `planner.rs:2117` is correct for the current tree though the ledger cited `:1897` and D43 cites `:2032`; `edit: unknown` is at `ipc.ts:134` while pre-existing plan text (unchanged context, not the amendment's) cites `:132`. Function/field identity binds, not the number; the report's Concern #2 already flags the first.

## HARVEST
- **Semantics-blind echo mock as a defect class.** The draft passed 27/27 precisely because the fixture collapsed two distinct concepts (file path, config locator) onto one value, so an echo-mock could not witness the swap. The general fire-once lesson: an echo/identity assertion over a value that equals another value in the fixture proves nothing about which one the code used — distinct fixture values per concept are the control, exactly as this amendment now mandates. Worth a process-conventions entry if not already covered by the existing falsifiability template.
- **"Refuted premise" amendments benefit from a quoted-original-plus-marker discipline.** Keeping the wrong wiring string (`SuggestionCard -> DiagnosticsPanel -> apply_suggestion`) *only* inside the amendment block, as the quoted refuted premise, let me grep-verify ripple cleanup deterministically (one intended hit, zero unintended). That is a reusable pattern: when correcting a topology claim, quote the dead claim exactly once in the amendment and nowhere else, so a post-edit grep distinguishes "documented history" from "surviving defect."
