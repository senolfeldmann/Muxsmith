# Blocked-pool sweep input (mechanical extraction, master@962005b, Plan 6 fully merged)

Evidence-only. No judgments, no recommendations. LANDED/NOT LANDED = whether the deferred
work exists in the current tree regardless of condition state. CLEARED/NOT CLEARED = whether
the `blocked_on` condition text itself has observably fired.

| file | id | blocked_on | gist | work landed? | condition cleared? | evidence |
|---|---|---|---|---|---|---|
| decision-ledger.yaml | core-56-batch-level-language-fold | v1.x planning | fold settable-language validation into `walk_exact_languages` (catch all-optional-unmatched invalid-language case), left for v1 | NOT LANDED | NOT CLEARED | `planner.rs:449-478` `walk_exact_languages` only recurses `expr.exact`'s `language` key; no settable/`changes.language` handling. Sibling entry core-55 (ledger:264) confirms settable-language is still validated separately per-file, not folded in. |
| decision-ledger.yaml | core-66-eager-resolution-discarded-plan | v1.x planning | on `render_output` failure, `resolve_chapters`/`resolve_attachments` still run (wasted I/O), left as-is by design | NOT LANDED (behavior unchanged by construction) | NOT CLEARED | `planner.rs:702-713`: `render_output`, `resolve_chapters`, `resolve_attachments` all called unconditionally in sequence; no failure-gating added. |
| decision-ledger.yaml | exec-36-core-stderr-logging | Plan 9 (re-pointed from Plan 6, 2026-07-15 scope re-cut) | single `eprintln!` in core (`queue.rs`) is first direct stderr I/O in core; idiomatic fix is a log/tracing facade, deferred | NOT LANDED | NOT CLEARED | `executor/queue.rs:396` still `eprintln!("muxsmith: worker thread panicked while running job {index}: {message}")`; no `log`/`tracing` dependency in any `Cargo.toml`; `.superpowers/sdd/` has no `plan-7`/`plan-9` directory yet, ROADMAP.md:126 shows Plan 9 not started. |
| decision-ledger.yaml | exec-37-panicked-msg-catalog | Plan 9 (re-pointed from Plan 6, 2026-07-15 scope re-cut) | rich worker-panicked message renders on no live surface; routing `JobOutcome.errors` codes through the diagnostics catalog deferred | NOT LANDED | NOT CLEARED | same `queue.rs:396` raw `eprintln!`, no diagnostics-catalog routing found for the panicked-job message path; no plan-9 dir yet. |
| decision-ledger.yaml | i18n-12-de-placeable-parity-deferred | future check:i18n extension (internal) | placeable-name/selector-structure drift in German catalogs not machine-checked; `check:i18n` extension deferred | NOT LANDED | NOT CLEARED | `scripts/check-i18n.mjs` has only `parseCatalogIds`/`listCatalogFiles` + id-parity logic; no placeable/selector-structure check function present. |
| decision-ledger.yaml | gui-26 | Plan 7 (re-pointed from Plan 6, 2026-07-15 scope re-cut) | no live in-session locale switch (bootstrap-once, restart notice); live switch deferred as Plan 6/7 candidate | NOT LANDED | NOT CLEARED | `locales/en/gui-settings.ftl:9` / `locales/de/gui-settings.ftl:10` still say the changed language "takes effect after restarting Muxsmith" / "wird erst nach einem Neustart... wirksam"; no `plan-7` SDD dir exists. |
| decision-ledger.yaml | ci-16-mise-not-ci | internal - kept until release stabilizes; removal tracked ROADMAP v1.x | CI must not fetch a floating mise binary at run time; dropping mise from CI deferred to post-1.0 (recorded although current CI then violated it) | NOT LANDED | UNCLEAR | `.github/workflows/ci.yml:82` still runs `jdx/mise-action`, now SHA-pinned (`@e6a8b3978addb5a52f2b4cd9d91eafa7f0ab959d # v4.2.0`) unlike the "without a version pin" state the entry describes; the action itself (the actual deferred removal) is still present. Mise-release-stability itself not independently checkable from the repo. |
| decision-ledger.yaml | testing-coverage-tooling | no measured coverage need in v1 (revisit at v1.x planning) | `cargo-llvm-cov` left out of Plan-1 tooling stock-take | NOT LANDED | NOT CLEARED | `BUILDING.md:119` still lists "Coverage tooling (cargo-llvm-cov): revisit at v1.x planning (ROADMAP)."; no coverage tooling in `Cargo.toml`/`mise.toml`/`.github/workflows/ci.yml`. |
| decision-ledger.yaml | testing-cli-helper-dedup | internal: next CLI test file | `have_mkvmerge`/`fake-stub` helpers duplicated 2-3x across CLI test files; T5-style consolidation deferred until the next CLI test file appears | NOT LANDED | NOT CLEARED | `have_mkvmerge()` independently redefined in `run_cli.rs`, `run_live.rs`, `dry_run_cli.rs`; `tests/support/mod.rs` shares only `insta_settings`/`insta_settings_with_tmp`/`fake_mkvmerge_that_fails_queries`, not `have_mkvmerge`; `run_live.rs` (last-created CLI test file, git-added 2026-07-10 00:50) predates the deferral's own source verdict `docs/process-journal/artifacts/plan-4-sdd/verdicts/whole-branch-review-verdict.md` (git-added 2026-07-11 17:10), which already counted it among the "2-3x" duplicates; no CLI test file added since. |
| decision-ledger.yaml | testing-comment-fixture-convention | second use (trigger-gated, internal) | ad hoc `_comment` fixture-note convention; promotion to written convention gated on a second fixture needing it | NOT LANDED | NOT CLEARED | repo-wide `grep -rl '"_comment"'` (excluding worktrees) hits only `crates/muxsmith-core/tests/fixtures/identify/with-attachments.json`; the only other grep hit (`prop_matcher.rs`) is a false-positive substring match on `flag_commentary`, not a second use. |
| decision-ledger.yaml | testing-check-i18n-self-test | v1.x test-hardening (internal) | `check-i18n.mjs` has no fixture self-test despite grown logic | NOT LANDED | NOT CLEARED | no test/fixture file targeting `scripts/check-i18n.mjs` found anywhere in the repo (`find`/`grep` for `check-i18n`+test/fixture patterns empty). |
| decision-ledger.yaml | cli-08-config-diags-json-ordering | Plan 9 (re-pointed from Plan 6, 2026-07-15 scope re-cut) | flat `config_diags` JSON is unsorted, inconsistent with `validate`'s sorted JSON; sorting for parity deferred | NOT LANDED | NOT CLEARED | `dry_run.rs`/`run.rs` json branches (e.g. `dry_run.rs:72,98,123`) pass raw `config_diags` into `config_only_document`/`batch_document`, and `report/json.rs`'s `config_only_document`/`rendered_diags` contain no sort call, while the same files' text branches call `severity_sorted(&config_diags)`; `validate.rs:20-29` by contrast sorts once and shares the sorted vec for both its JSON and human paths. No plan-9 dir yet. |
| decision-ledger.yaml | core-121-planner-seam-and-hoist | Plan 9 (seam interface open question; hoist vehicle settled as its own plan per 2026-07-15 owner call) | four-copy planning pipeline (cli `dry_run.rs`/`run.rs`, src-tauri `lib.rs`/`run.rs`) and the never-decided injectable-planner-seam interface; hoist left undecided/unbuilt | NOT LANDED | NOT CLEARED | repo-wide `grep -rn plan_pipeline` across `crates/` and `src-tauri/` returns nothing; no shared hoisted function found. No `plan-9` SDD dir exists (ROADMAP.md:126 "Plan 9" section describes it as not-yet-started). |
| decision-ledger.yaml | proc-subagent-commit-trailer-set | owner/controller convention call on the next commit-policy touch | unsettled which trailer set subagent worktree commits carry (`Co-Authored-By` universal, `Claude-Session` inconsistent) | NOT LANDED (non-decision, N/A) | NOT CLEARED | no trailer-set convention recorded in `docs/*.yaml` or `docs/*.md` (`grep -rn "trailer"` finds only this entry and one unrelated per-repo authorization decision); `git log -5` on current HEAD shows commits still carrying `Co-Authored-By` only (no `Claude-Session` trailer), the same inconsistency the entry describes. |
| product-boundaries.yaml | core-19-sync-delay-stretch | no batch-generalizable model for per-file offsets | per-file `--sync` delay/stretch changes deferred (v1.x candidate) | NOT LANDED | UNCLEAR | repo-wide grep for `--sync`/`sync_delay`/`SyncOffset`/`stretch` across `crates/` returns nothing; feature is simply absent, so no generalizable model has been produced either. |
| product-boundaries.yaml | exec-16-failfast-now-deferred | external: real usage demand | a `--fail-fast=now` value that also kills in-flight jobs (GNU-parallel `now,fail=1` analog) deferred to v1.x | NOT LANDED | UNCLEAR | `executor/queue.rs`'s `QueueOpts.fail_fast` (line 79) is still a plain `bool` implementing only soft fail-fast (D14: cancels queued jobs, not in-flight); no "now" variant/enum exists. |
| product-boundaries.yaml | exec-45-runlog-config-deferred | internal: option surface not earned; parked to IDEAS #7 pending real demand | run-log retention (fixed 14-day prune) not configurable | NOT LANDED | NOT CLEARED | `executor/joblog.rs:85` comment still reads "configurability parked as IDEAS #7"; `prune_stale_runs` (line 112) still hard-codes the 14-day window; `docs/IDEAS.md` item 7 ("Configurable run-log prune behavior (disable / change days)") still listed as an unrealized idea. |
| product-boundaries.yaml | cross-07-watch-daemon-mode | post-v1 scope; no v1 use case established | watch/daemon mode is a v1 non-goal, no reconsider trigger | NOT LANDED | UNCLEAR | no `--watch`/`daemon` code anywhere in `crates/muxsmith-cli/src` (grep empty). |
| product-boundaries.yaml | gui-17 | external (product / v1.x) | abort-confirmation-suppression setting (mkvtoolnix `m_warnBeforeAbortingJobs` analog) parked as v1.x settings candidate | NOT LANDED | UNCLEAR | no abort-confirm/`warnBeforeAborting`-style setting found in `src/` Vue components/views (`SettingsDialog.vue` etc.; grep empty). |
| product-boundaries.yaml | cli-07-ndjson-deferred | external: v1.x demand | streaming NDJSON event mode (`--json-events`) riding the D13 enum, explicitly not built in Plan 4 | NOT LANDED | UNCLEAR | `executor/queue.rs:19` comment still reads "...a future `--json-events` streams it" (future tense); no `--json-events` flag or NDJSON emission code found anywhere. |
| conventions.yaml | core-20-ondisk-cache | in-memory per-session cache suffices for v1, no measured need | identification cache is in-memory per session (keyed path+mtime+size); on-disk cache a future candidate | NOT LANDED | UNCLEAR | `identify.rs:301-312`: `IdentifyCache` is a plain `HashMap<PathBuf, (CacheKey, Identification)>`; doc comment at line 303 still says "On-disk caching is a future candidate (spec non-goals)." No `Serialize`/`Deserialize`/persistence code on the struct. |
| conventions.yaml | core-84-regex-recompile | later cleanup pass | regex recompiled per `matches()` call, deferred to a later cleanup pass | NOT LANDED | NOT CLEARED | `matcher.rs:74` still calls `regex::Regex::new(pattern)` fresh inside the per-item `matches()` function body, no precompilation/caching (e.g. no `once_cell`/pre-parsed `Regex` field). |
| conventions.yaml | exec-03-mkvpropedit-fastpath | v1.x optimization; full remux path ships first | mkvpropedit fast path for metadata-only changes deferred | NOT LANDED | NOT CLEARED | repo-wide `grep -rn mkvpropedit` across `crates/` returns zero hits; only the full remux (mkvmerge) path exists. |
| process-conventions.yaml | proc-54-doc-deliverables-deferred | external: awaiting the 1.0 tag | 1.0 doc formats fixed (single-file EN `GUIDE.md` + two EN+DE blog posts) but authoring deferred to the 1.0 tag via three fresh sessions | NOT LANDED | NOT CLEARED | no `GUIDE.md` anywhere in the repo (`find -iname GUIDE.md` empty); `git tag -l` and `git ls-remote --tags origin` both return empty - no 1.0 tag exists yet. |
| process-conventions.yaml | ci-04-dependabot-cadence | go-public/1.0 timing (free only once the repo is public); activation remains Şenol's call | Dependabot/Renovate activation deferred to Şenol's call; cadence a CI-cost decision (every dependency PR triggers the full 3-OS matrix) | NOT LANDED | CLEARED (go-public component) | `gh repo view --json nameWithOwner,isPrivate,visibility` on `senolfeldmann/Muxsmith` returns `{"isPrivate":false,"visibility":"PUBLIC"}` - the repo is now public, clearing the "free only once public" precondition named in `blocked_on`. No `.github/dependabot.yml`/renovate config exists (`.github/` contains only `workflows/ci.yml`), so the deferred activation itself has not landed, and the entry's own "activation remains Şenol's call" residual gate is untouched by this check. |
| process-conventions.yaml | ci-13-packaging-deferred | external - go-public / paid-minutes cost | release-tag packaging artifacts (msi/dmg/deb/rpm/AppImage) deferred until go-public | NOT LANDED | CLEARED | same repo-public evidence as ci-04 above. `.github/` contains only `workflows/ci.yml` - no release/packaging workflow, no msi/dmg/deb/rpm/AppImage reference anywhere under `.github/`. |

**Counts**
- decision-ledger.yaml: 14 blocked entries
- product-boundaries.yaml: 6 blocked entries
- conventions.yaml: 3 blocked entries
- process-conventions.yaml: 3 blocked entries
- **Total: 26 blocked entries**
- LANDED: 0 / 26
- CLEARED (blocked_on condition observably fired): 2 / 26 (ci-04-dependabot-cadence, ci-13-packaging-deferred - both via the repo going public)
