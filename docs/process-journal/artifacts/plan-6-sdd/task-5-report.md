# Task 5 report: D44 - ts-rs bindings, committed, with a CI drift check

Branch `plan6-d`, worktree `/home/senol/Git/Muxsmith/.worktrees/plan6-d`.
Commit `1f02004` on top of `5349ed0` (streams A+B already merged).

## What was implemented

- `crates/muxsmith-core/Cargo.toml`: `ts-rs = { version = "12.0.1", optional = true }`
  and a `ts = ["dep:ts-rs"]` feature, with a doc comment recording the
  isolation rationale.
- `.cargo/config.toml` (new): `[env]` block with `TS_RS_EXPORT_DIR = { value
  = "src/bindings", relative = true }` and `TS_RS_LARGE_INT = "number"`.
- `crates/muxsmith-core/src/profile/model.rs`: `#[cfg(feature = "ts")] use
  ts_rs::TS;` plus `#[cfg_attr(feature = "ts", derive(TS), ts(export,
  export_to = "profile.ts"))]` on all 18 model types defined there (12
  structs + 6 enums).
- `crates/muxsmith-core/src/profile/match_expr.rs`: same cfg-gated import
  and derive on `Scalar` (enum) and `MatchExpr` (struct) - the 13th struct
  and 7th enum of the 20-type model set.
- `crates/muxsmith-core/src/planner.rs`: same cfg-gated import and derive
  on `StructuredEdit` (the 21st type, D49) - the one ts concern Task 6
  deferred.
- `crates/muxsmith-core/tests/ts_export.rs` (new): `#![cfg(feature =
  "ts")]`-gated integration test. Doesn't call ts-rs's `export()` itself -
  ts-rs's own `#[ts(export)]` attribute generates one hidden `#[test]` per
  type that writes `profile.ts` as a side effect of `cargo test`. This file
  supplies the one thing ts-rs cannot: `keywords.ts`, the D44 `:597-611`
  emitter verbatim, unit-testing `FilenameCfg`/`SourceCfg`/
  `ChaptersCfg`/`TitleCfg`'s `KEYWORDS` consts (Task 3) into `as const`
  arrays.
- `src/bindings/profile.ts`, `src/bindings/keywords.ts` (new, committed,
  generated - not hand-written).
- `.github/workflows/ci.yml`: new `TS bindings are not stale` step, Linux
  leg only, with the step-8 known-hole comment.

## Measured checks, in step order

**Step 1 - registry verification.** `curl -A "muxsmith-plan6-verify
(senol.feldmann@gmail.com)" https://crates.io/api/v1/crates/ts-rs` (the
first bare `curl -s` attempt returned a body with no `crate` key - crates.io
requires a User-Agent header; retried with one):

```
max_stable_version: 12.0.1
newest_version: 12.0.1
```

Matches the plan's pin exactly. No NEEDS_CONTEXT.

**Step 2 - isolation before any derive exists.**
`cargo tree -p muxsmith-core | grep -c ts-rs` -> `0`.

**Step 6 - generation and inspection**, `cargo test -p muxsmith-core
--features ts`: 19 tests in the crate's other integration suites plus the
new `emit_keywords_ts` all passed; `ls -1 src/bindings/` -> `keywords.ts`,
`profile.ts`.

`src/bindings/keywords.ts` (full file, 5 lines):
```ts
// @generated by `cargo test -p muxsmith-core --features ts`
export const FILENAME_KEYWORDS = ["keep"] as const;
export const SOURCE_KEYWORDS = ["primary"] as const;
export const CHAPTERS_KEYWORDS = ["keep", "drop"] as const;
export const TITLE_KEYWORDS = ["keep", "clear"] as const;
```

`src/bindings/profile.ts` (405 lines, 21 exported types). `Scalar`:
```ts
export type Scalar = boolean | number | number | string;
```
matches D44's measured shape exactly (duplicate `number` = `Int`/`Float`
both mapping to `number`, cosmetic). `grep -c bigint src/bindings/profile.ts`
-> `0`.

`StructuredEdit` (doc comments elided for the shape comparison; full text is
in the committed file):
```
export type StructuredEdit = { "kind": "add_exact", property: string, value: Scalar, }
                             | { "kind": "add_not_exact", property: string, value: Scalar, }
                             | { "kind": "add_substring", value: string, }
                             | { "kind": "add_not_substring", value: string, };
```
matches D49 `:1214-1218` exactly: `value: Scalar` on `add_exact`/
`add_not_exact`, `value: string` on the substring variants.

**Step 7 - isolation re-proof (default build, derives now present).**
`cargo tree -p muxsmith-core | grep -c ts-rs` -> `0`.

**Positive control (falsifiability check, `proc-verification-step-must-be-falsifiable`).**
`cargo tree -p muxsmith-core --features ts | grep -c ts-rs` -> `2` (`ts-rs`
+ `ts-rs-macros`). Confirms the absence-check in steps 2/7 can actually fire
and isn't vacuously green.

**Step 9 - drift check fires and clears.** First attempt was invalid and
discarded: I ran the append/diff/revert sequence before `git add`-ing
`src/bindings/`, so `git diff --exit-code` compared an untracked file
against nothing and returned `0` on both the drifted and "reverted" runs -
and `git checkout src/bindings/keywords.ts` silently failed (`pathspec ...
did not match any file(s)`, non-fatal in the shell chain), leaving stray
`// drift` text in the working file. Caught it, regenerated `keywords.ts`
via `cargo test -p muxsmith-core --features ts --test ts_export` (confirmed
byte-identical to the first generation - deterministic), `git add`-ed
`src/bindings/` (this is also what step 10 does), and reran the proof
correctly:

```
$ printf '\n// drift\n' >> src/bindings/keywords.ts
$ git diff --exit-code src/bindings/ ; echo exit=$?
[... diff showing the added "// drift" line ...]
exit=1
$ git checkout src/bindings/keywords.ts
$ git diff --exit-code src/bindings/ ; echo exit=$?
exit=0
```
Post-revert file content verified identical to the clean generation (5
lines, no drift text).

## Gate results (nine parts, foreground, in order)

1. `cargo fmt --all --check` - clean.
2. `cargo clippy --workspace --all-targets -- -D warnings` - clean (workspace-wide:
   confirms `ts` does not leak into `muxsmith-cli`/`muxsmith-gui` builds).
   Also ran `cargo clippy -p muxsmith-core --all-targets -- -D warnings`
   both without and with `--features ts` individually - both clean, proving
   the `#[cfg(feature = "ts")] use ts_rs::TS;` gating doesn't trip an
   unused-import warning on the default build.
3. `cargo test --workspace` - **486 passed, 0 failed** across 39 test
   binaries/suites (summed from every `test result: ok` line). The new
   `ts_export.rs` binary compiled to 0 runnable tests on this default
   (no-`ts`-feature) invocation, confirmed via `cargo test -p muxsmith-core`
   showing `Running tests/ts_export.rs` / `running 0 tests` - the
   `#![cfg(feature = "ts")]` file-level gate strips its content entirely
   when the feature is off.
4. `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps` - clean.
5. `cargo deny check` - `advisories ok, bans ok, licenses ok, sources ok`
   (exit 0). `ts-rs`, `ts-rs-macros` (MIT, verified via crates.io API) and
   `termcolor` (`Unlicense OR MIT`) all resolve against the existing
   `deny.toml` allow list without changes. No new `multiple-versions`
   warning attributable to this change (grepped the full output for
   `ts-rs`/`termcolor`/`thiserror`: none appear in a duplicate-entries
   block; `thiserror 2.0.18` was already present in `Cargo.lock` on
   `master` before this task).
6. `pnpm lint` - clean (exit 0).
7. `pnpm build` (`vue-tsc --noEmit && vite build`) - clean. Confirms the
   design's stated fact that nothing in `src/` imports the bindings yet
   (that's Task 9, wave 3).
8. `pnpm check:i18n` - `check-i18n: ok (19 source files scanned, 181
   catalog ids, 12 unused warning(s), ...)`. The 12 unused-key warnings are
   pre-existing IPC-error catalog entries, unrelated to this change.
9. `pnpm test:e2e` - `7 passed (1.0s)`.

`pnpm install` was run first (node_modules was missing in the fresh
worktree, as expected).

## Files changed (commit `1f02004`)

```
.cargo/config.toml                             |   3 + (new)
.github/workflows/ci.yml                       |  18 +
Cargo.lock                                     |  32 +
crates/muxsmith-core/Cargo.toml                |   8 +
crates/muxsmith-core/src/planner.rs            |   3 +
crates/muxsmith-core/src/profile/match_expr.rs |   4 +
crates/muxsmith-core/src/profile/model.rs      |  20 +
crates/muxsmith-core/tests/ts_export.rs        |  36 + (new)
src/bindings/keywords.ts                       |   5 + (new)
src/bindings/profile.ts                        | 405 + (new)
```
Staged exactly per the brief's step-10 file list; no `git add -A` used.
Cargo.lock's new entries: `ts-rs 12.0.1`, `ts-rs-macros 12.0.1`,
`termcolor 1.4.1` (the last a transitive dependency of `ts-rs-macros`).

## Self-review

- All 21 types carry `#[cfg_attr(feature = "ts", derive(TS), ts(export,
  export_to = "profile.ts"))]`: `grep -rc 'cfg_attr(feature = "ts"'` gives
  18 (model.rs) + 2 (match_expr.rs) + 1 (planner.rs) = 21.
- No bare `use ts_rs::TS;` anywhere - both occurrences are `#[cfg(feature =
  "ts")]`-gated, and clippy `-D warnings` on the default build (which would
  catch an unused import) is clean.
- Bindings are committed (`git show --stat HEAD` lists both files as
  `create mode`).
- The CI step is Linux-leg only (`if: runner.os == 'Linux'`), same shape as
  the existing `check:i18n`/Playwright steps.
- The step-8 known-hole comment is present in `ci.yml`, stating the
  never-committed-file blind spot and naming Task 9 as the wave-3 closer.
- 20-type set closure: verified by direct enumeration against
  `model.rs`/`match_expr.rs` rather than trusting the design doc's citation
  (D45's cited `:768-782` range turned out to state the *count* "13 structs
  ... 7 enums" derivationally, not list the 13 struct names) - counted
  directly in the source: `Profile`, `Meta`, `Input`, `OutputCfg`,
  `TemplateBlock`, `ExternalBlock`, `TrackRule`, `Locator`,
  `AttachmentsCfg`, `TracksCfg`, `AttachmentRule`, `TagsCfg` (12, in
  `model.rs`) + `MatchExpr` (1, in `match_expr.rs`) = 13 structs;
  `FilenameCfg`, `CollisionPolicy`, `KeepDrop`, `SourceCfg`, `ChaptersCfg`,
  `TitleCfg` (6, in `model.rs`) + `Scalar` (1, in `match_expr.rs`) = 7
  enums - exactly the closed set the task brief names. No type outside
  this set was touched; no NEEDS_CONTEXT triggered.

## Concerns

- None blocking. One process note worth flagging for future task briefs:
  step 9's drift-proof procedure implicitly assumes `src/bindings/` is
  already staged (in the git index) before the append/diff/revert sequence
  - the brief's own step ordering (generate in step 6, stage+commit only in
  step 10) leaves a window where a literal reading of step 9 silently
  no-ops (`git diff --exit-code` against an untracked path always exits 0).
  I resolved it by staging before running step 9 (which step 10 needs
  anyway), but a future brief could make this explicit rather than relying
  on the executor to catch a silent-pass first attempt.
