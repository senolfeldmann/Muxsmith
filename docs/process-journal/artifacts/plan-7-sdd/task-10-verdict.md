# Task 10 review verdict: NEEDS FIXES

> **Round 2 (fix review, commit 8d0e860): APPROVED.** Disposition at the end of this file; the round-1 body below is preserved unchanged.

Reviewer: independent SDD task reviewer, Plan 7 Task 10 (editor control topics 10-18, D54 rows 10-18, en+de, 18 files).
Commit reviewed: 20c2586 on plan7-e (`/home/senol/Git/Muxsmith/.worktrees/plan7-e`; worktree clean at that commit, diff touches exactly the 18 help files, so the code beside them is the real tree).
Ground truth used: plan Task 10 + shared Tasks-8-10 rules (2026-07-21-plan-7-help-i18n.md:790-836), design D51/D54 rows 10-18, v1 spec 4.3-4.9 + 5.2, conventions.yaml (core-80), REAL tree (command.rs, validate.rs, planner.rs, discovery.rs, model.rs, registries.ts, keywords.ts, EditorView.vue, ListWidget.vue, PropertyMapWidget.vue, OptionalFlagWidget.vue, locales/en+de/gui-editor.ftl, diagnostics.ftl), sibling verdicts task-08/task-09 for cross-stream consistency.

One blocking content-accuracy finding; everything else is clean or owner-pass material.

## Dimension 1: content accuracy (load-bearing claims)

Spot-verified the hard claims directly against spec and code:

- **Keep-mode track order** (tracks-unmatched + tracks-rules vs spec 4.5, core-80, executor): "primary tracks first in the primary's own order, then donor tracks in rule order; kept-but-unmatched primary tracks count as matched; a rule matching a primary track does not reposition it; reordering primary tracks requires drop" - exact. conventions.yaml core-80 (option B, live-verified against v100) and `push_track_order_keep` (command.rs:283-297: `primary_track_ids` as `0:tid` first, donor assignments trailing, matched-primary not repeated) both confirm every clause.
- **Empty-rules legality** (both tracks topics vs spec 4.5/D38 + validate.rs:61-75): empty rules under `keep` legal with info-severity `PassthroughProfile`; under `drop` a `NoTrackRules` error. The topics' "validate announces that case with an info notice" and "under drop at least one rule is required" are code-true. The de wording "reiner Passthrough-Remux" matches diagnostics.ftl:15 verbatim.
- **match_to_source** (vs spec 4.6, model.rs:361-366, validate.rs:460-472, discovery.rs:140-166, OptionalFlagWidget.vue): only-true flag (explicit `false` is rejected `InvalidKeyword`; the widget writes `true`/`undefined`, so "clearing it removes it" is exact); sugar for `match_pattern: '{match}'`; containment via unanchored regex search on the donor basename (`re.is_match(name)`); case-insensitive unless `case_sensitive`; mutual exclusivity (`LocatorConflict`); two files = `AmbiguousExternal`, zero = `MissingExternal` "for a non-optional use" (spec 5.2:262) - all exact. The no-flag-no-pattern fallback claim ("every file the locator finds is a candidate") is code-true: empty template renders an empty regex, which matches every name (discovery.rs:146).
- **match_pattern** (vs spec 4.7 + discovery.rs): template-in-regex-mode, interpolated values regex-escaped literals, surrounding text is regex, searched (not anchored) against candidate basenames, case handling, fields and filters, `{source_stem}` literal-mode-only (validate.rs:475: `UnknownTemplateField` there) - exact. The worked example is spec 4.7's own example verbatim, including the case-insensitive `Staffel3Episode1` hit.
- **Exact match** (vs spec 4.3/4.4): typed equality (`6` == `6.0`; `de` == `ger`, `pt-Latn-BR` == `pt-BR`, `pt-BR` != `pt-PT`; strings case-sensitive), curated domains (`type`/`codec_kind` config-time `InvalidPropertyValue`, `language` plan-time against `mkvmerge --list-languages`), `codec_kind` exact-only (`CodecKindExactOnly`) with the pattern-match-`codec_id` redirect, booleans absent-means-false including the `flag_hearing_impaired: false` example, `raw:` bypass (no existence/type/domain check, no normalization, no absent-false shortcut, byte-literal equality, every bypass flagged - `RawProperty` info / `RawOnKnownProperty` warning / plan-time `UnknownPropertySkew`, spec 4.4) - all exact.
- **Attachments** (vs spec 4.9, validate.rs:228-257, planner.rs:1068-1129): exactly-one-of select/drop/add (`AttachmentRuleShape` enforces it, so "each rule does exactly one of three things" is code-true), first-match-wins in list order, fall-through to `unmatched`, no uniqueness constraint, algebra over `file_name`/`content_type`/`description`, `unmatched` default `keep` with the deliberate asymmetry to tracks' default `drop` and the fonts/ASS rationale (spec 4.9 verbatim), add-locator pairing using the same mechanism (planner calls `discovery::resolve_locator` with the primary's identifier) - all confirmed. One imprecision: finding 2.
- **Chapters** (vs spec 4.9 + planner.rs `resolve_chapters`:1002-1040): `keep`/`drop` keywords (keywords.ts: CHAPTERS_KEYWORDS `["keep","drop"]`), external locator resolving to exactly one file per primary, zero = `MissingExternal` error, >= 2 = `AmbiguousExternal`, XML or simple format - exact. The topic correctly offers no optional escape for chapters (the planner comment states there is none).
- **Title** (vs spec 4.9/4.7 + keywords.ts): `keep`/`clear` keywords (TITLE_KEYWORDS `["keep","clear"]`), template in literal mode, `{source_stem}` available, fields from `Input.pattern` capture groups, filters - exact. Widget shape confirmed: `keywordOrBlock` + `templateBlock` (registries.ts:90-93); chapters is `keywordOrBlock` + `externalBlock` (:85-88).
- **Grid mechanics** (tracks-rules vs EditorView.vue): rows draggable with dragstart/drop/dragend (:489-493), click-to-select via the source-cell button (:497-501), detail panel below the grid editing the selected rule through SectionWidget (:519-528), column set source/match/optional/changes matching the topic's row-summary list - confirmed. Suggestion-card narrowing claim confirmed (core-33, SuggestionCard.vue on BatchView). **But see finding 1: the Add/Remove sentence is false.**

## Dimension 2: rule conformance

- **h1 opener**: all 18 files open with an h1 naming the surface (see Q2 for the wording-vs-label question).
- **Markdown subset**: clean. No fences, no tables, no raw HTML, no markdown links, no URLs (grep runs foreground this session; every pattern fire-verified, see below).
- **Size band**: all 18 within 1-3 kB. en 1189-2029 bytes, de 1309-2273 bytes (`wc -c` per file).
- **File set**: exactly D54 rows 10-18 x 2 locales; `ls help/en help/de | sort | uniq -c` shows each of the 9 ids exactly twice and nothing else; commit 20c2586 touches exactly those 18 files (18 files changed, 360 insertions); no Task 9 id present anywhere in this worktree's `help/`.
- **Commit**: message `help: editor control topics 10-18, en+de (D54)` + house trailer, exactly as plan Step 3 specifies.

## Dimension 3: de quality

- **Register**: du-imperative throughout ("Ziehe", "setze", "Nimm", "schreibe", "Klicke", "verenge", "Nutze", "bevorzuge", "wähle ... ab"), per the plan's Tasks-8-10 rule; config keywords literal (`keep`, `drop`, `clear`, `exact`, `substring`, `regex`, `any`, `not`, `raw:`, `{match}`, property names).
- **Independence**: genuinely authored de prose, not an en echo - native recastings throughout ("über sein Schicksal", "Begleitmaterial", "unnachgiebig", "sonst sind sie weg"), structure parallel but phrasing independent, consistently longer than en as native de runs.
- **Catalog terminology**: anchored. "Nicht zugeordnet", "Match zur Quelle", "Match-Muster", "Groß-/Kleinschreibung beachten", "Auswählen"/"Verwerfen"/"Hinzufügen", "Kapitel", "Titel", "Optional", "Match" match locales/de/gui-editor.ftl exactly; "Spender-Datei", "Quelldatei", "Passthrough-Remux" match diagnostics.ftl; "Verweis"/"Vorlage"/"Eigenschaft" follow the de catalog header's own term table.
- **Orthography**: real umlauts/ß throughout; no banned glyphs (em/en dash, typographic dashes, curly quotes, ellipsis, NBSP - grep -P scan clean, fire-verified).

## Fire-verification (dimension 6)

All three absence checks were made to produce output once: a control file (scratchpad copy of `editor-profile-title.md`, outside the worktree) seeded with `https://example.com`, an em-dash, curly quotes, a code fence, raw HTML, and a pipe table. URL pattern: 1 hit; glyph pattern: 1 hit; markdown-subset pattern (fence/table/link/HTML): 4 hits. The same patterns return empty over the real `help/` tree (exit 1 each). The absence results are therefore trustworthy.

## Dimension 4: house

- Cross-topic references are prose ("see the Match topic" / "siehe das Thema Match"), never links - D62 check 4 posture, uniform (4 out-of-stream + all in-stream sites).
- Diagnostic concepts are described in words ("ambiguity error", "missing-external error") rather than by DiagCode, unlike Task 9's backticked codes - a legitimate depth choice for control-level topics (the codes belong to the topics whose surface shows them), noted for the owner pass, not a defect.
- Scope discipline: rows 10-18 covered per their justification lines; algebra detail correctly deferred to Task 9's Match topic with a pointer, mirroring D54's partition (the exact topic carries only what row 10 assigns it: types, domains, `raw:`, booleans).
- core-80 respected in both tracks topics, including the reversal's user-facing consequence (reordering primaries requires `drop`) - the exact usability trap the ledger entry records.

## Findings

1. **(blocking)** `help/en/editor-tracks-rules.md` + `help/de/editor-tracks-rules.md`, "Editing a rule": "`Add` appends a new rule, `Remove` deletes the selected one." / "`Hinzufügen` hängt eine neue Regel an, `Entfernen` löscht die ausgewählte." **These controls do not exist on the tracks grid.** The bespoke grid (EditorView.vue:454-530) renders the unmatched select, the table, and the detail panel - no add/remove buttons anywhere in the view (buttons present: open, profile-pick, recents, rule-select, save). The generic `editor-action-add`/`-remove` buttons live only in ListWidget.vue and PropertyMapWidget.vue, neither of which the tracks grid uses (`topLevelFields` excludes `tracks`; the registry's `tracks.rules` list widget is bypassed by the bespoke fieldset). No wave-2/3 task adds them (Tasks 13-16 checked: selection, markers, dropdowns, ordinal column only). The sentence describes a nonexistent affordance in both locales - precisely the plausible-prose failure this review dimension exists to catch. Fix: delete or rewrite the sentence to the real mechanics (rules are edited via row selection + detail panel; the profile YAML is the current route for adding/removing rules). Side-product for the controller, outside this task: the editor genuinely has no way to add or remove a track rule, and spec 8.2's editor description does not list one either - worth an IDEAS/product-boundaries entry, because the help author's reflex ("obviously there is an Add button") is how users will react too.
2. **(minor)** `editor-attachments-rules.md` (both locales), "Sets, not single picks": "Zero matches is not an error either; a rule that matches nothing simply does nothing." True for `Select`/`Drop` (no diagnostic, planner.rs:1076-1096). An `Add` rule whose locator finds zero files, however, emits a `MissingExternal` **warning** (planner.rs:1111-1118) - deliberately a warning, not an error, so the "not an error" half is literally correct, but "simply does nothing" hides a user-visible diagnostic. One qualifying clause fixes it ("an Add rule that finds no file additionally warns").
3. **(minor)** The two locator topics state uniqueness/escape rules in track-rule terms only: "zero is a missing-external error, unless the rule is `Optional`" and "the rule's match expression selects exactly one track". The same help-ids serve the locator fields inside the chapters external block (no optional escape exists there - planner.rs:1006-1007 - and no match expression) and the attachment `Add` section (warning, no uniqueness). In two of the three surfaces the id serves, the Optional escape promised does not exist. The chapters topic itself states its own rules correctly, which mitigates. Fix or owner-pass: one sentence scoping the Optional clause to track rules ("bei Spurregeln").
4. **(info)** en topics write "`Case sensitive`" where the en catalog label is "Case-sensitive" (locales/en/gui-editor.ftl:58). de matches its catalog exactly. Owner-pass wording item.

## Q1 adjudication: cross-topic prose references to Task 9 topics landing in a parallel worktree

**Ruling: acceptable merge-resolved coupling; do not soften.**

For softening: a prose reference is anchored to a topic *title* concept, not a file id; a Task 9 fix loop could reword a title and leave "the Match topic" pointing at stale wording; and no structural gate checks prose references (D62 checks files), so the coupling is invisible to automation. Against softening: every referenced topic (`editor-track-rule-match-expr`, `editor-track-rule-optional`, `editor-input-pattern`) is inside D54's closed id set, which the design freezes ("the id set, file set and host elements above are closed here"); the plan's shared rules *prescribe* exactly this form ("cross-topic references are prose, 'see the Match topic', never links"); Task 9 is already committed and approved (34a5aa7, task-09-verdict APPROVED) with matching reference posture in the other direction; and softening to vaguer phrasing ("the match documentation") degrades the user's ability to find the topic in the sidebar for zero real risk reduction. The references can only dangle if a whole stream fails to merge, which the nine-part gate catches. Residual risk (title wording drift) is exactly what the owner's plan-close rendered-surface pass sweeps; recommend that pass do one cross-reference sweep over all 44 files.

## Q2 adjudication: h1 disambiguation beyond the bare catalog label

**Ruling: within the shared rule; alignment rides the owner pass, not this fix loop.**

For calling it drift: the provisional rule says "an h1 naming the surface", the catalog names the surface "Rules"/"Regeln", and the rendered grid heading shows exactly that; "Track rules"/"Spurregeln" is wording no catalog contains, and user-visible wording rides the owner's surface pass (latitude-carveout), so a stream inventing display names oversteps. For conformance: the rule's own text demands *naming the surface*, not *reproducing the label* - and the bare labels cannot name these surfaces, because they collide pairwise (`editor-tracks-rules` and `editor-attachments-rules` are both "Rules"/"Regeln"; both `unmatched` ids are "Unmatched"/"Nicht zugeordnet"). A sidebar h1 read without hover context that says only "Rules" fails the naming requirement worse than one that adds the disambiguating noun. Disambiguation is therefore required here, and the chosen forms use catalog vocabulary (Spur, Regeln, Anhänge, Match). The genuine issue is cross-stream: Task 9 used `<label> (<section>)` (its verdict recommends that as branch convention), Task 10 uses natural-language compounds ("Track rules", "Unmatched attachments"). That is a 22-topic consistency property no single stream can see or fix; per the Task 9 ruling, it belongs to the owner's plan-close surface pass / whole-branch review, which should pick one scheme (the parenthetical form generalizes mechanically; Task 10's compounds read more naturally - owner's call) and normalize all 22.

## Q3 adjudication: two spec-backed extensions beyond the D54 justification line

**Ruling: within D54's when-to-use scope; do not strip.**

For stripping: the plan calls the justification column "the topic's required content"; rows 13/14 name ordering and grid semantics, not empty-rules legality, and row 14 does not name suggestion cards; the owner's option-B ruling is "explicitly NOT inflationary", and unrequested content is the first step of inflation. Against stripping: the shared rule's own definition of content is "what the control/view does, when to use it, interactions with other settings - the beyond-tooltip depth that earned the topic its slot"; "covers the D54 justification line" is a floor, not a ceiling. The empty-rules/PassthroughProfile passage is literally an interaction between the two controls these topics document (`unmatched` value flips an empty `rules` list between error and legal-with-info-notice, spec 4.5/D38) and surfaces on this exact editor surface via validate-on-edit - omitting it would leave a user staring at a `NoTrackRules` error the topic never explains. The suggestion-card sentence is the documented remediation for the ambiguity error row 14's uniqueness content must explain, and D54's own `batch-suggestion-card` row establishes narrowing (core-33) as sanctioned content. Both extensions are one sentence, spec-verified true, and both files sit mid-band; the option-B inflation guard governs the topic *set*, not the sentence count inside an included topic - the 1-3 kB band is the size guard, and it holds.

## HARVEST

- **Over-restriction watch on the provisional content rules (veto window open):** none of the three bit in Task 10. Markdown subset: no topic wanted a table, fence, or link (the config example fit inline code). 1-3 kB band: all 18 land 1.19-2.27 kB with no padding and no truncation smell - the band reads correctly calibrated for control topics. h1 rule: underspecified exactly where labels collide (Q2); when the veto window closes, sharpen it to "an h1 naming the surface unambiguously - the bare catalog label where unique, a disambiguated form where labels collide" and pick one disambiguation scheme branch-wide.
- **Product gap for the controller:** no add/remove affordance for track rules anywhere in the editor (finding 1's side-product). Neither spec 8.2 nor any plan-7 task provides one. Route to IDEAS/product-boundaries; the fabricated help sentence is evidence users will expect it.
- **de-terminology for the owner pass:** fully catalog-anchored; "matcht/gematcht" as a verb is new coinage consistent with the header's Match-loanword policy, alternating with the "zugeordnet" noun family (`Nicht zugeordnet`) - a consistent verb/noun split worth one conscious ratification. "Spender-Datei"/"Quelldatei" follow diagnostics.ftl. en "Case sensitive" vs catalog "Case-sensitive" (finding 4).
- **Owner-pass sweep list fed by this review:** h1 scheme normalization (Q2), cross-reference sweep over all 44 files (Q1), DiagCode-in-prose vs described-in-words convention divergence between streams D and E (house note), findings 3-4 if not taken in the fix loop.
- **Fix-loop scope:** finding 1 (mandatory, both locales), findings 2-3 (cheap, same commit recommended). Finding 4 and all Q-rulings ride the owner pass.

---

## Round 2 disposition (fix review): APPROVED

Same reviewer. Commit reviewed: 8d0e860 on plan7-e (20c2586 untouched beneath it; worktree clean; house trailer present). Diff: exactly 8 files, 8 insertions, 8 deletions - the four topic pairs findings 1-3 named, nothing else.

**Finding 1 (blocking) - FIXED.** The fabricated sentence ("`Add` appends a new rule, `Remove` deletes the selected one." / "`Hinzufügen` hängt eine neue Regel an, `Entfernen` löscht die ausgewählte.") is deleted in both locales with no replacement. Survival grep over all 18 files (`append|new rule|neue Regel|hängt eine|deletes|löscht|Entfernen|Remove`, case-insensitive) returns 5 hits, each contextually reviewed and legitimate (attachment `Drop`/`Add` rule-kind semantics, chapters `drop`, the match-to-source flag's "clearing it removes it"); none concerns creating or deleting track rules. Pattern fire-verified against the pre-fix content: the same grep over `git show 20c2586:help/{en,de}/editor-tracks-rules.md` catches the removed sentence in both locales. The remaining "Editing a rule" paragraph is complete against the real affordances: row select (click), detail panel below the grid with the four editable aspects; drag-reorder lives in the "Order is output order" section. No dangling reference in either locale.

**Finding 2 (minor) - FIXED.** The zero-match sentence now splits the cases: "a `Select` or `Drop` that matches nothing simply does nothing, and an `Add` whose locator finds no file emits a warning but does not fail the plan." Code-exact: planner.rs:1076-1096 emits no diagnostic for unmatched select/drop expressions; planner.rs:1111-1118 emits `Diagnostic::warning(MissingExternal, ...)` for a zero-hit add, and the function comment states the design intent verbatim ("not an error: ... a miss must never suppress the plan"). de counterpart faithful: "meldet eine Warnung, lässt den Plan aber bestehen".

**Finding 3 (minor) - FIXED.** Both locator topics, both locales, now scope the escape: "an escape that exists only on track rules (external chapters and attachment `Add` locators have none)" / "Diesen Ausweg gibt es nur bei Spurregeln (externe Kapitel und `Hinzufügen`-Verweise bei Anhängen haben keinen)". Verified: chapters has no optional escape (planner.rs:1006-1007, "zero matches is always `MissingExternal`"), and `AttachmentRule` carries no optional flag (model: select/drop/add only). Residual nuance, info-level: for attachment `Add` the zero-hit outcome is a warning rather than an unescapable error, so "have none" is literally true (no Optional flag exists there) but reads slightly sterner than the Add case's real severity - the accurate outcome is stated one topic over (finding 2's fix). Owner-pass polish candidate at most; not a blocker.

**Finding 4 (info) - not addressed, as ruled.** Owner-pass routed ("Case sensitive" vs catalog "Case-sensitive").

**No regression.** The 10 unnamed files are byte-identical to 20c2586 (the diff stat lists only the 8). Touched files stay in band: 1335-2347 bytes (max 2347 < 3072). Re-run structure checks over the whole tree: URL grep clean, banned-glyph grep clean, markdown-subset grep clean (all three patterns fire-verified in round 1 against a seeded control), every file still opens with exactly one h1 and no h1 changed in the diff. New prose uses only subset constructs (inline code, plain prose, spaced ASCII hyphens).

**Verdict: APPROVED.** Fix-loop closed. Open items riding the owner pass, unchanged from round 1: finding 4, the finding-3 severity nuance, h1 scheme normalization (Q2), cross-reference sweep (Q1), DiagCode-vs-prose convention divergence, and the product-gap escalation (no track-rule add/remove affordance in the editor) for the controller.
