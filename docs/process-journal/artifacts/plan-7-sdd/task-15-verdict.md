# Task 15 verdict: D58 curated-domain dropdowns in exact-match value cells

**Reviewer:** independent (fresh eyes), commit `9f4aa8a`, branch `plan7-f`, worktree `/home/senol/Git/Muxsmith/.worktrees/plan7-f`.

## Combined verdict: APPROVED

- **Spec compliance: PASS** - every D58 behavior present, exact values recomputed at source, nothing beyond the brief's four files.
- **Quality: PASS** - full gate green under my own build; fire-verification independently strengthened.

---

## Gate runs (all foreground, my own build)

| gate | result |
|---|---|
| `cargo test -p muxsmith-core --features ts` | green (all binaries), emitter re-run -> **`git diff --exit-code src/bindings/` clean = no drift** |
| `pnpm build` (vue-tsc --noEmit && vite build) | clean |
| `pnpm test:e2e` (harness+mount build + all specs) | **52 passed, 0 failed** (incl. 10 dropdowns, Task-14 markers, editor-tooltips all-widget mount) |
| `npx playwright test --grep dropdowns` | **10 passed** |
| `npx eslint` (touched files) + `npx eslint .` | clean, exit 0 both |
| `cargo fmt -p muxsmith-core -- --check` | clean (bonus, house rule ci-06) |
| `cargo clippy -p muxsmith-core --features ts --tests` | clean, no warnings (bonus) |

Exact-value recomputation against the Rust source (`capability/mod.rs`):
- `TYPE_VALUES` = `["audio","buttons","subtitles","video"]` -> **4**, matches emitted array.
- `CODEC_KINDS` has **17** tuples; `CODEC_KIND_NAMES` = `CODEC_KINDS.iter().map(|(k,_)| *k).collect()` (derived, cannot drift). The committed `settables.ts` lists all 17 keys in source order (srt..vp9). Derivation and count verified.
- Emitter re-run produced byte-identical `settables.ts` -> the D44 directory-scoped drift gate (`ci.yml:135-139`, `git diff --exit-code src/bindings/`) covers the change with no CI edit, exactly as the spec claims.
- Registry boundary verified at source: `matchExprFields.exact` is `propertyMap/matchable/scalar`; `substring`/`regex` are `matchable/string`; `changes` is `settable/scalar`. `exact` is the sole matchable+scalar map -> the four-condition gate targets exactly it, matching D58 verbatim.
- Task 14's `path?: string` prop consumed as designed (gate condition 2). Real-app integration confirmed: `editor-markers.spec` (rule 0 carries `exact:{type:"video"}`) renders the select and passes its axe check.

---

## Findings by severity

### Critical
None.

### Important
None.

### Minor
1. **Report count imprecision (no code impact).** The report's "`cargo test ... 15 passed`" understates the actual multi-binary run (the `ts_export` binary alone is 22; `validate_structure` is 15). Cosmetic reporting only; all green.
2. **Case 2's select-value assertion is vacuous for binding-correctness in isolation.** `toHaveValue("audio")` on a fresh `type:"audio"` mount would pass even with the `:value` binding broken, because `audio` is `TYPE_VALUES[0]` and a `<select>` with no resolved selection defaults to option 0 (I reproduced this: with `:value` removed, case 2 still passes). Not a defect - case 10 (`video`, the last option) is the non-vacuous binding guard and does catch it (see Q4). Recorded as test-adequacy calibration.
3. **Pre-existing doc drift, not this diff.** The widget's script comment still reads "catalog budget 45" (unchanged context line), while `editor-generic-action-keys` now records the budget as 46 (D59 revision). Out of this task's scope (untouched by the diff); flagged only for a future sweep.

---

## Adjudications

**Q1 - second header comment line: legitimate additive emitter change (not an unauthorized format change).**
The `@generated` marker line in `settables.ts` is byte-identical to `keywords.ts`'s first line (verified: both `// @generated by \`cargo test -p muxsmith-core --features ts\``); the brief/spec-mandated text ("type maps + curated matchable value domains") is appended as line 2. The whole file is emitter output committed and drift-gated - this is a regeneration change, not a hand-edit to a generated artifact. No consumer parses the header (grep across `src/`, `e2e/`, `scripts/`, `.github/`: only a source comment mentions the phrase; `check-i18n` reads `.ftl`, not `.ts`; the drift gate is a whole-file `git diff`). The brief said "the header LINE" (singular), but `settables.ts` carried only the `@generated` marker with no descriptive header to update; appending rather than overwriting the marker is the correct tree-adapted realization (brief-drafts-verified-against-tree), and the implementer surfaced the divergence explicitly. Within contract.

**Q2 - `cellKindFor(key) -> (key, value)`: component-internal, no outward surface observable.**
No `defineExpose` in the file; `cellKindFor` is a `<script setup>` top-level local, which Vue 3 exposes to the template only, never to other components absent `defineExpose`. The only external reference to the symbol is `e2e/.generated/mount-harness.js` - the compiled bundle of this same component, not a source consumer. Nothing outside `PropertyMapWidget.vue` can see the change. The zero-outward-effect structural grant covers it.

**Q3 - no new Fluent message owed: premise verified.**
`SelectWidget.vue` renders its options as raw `{{ option }}` (untranslated config tokens), not `$t` - the established, tested house pattern (smoke test "select widget renders a combobox of its raw (untranslated) domain tokens" passes). D58 and `gui-closed-domain-dropdowns` mandate no localized option labels; the option strings (`audio`, `h264`, ...) are literal YAML config values the user writes, not prose (aligned with `core-37-prose-free-core`). The `{{ opt }}` interpolation is a variable, not a template text node, so `@intlify/vue-i18n/no-raw-text` does not fire (eslint clean, confirmed). No message was created, so the bilingual-en+de constraint is not triggered. The dropdown correctly owes nothing.

**Q4 - `:value` + `@change` binding correct from observable behavior (source citation not relied upon).**
Independently falsified: removing `:value="row.value"` from the `<select>` and rebuilding the mount bundle makes **case 10 fail** ("unexpected value 'audio'" where "video" expected) - the select falls back to its default first option when the binding is gone. Restored byte-identically afterward (`git checkout`, tree clean). This proves (a) the binding is load-bearing and (b) case 10 (`video` = last, non-default option) is a real guard of it. The report's Vue-3.5.39-`mountElement` mount-order citation is a borrowed mechanism claim; it is not needed - the rendered selection is observably correct, and the write path (`selectOption("video")` -> model `{type:"video"}`) matches the text cell's `setValue` currency (`Scalar::Str`).

---

## HARVEST

- **Dominant patterns, all correctly applied:** `core-derive-dont-restate` (domains emitted through the drift gate, not hand-listed frontend-side - the D58 occurrence itself); `brief-drafts-verified-against-tree` (the "header line" singular mismatched the tree's marker-only header; adapted by appending and surfaced); `latitude-carveout-zero-content-structural-forks` (the `cellKindFor` signature widening and `onSelectInput` are internal, zero outward surface, additive test rows conform to sibling shape).
- **Over-restriction watch: none forced.** The structural-conformance grant covered the signature widening cleanly (verified internal per Q2); no stop it should have covered but didn't. Positive calibration data for the do-not-over-restrict direction, same shape as T14's `suppressSelfAnchor`.
- **Ledger candidate (test-adequacy heuristic):** an initial-render test of a `<select :value="x">` binding whose fixture value is the FIRST domain option is vacuous - the element default-selects option 0 regardless of the binding, so the assertion passes on a broken binding (reproduced here on case 2). Such a binding must be guarded by a case whose expected value is a NON-default option (case 10 does this with `video`=last). This is the `:value`-select analogue of the control-firing discipline in `proc-verification-step-must-be-falsifiable`; worth folding in as calibration for future select-cell tasks.
- **Controller note:** the "catalog budget 45" comment in `PropertyMapWidget.vue` predates the D59 45->46 budget revision (`editor-generic-action-keys`); a stale context line untouched by this task, worth a future doc sweep, not a fix here.
