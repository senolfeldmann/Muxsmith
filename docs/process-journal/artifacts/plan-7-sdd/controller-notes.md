# Plan 7 controller notes (process bookkeeping, not a tracker)

## T11/T13/T14 verdict-harvest mining EXECUTED (2026-07-22, session 21 start)

S20's context-budget cut landed between these verdicts and their mining;
executed at session-21 start per the verdict-arrival gate, n-in/n-out over
the 10 harvest items:

- T13 fire-verify-negatives -> reinforced occurrence on
  proc-verification-step-must-be-falsifiable (6->7).
- T14 Q1 over-restriction direction -> reinforced occurrence on
  latitude-carveout-zero-content-structural-forks (4->5).
- T14 illustrative-snippet/axe -> reinforced occurrence on
  brief-drafts-verified-against-tree (9->10).
- New Tier-1 entries: proc-plan-prescribed-code-collapses-grading (T11),
  code-comment-line-citations-drift (T11 minor), e2e-focus-delegation-
  needs-synthetic-events (T13 + T12-fix probe, count 2),
  e2e-filter-invokes-playwright-directly (T3 Q2 + T13, count 2),
  gui-provide-inject-template (T14), gui-single-anchor-per-config-path
  (T14 Q1 + F1, count 2), test-contract-needs-a-reaching-fixture (T14 F1).
- DROPPED with reason: T13's over-restriction note on the F2 fix scoping -
  consumed by the executed, correctly-scoped fix dispatch (f8e7d5d); the
  enumerate-the-trigger principle is framework baseline, no standing repo
  consideration remains.

## DESIGN APPROVED 2026-07-21 - approval-gated bookkeeping EXECUTED

Design approval complete: reviewer rounds 1-3 (round 3 APPROVED on the E1
re-fold delta) + owner approval ("ansonsten approved" with the E1
re-ruling folded). All queued mining executed, n-in/n-out:

- core-derive-dont-restate: 3rd occurrence (D58 strict fit) -> PROMOTED,
  migrated ledger -> conventions.yaml (tier 2, promoted_at 3).
- New Tier-1 entries: proc-enumeration-fired-by-execution (r2 harvest),
  proc-supersede-never-overwrite (count 2: S16 + S20 events),
  proc-post-sweep-invariant-framing (r3), gui-single-vhtml-site,
  i18n-closed-attribute-name-set, gui-helpid-equals-labelkey (r1
  candidates, agent-emergent per house precedent for design-approved
  decisions).
- Occurrence appends: proc-proposed-safeguard-stays (D62 retention),
  editor-generic-action-keys (trigger 10, budget 45->46).
- ROADMAP: trigger 11 folded into the mirrored trigger-2 line.
- DROPPED with reason: trigger-numbering stability (editorial case-by-case
  judgment, fails scrutiny gate 1); locale-pinning generalization beyond
  this repo (carried by D64 here; a framework-level hoist is not this
  project's call).
- IpcError render-funnel (r1) already has its Plan-9 ROADMAP line;
  URL-ban over-breadth stays recorded in D62 + safeguard-stays occurrence.

## Plan-phase routings (2026-07-21)

- Plan authored (docs/superpowers/plans/2026-07-21-plan-7-help-i18n.md,
  21 tasks, 4 waves, streams A-H), uncommitted pending review.
- Author's open Q1 (D64 funnel vs no-args test semantics): CONTROLLER
  RULING - muxsmith_bare() enumerated exception (test meaning never
  weakened silently; grep invariant holds). Internal technical fork,
  no contested criterion, recorded in the D64 amendment.
- Author's open Q2 (content-stream worktree count): ruled as planned -
  three parallel worktrees (disjoint new files, house parallelism
  default).
- Design defects 1-4 from plan-authoring: 1 (D62 empty-state note),
  3 (D64 exception), 4 (D54 paraphrase vs D52) -> design amendment via
  resumed author + resumed reviewer delta (round 4); 2 (D63 test 2
  mechanism under-specified) -> covered by the plan's synthetic-bundle
  implementation, no design change (the design names the WHAT, the plan
  the HOW; watch it in the plan review).
- Sequence: design amendment -> reviewer round 4 (delta) -> plan author
  updates Task 1 -> fresh plan reviewer (full pass, coverage dimension).

## Superseded planning notes below (history)

## Pending harvest mining, gated on DESIGN APPROVAL

The round-1 review harvest surfaced items that are decisions/patterns of
the still-unapproved design draft. Per the ledger anti-fabrication rule
(a decision proposed in an unapproved draft is not an event), their
occurrences are written ONLY when the design document is approved (by
reviewer + governing human). At that moment, mine:

1. "No second representation of the same truth" as recurring rejection
   principle (D53/D57/D58/D62) -> reinforced occurrence on the existing
   single-source-derivation entry (decision-ledger.yaml, the candidate-
   parent entry harvested from the plan-6 design review, ~line 3440).
2. New convention candidates from the approved design (each its own
   narrow Tier-1 entry, source agent-emergent, count 1, ref = design ADR):
   single-`v-html`-site license (D50), closed attribute-name set (D55),
   helpId==labelKey coupling (D51/D53).
3. Owner rulings on E1/E2/E3 and the CSS-ownership question:
   RESOLVED 2026-07-21 (S20), all four accounted:
   - E1 (CLI English-only, multilang GUI-only): STANDING boundary ->
     product-boundaries.yaml `cli-english-only` (human, promoted at 1).
   - F5/CSS (presentation-token carve-out + "schick machen is 1.x"):
     STANDING process rule -> process-conventions.yaml
     `latitude-carveout-presentation-tokens` (human, promoted at 1) +
     ROADMAP v1.x UI-polish entry.
   - E2 (three spec views) and E3 (suppress-all activation with
     allowlist): artifact-scoped design decisions -> recorded in the
     design ADRs + spec amendments by the fix round; NO house entry
     until they recur (statement-fit: one-off decisions, the ADR is
     their event log).

## Pending at OWNER DESIGN APPROVAL (round 2 additions)

- Design trigger 10: update product-boundaries `editor-generic-action-keys`
  budget 45 -> 46 (ordinal header key; tooltips land as attributes = zero
  ids). Held until the owner approves the design, because it books the
  design's future key state.
- Round-2 harvest mining (same anti-fabrication gate as round 1's):
  fire-verification-of-corrected-enumerations-by-execution as convention
  candidate (check statement-fit against proc-07-verify-against-source
  before opening a new entry); trigger-numbering stability and
  recorded-redundancy (D62 c) as weak micro-pattern candidates - judge
  at mining, drop with reason if they fail the scrutiny test.
- Round-2 status: APPROVED; the one new Minor (line-106 section
  reference) was author-fixed and controller-verified on disk; design
  triggers 2-8 mirrored to ROADMAP and trigger 9 executed 2026-07-21
  BEFORE owner review (fact-based, design-independent).

## Finding routing, round 1 (2026-07-21)

- Findings 1-4, 6-10: FIX, dispatched to the resumed design author.
- Finding 5 (explicit CSS-latitude clause in D52): awaiting owner ruling
  (the harvest's presentation-token question); the clause is NOT resolved
  implementer-side. Routed to the governing human together with E1-E3.
- Re-review: the resumed round-1 reviewer judges the delta.

## Verified at dispatch time (controller)

- Verdict file exists: .superpowers/sdd/plan-7/design-review-round-1.md.
- Harvest items (a) negative-with-control -> reinforced occurrence
  appended to proc-verification-step-must-be-falsifiable (count 5->6) and
  (d) IpcError render funnel -> Plan 9 ROADMAP bullet, both written
  2026-07-21 before this dispatch (verdict-arrival gate).

## Cross-task constraints (travel verbatim in the named dispatches)

- T19 (D62 gate): use the ZERO-PIPE table check (T9 review fire-tested
  the LEADING-PIPE pattern and stated zero-pipe dominance - it also
  catches headerless GFM tables; "pipe-pair" in earlier copies of this
  note was a controller paraphrase, not T9's tested pattern - round-8
  review M1); the
  raw-HTML check MUST exempt inline code spans - `(?<season>...)` in
  both pattern topics false-positives a naive pattern (two legitimate
  files would go red).
- T4-T7 (stream B): the filtered e2e form is `pnpm test:e2e --grep ...`
  (NO `--` before --grep; pnpm 11 forwards `--` literally and playwright
  drops post-`--` tokens - T3 review Q2, empirically proven). The plan's
  `-- --grep` form runs the full suite (green but slow).
- T5-T7 (stream B): register per catalog header governs de wording of
  ATTRIBUTES (declarative for gui-editor labels/tooltips); the
  du-imperative plan rule governs help TOPICS only (T3 review Q4).

## Whole-branch verdict routing (2026-07-22)

- Verdict NOT READY, blocking set = I1 only. Split ruling: the
  capture-phase dragstart suppression is SPEC-MANDATED (amended 8.3
  "control activation suppressed" - drag mutates, so suppressing aligns
  with the sentence as written) -> fix wave. The KEYBOARD/text residual
  is a product-wording question (scope the spec sentence vs broaden the
  suppression) -> OWNER, batched with the surface pass.
- Fix wave (ONE fixer, complete list per SDD): I1 dragstart; M1
  mocks/smoke narrative sweep (restart-path scoping + pointer to
  locale-switch.spec); M2 budget-45 at BOTH sites (PropertyMapWidget:45,
  ListWidget:15); M3 check-i18n.mjs:56 overgeneralization; M5 stale
  check-3 "current tree" paragraph; T18-nit FirstRun literal precision.
  M4 + M6 ride the plan-close design one-liner batch (resumed design
  author): M4 = D51's i18n.rs:41-46 -> :121-123 (+ the T3 D54
  exclusion-parenthetical one-liner already queued); M6 =
  focus-equals-hover recorded in design/spec.
- Harvest mined: 3 new Tier-1 entries (event-class enumeration,
  denial-comment sweep, e2e binding helpers positive), citation-drift
  PROMOTED to conventions.yaml (3rd instance). DROPPED with reason:
  D61 commented-out-code false-red = watch-only (verdict records it);
  Low observations stay in the verdict.
- OWNER-PASS package (batched): keyboard-residual wording ruling;
  h1-scheme (recommend T9's "Label (section)" form); markdown-subset
  content-criteria veto window; collected de coinages
  (owner-surface-pass-inputs.md); double-title 1.x budget note;
  track-rules editability ROADMAP anchor (standing).

## T21 verdict routing (2026-07-22)

- F1 (Medium) + Low nit: fix dispatch to the resumed T21 implementer,
  6(b) clause read from DESIGN section 6 directly (no re-transcription
  through a controller paste); re-review as resumed-reviewer delta.
- H3 dropped with reason: keep the verbatim-safe-alignment latitude in
  future amendment dispatches; do NOT invert the missed-echo finding
  into an any-out-of-enumeration-site-stops rule (the fix is H2's wider
  sweep coverage). Shapes controller dispatch wording; verdict carries it.
- H4 dropped with reason (agent-side class, already covered by Peter's
  zitat-und-zahl memory): the Q1b adjudication prompt cited superseded
  E1 Ruling 1 as binding; check the ledger for same-day supersessions
  before citing a ruling in a dispatch.
- H5 rides the F1 fix (continuation indent + sentence-start).

## Plan-close obligations (read at the close gate)

- Salvage step: the plan-7 design doc cites `.superpowers/sdd/plan-7`
  at three sites (~820, 1348, 1383; only 1348 carries line refs, which
  survive a plain move) - dispatch the re-pointing to the salvaged
  artifacts dir IN THE SAME CHANGE as the salvage (round-8 fix delta
  adjudication; ROADMAP trigger mirrors this).

## Pending design one-liners (batched tidy at plan close, via resumed
design author)

- D54 exclusion parenthetical for OutputCfg.directory: correct to
  source-dir fallback (planner.rs:285) per T3 review finding 1/Q3.

## T17 NEEDS_CONTEXT ruling (2026-07-22, S21): D55 rule 5 select-structure

The brief/design premise "this tree's catalogs have no nested selects"
is refuted (verified by the controller on the tree: validate-summary /
batch-diagnostics-summary sibling reopeners at column 0; nested
suggestion-partition in diagnostics.ftl). D55 rule 5d's absolute
"exactly one *-default" is permanently RED on the correct tree within
the line-based charter. Controller ruling: OPTION B - the select-
structure part of rule 5 becomes a pure en-vs-de PARITY check (de
compared against en's structure) instead of absolute assertions;
absolute Fluent validity stays with assertAllCatalogsParseCleanly
(e2e/i18n-en.ts, walks every locale x file in the app's two bundle
groupings - controller-verified). Purpose preserved: structural de
drift (the i18n-12/D39 motivation) still caught, incl. on
suggestion-partition; an allowlist would have exempted exactly that
message. Internal technical fork (no user-visible behavior, no
contested criterion once the premise fell); routed as design amendment
round 7 via four-eyes (fresh author + fresh delta reviewer, top model
per proc-03). Rules 1-4 + 5a stay as implemented (uncommitted in
plan7-g). T18/T19 remain blocked behind the amendment + T17 completion.

## T5 NEEDS_CONTEXT ruling (2026-07-22)

Option A: e2e/i18n-en.ts parseOrThrow discriminator corrected to its
documented intent (!m || (value null && zero attributes)) - the T4-era
guard conflated dropped entries with D55's sanctioned value-less shape.
Controller-authorized file addition to T5's staging (Files list +1);
no design/plan amendment (unenumerated implementation detail of a
pre-plan-7 guard); T5's reviewer gets the ruling as explicit addendum;
whole-branch review sees the final shape. Verification both ways
mandated (green reachable + red still fires + the T3 fire-case class
addressed empirically).
- T12/T13/T20: any STRING-TYPED SINK (placeable, function arg, prop)
  fed from an Option<T>-derived (T | null) IPC field needs
  null-narrowing first - only vue-tsc catches it (T6 H3, broadened by
  T7 review H3). Travels verbatim in those dispatches.

## Whole-branch review inputs (collected at verdict/report arrival)

- e2e/mocks.ts doc comment (~70-77) narrative stale post-D56 (no live
  swap claim); mechanism still works (T7 report item 1).
- smoke.spec.ts german-locale test #2 narrative partially obsolete
  post-D56 (still passes; live path covered by locale-switch.spec)
  (T7 report item 2).
- KeywordOrBlockWidget double-title known-benign fact (T4 verdict
  harvest; 1.x polish option: distinct block labelKey).
- h1-scheme cross-stream alignment recommendation: adopt T9's
  "<label> (<section>)" form (T9 verdict Q1; T10 verdict Q2 concurs
  routing to owner pass).
- Whole-branch input (T7 review H1): a stateful-view-across-switch
  assertion would witness D56's differentiator over remount (the marker
  guards reload only); H4: e2e/mount-entry.ts is a legitimate second
  createFluentVue (test harness) - exempt it from any single-instance
  grep.
- Whole-branch input (T11 verdict harvest): topics.ts comment cites
  i18n.rs:41-46 for the raw-id posture; actual fallback sits in render
  (~:123) - one-line comment fix candidate; source is the design's own
  citation. Framework note: cite-by-quoted-text should cover code
  comments too.
- Whole-branch input (T15 verdict minor 3): PropertyMapWidget's "catalog
  budget 45" comment predates the D59 45->46 revision (ordinal header
  key) - stale context line, outside T15's and T16's file sets; one-line
  comment sweep candidate.
- Whole-branch input (T19 verdict, over-restriction watch, both benign
  today): (a) the constrained id grammar silently skips malformed
  non-kebab id literals (CI blind spot, bounded by the runtime raw-id
  fallback); (b) RAW_HTML_RE strips inline code spans only - a fenced
  code block or 'a<b' prose could false-positive. Watch items, not
  fixes.
- Whole-branch input (T18 verdict nit): the retired residual comment's
  general prose mildly overgeneralizes ("never spelled out literally" -
  mkvmerge-not-found/too-old are literals in FirstRun.vue); zero
  behavioral consequence, noted for the sweep.

## Duration data polluted (2026-07-22, owner report)

Three background runs stalled on the EnterWorktree permission prompt
(auto mode cannot approve permission-root relocation to paths outside
.claude/worktrees/). T4 (~3.2h), T5 (~6.6h), T11 (~3.6h) durations are
prompt-wait, NOT compute - exclude them from any process-timing use
(EFFORT-KONSTANTE stats). Fix: preamble ban on session-relocation tools
(mechanism); ledger entry proc-no-session-relocation-in-subagents.
- T14+ e2e (harness fact, T13 verdict): programmatic .focus()/Tab does
  not drive focusin-delegated app state in this Playwright/headless
  setup - use synthetic dispatchEvent("focusin"); real mouseover works.
- T13 routed F2: D52 clear-enumeration misses hoverId on view switch
  (design asserts view-topic fallback; reproduced stale-card repro).
  Route: design amendment round 6 -> one-line fix dispatch (clear
  hoverId in watch(activeView), NOT broadened per the reviewer's
  over-restriction note) + F1 comment-wording nit in the same dispatch,
  BEFORE T14. Fix verified by asserting the instant post-switch state.
- Whole-branch minor (T12 fix round part 3): the focusin comment in
  e2e/help-mode.spec.ts was reworded to a plausible-but-false mechanism
  claim (dictated by the controller from the T13 verdict, refuted by the
  T12 reviewer's probe: real .focus() does NOT drive the delegated
  state). Recommended mechanism-NEUTRAL wording: "in this headless
  harness a real .focus() does not drive the focusin-delegated app
  state; direct dispatchEvent('focusin') exercises the same capture
  listener deterministically". Controller lesson recorded: a reviewer's
  mechanism claim is a borrowed claim - do not dictate it into a fix
  dispatch unverified; instruct the observable fact instead.
