# House-knowledge promotion candidates - TRIAGE (48)
Confirmed by the occurrence audit (>=3 verified, cited occurrences).
These become standing CONVENTIONS.yaml entries on your approval.

## core (24)
- **core-37-prose-free-core** (pattern, count 6) - muxsmith-core emits a diagnostic code + structured params only; all human text lives in the Fluent catalog / an injected DiagnosticRenderer port; core never ...
- **core-41-sourceoverwrite-batch-wide** (pattern, count 5) - Collect all input paths batch-wide (primaries + every resolved track/attachment/chapters donor) before rendering and fire SourceOverwrite if any rendered out...
- **core-06-schema-build-time-extraction** (pattern, count 4) - Matchable property names/types are generated into the capability model at build time by an xtask into a committed generated.rs; only derived facts ship, the ...
- **core-12-unknown-keys-are-errors** (pattern, count 4 [REDUCED from 5]) - An unknown profile key is a config-time hard error, not a warning; #[serde(deny_unknown_fields)] on every profile struct. Enforcement: the attribute was sile...
- **core-31-rendered-filename-invariant** (pattern, count 4) - The planner re-checks the rendered filename independently of the config-time template-text check (no separators, non-empty stem, not ./..); new per-file erro...
- **core-63-input-group-membership** (pattern, count 4) - input_groups = primary always (group 0) + donor sources with at least one track_id=Some assignment; exclude track-less donors. The first impl unconditionally...
- **core-65-empty-plan-warning** (pattern, count 4) - A plan resolving to zero track assignments emits EmptyPlan (warning, per-file, batch-visible); keep-mode plans are naturally exempt. A deliberate divergence ...
- **core-80-keep-mode-track-order** (pattern, count 4) - Under keep, --track-order lists all primary tracks first in source order (0:id), donors trailing (g:id in rule order). The initial assumption (only matched t...
- **core-03-suggestion-verified-edit** (pattern, count 3 [REDUCED from 4]) - Suggestions are structured edits simulated against the cached identification of the whole batch before being shown; only refinements that resolve every insta...
- **core-34-suggestion-cap-non-silent** (pattern, count 3 [REDUCED from 4]) - Deterministic preference order among accepted candidates; emit at most 3 per conflict group; if more were accepted, log the cap (SuggestionsCapped info) - bo...
- **core-35-overlap-auto-suggestions** (pattern, count 3) - OverlappingRules auto-narrowing suggestions: generate narrowing candidates for ALL overlap claimants symmetrically (including claimant 0); the existing resol...
- **core-38-absent-bool-equals-false** (pattern, count 3) - An absent boolean-typed matchable property compares equal to false for exact matching (mirrors mkvmerge/Matroska), so exact:{flag:false} matches a track lack...
- **core-40-output-collision-planned-twice** (pattern, count 3) - Two planned outputs colliding is always a hard OutputCollision error (drop both plans) regardless of on_collision; the policy (error/warn+drop/info+keep) gov...
- **core-44-suggestion-no-clobber** (pattern, count 3) - with_rule_match must not overwrite an existing exact/substring key (or_insert semantics); a clobbering candidate becomes a no-op then rejected by the accepta...
- **core-45-yaml-fragment-serializer** (pattern, count 3) - Render the suggestion yaml_fragment by serializing the real MatchExpr delta via yaml_serde, not by hand-formatting, so any value (colon/comma/brace, bool/int...
- **core-47-with-severity-builder** (pattern, count 3) - Add a rustdoc'd Diagnostic::with_severity builder and use it instead of mutating the public severity field directly (Diagnostic::info then .severity = ...).
- **core-48-extension-validation** (pattern, count 3) - Profile and locator extensions are validated against mkvmerge --list-types once per batch (not per file), degrading to a warning when mkvmerge is absent, mir...
- **core-54-reuse-plan2-machinery** (pattern, count 3) - Every new Plan-3 resolution site reuses a Plan-2 mechanism (locator machinery, template engine, LanguageIndex, existing DiagCode variants) rather than introd...
- **core-59-matchable-trait** (pattern, count 3) - The matcher is genericized over a Matchable trait implemented for both Track and Attachment so one match algebra evaluates both. The plan brief's 'resolves v...
- **core-72-exact-typed-value-equality** (pattern, count 3) - exact compares each property in its own domain (numbers numerically, languages canonicalized so de==ger, pt-Latn-BR==pt-BR), preserving meaningful distinctio...
- **core-81-unsupported-source-gate** (pattern, count 3) - A source mkvmerge identifies (exit 0) but cannot mux emits one clear UnsupportedSource error (distinct code from UnidentifiableSource, different remediation)...
- **core-85-report-json-dry** (pattern, count 3) - The batch/config/run JSON report documents (spec 7) are hoisted from the CLI into core::report::json, lifted 1:1, so CLI and GUI render byte-identical report...
- **core-89-homebrew-apple-silicon-path** (pattern, count 3) - /opt/homebrew/bin/mkvmerge (Apple-Silicon Homebrew) is a detection candidate: a Finder-launched Tauri app does not inherit the shell PATH, so without it dete...
- **core-99-schema-drift-advisory** (pattern, count 3) - A newer-schema advisory is its own SchemaDrift diagnostic, once per batch (info severity), message carrying the raw: discovery hint. T16 removed the mis-scop...

## process (9)
- **proc-01-sdd** (pattern, count 7) - Plans run via subagent-driven-development under a controller: a fresh implementer per task, an independent task reviewer, fix/re-review waves, and a final wh...
- **proc-02-whole-branch-review** (pattern, count 4) - A separate final whole-branch review grades the plan's code against its own constraints after every per-task review passes; it repeatedly caught cross-task d...
- **proc-04-spec-wins** (pattern, count 4) - The design spec is the binding contract; on any plan/code/spec conflict the spec wins and the conflict is flagged, not improvised around. Corollaries: normat...
- **proc-06-mkvtoolnix-parity** (pattern, count 4) - Because mkvtoolnix is interactive-GUI and Muxsmith is declarative-batch, only muxing semantics/output are parity targets, not input-time convenience guesses....
- **proc-08-parallel-worktrees** (pattern, count 4) - Genuinely independent task streams run as parallel git worktrees with a full gate per merge, not strictly serial. Origin was a failure (strictly-serial execu...
- **proc-03-model-assignment** (pattern, count 3) - Model strength goes where judgment is, cheaper capacity where transcription is: strongest model for the final review and the decision memos, mid-tier for jud...
- **proc-05-commit-signing** (pattern, count 3) - Agent commits and merges are deliberately left unsigned via -c commit.gpgsign=false. Origin was mechanical (GPG blocks agent commits); elevated to policy (SI...
- **proc-07-verify-against-source** (pattern, count 3) - Load-bearing tooling and dependency behavior is confirmed against the crate's actual source or the registry, never from a brief's claim, a docs sentence, or ...
- **proc-09-idiomacy-review** (pattern, count 3) - A whole-codebase idiomacy review is a distinct pre-1.0 gate scoped to complexity only (correctness/security/perf routed to a separate list). The rubric is si...

## ci (4)
- **ci-08-gated-tests-run** (pattern, count 6) - The mkvmerge-gated integration tier must run in CI, not self-skip. E2 accepted the self-skip (mkvtoolnix not installed) as a coverage gap; E5 installed mkvto...
- **ci-01-matrix-strategy** (pattern, count 3) - While the repo is private, branch pushes run Linux only and the full windows/macos/linux matrix runs only on PRs, tags and manual dispatch via a dynamic from...
- **ci-06-per-commit-gate** (pattern, count 3) - Standing house rule: cargo test --workspace, cargo fmt --all --check, cargo clippy --workspace --all-targets -D warnings and cargo deny check must all pass b...
- **ci-10-pin-everything** (pattern, count 3) - Every toolchain and CI input is pinned exactly - rust 1.96.1 over floating stable, all GitHub Actions SHA-pinned, runner images pinned, ctrlc full-pinned, np...

## testing (4)
- **testing-si3-run-binary** (pattern, count 5) - mkvmerge behavior (flag spellings, argv semantics, -J shape, cross-file donor+keep ordering) is confirmed by running the installed real binary (v100), never ...
- **testing-support-helpers** (pattern, count 4) - Cross-file test helpers (FakeIdent, lang()) are consolidated into a shared tests/support/mod.rs subdirectory module - deliberately a submodule, not its own t...
- **testing-command-golden** (pattern, count 3) - Command generation (Plan -> mkvmerge argv) is verified by golden tests mapping fixture identification JSON to expected argv; built incrementally with full-ve...
- **testing-rich-gated-live-guard** (pattern, count 3) - The Plan-3 one-off manual v100 validation is converted into a standing gated live guard: the test builds a primary MKV with a real attachment (--attach-file ...

## executor (2)
- **exec-19-delete-partial-errsurface** (pattern, count 3) - Surfacing a failed partial-output delete via outcome.errors was flagged and deferred twice at Plan-4 review (no JobOutcome error channel existed, and a faile...
- **exec-44-runlog-14day-autoprune** (pattern, count 3) - Run-log pruning was left out of v1 at D26 (location documented, prune facility parked), then reversed at D35 to an automatic 14-day fixed prune with no v1 co...

## i18n (2)
- **i18n-02-prose-free-core** (pattern, count 4 [REDUCED from 5]) - muxsmith-core emits diagnostic codes + structured params only, never user-facing prose; all labels, messages and hints live in shared Fluent catalogs (and pe...
- **i18n-05-plural-selectors** (pattern, count 3) - Count-dependent user-facing messages pluralize via Fluent CLDR plural selectors ({ $n -> [one] ... *[other] ... }), not an error(s)/(s) provisional. First ap...

## cli (2)
- **cli-02-dryrun-config-diags** (pattern, count 4) - Dry-run must run profile::validate + lint::provable_overlaps and fold config-time diagnostics in on every code path (happy, mkvmerge-not-found, mkvmerge-quer...
- **cli-09-collision-parity** (pattern, count 3) - --on-collision <error|skip|overwrite> is exposed on both run and dry-run for parity, via a CLI-local CollisionArg (core stays clap-free) mapping 1:1 to Colli...

## cross (1)
- **cross-01-stack** (pattern, count 3 [REDUCED from 4]) - Build on Tauri 2 with a Rust core crate, a web frontend and a clap CLI; Rust accepted despite being only recently picked up. React was the initial frontend, ...
