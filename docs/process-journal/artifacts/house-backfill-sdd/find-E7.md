# Muxsmith era E7 (Plan 5.5, 2026-07-11..12) — house-knowledge backfill

Decision history reconstructed from: plan (`2026-07-11-plan-5.5-pre-1.0-hardening.md`),
memo (`2026-07-11-plan-5.5-design-decisions.md`), D32/D33 analyses, 30 task
verdict/report files, the whole-branch verdict + fix report, the progress ledger,
and the Plan-5.5 journal entry. One record per occurrence; grounded in a cited
artifact. Domains: core, cli, gui, executor, i18n, process, ci, testing, cross.

---

## D32 — forward-compat for unknown properties (`raw:` opt-in)

1. **[pattern/decided][core]** UnknownPropertySkew mitigation — `raw:` opt-in (shape B): unknown property names stay hard-rejected at config time UNLESS marked `raw:`; a `raw:` property matches untyped, emits `RawProperty` info at validate and `UnknownPropertySkew` at plan time. `occ_ref: memo D32 (Şenol 2026-07-11)`. Evidence: "unknown property names stay hard-rejected at config time UNLESS marked with a `raw:` prefix."
2. **[pattern/reinforced][core]** shape B shipped exactly per the binding B-1..B-11 table. `occ_ref: task-16-verdict.md`. Evidence: "B-1..B-7, B-9..B-11 exactly per the binding table (B-1 hard-reject byte-unchanged; raw: strip before type/domain checks)."
3. **[restraint/decided][core]** Shape A (did-you-mean + formally drop §9.2 promise) rejected. `occ_ref: memo D32 / d32-analysis.md`. Steelman: "Honors typo protection: maximally... the correct cleaner downgrade if forward-compat-without-a-release is YAGNI." Rejected because "the §9.2 promise is kept deliberately."
4. **[restraint/decided][core]** Shape C (warning-only downgrade) rejected outright. `occ_ref: memo D32`. Steelman: "Forward-compat delivery: full and automatic... the cheapest path to a literally-§9.2-compliant tool." Rejected: "guts typo protection unconditionally... opens the optional-rule and not:-inversion silent holes."
5. **[restraint/decided][core]** Version-gated hybrid (error at validate, warning at dry-run) rejected pre-emptively — violates spec 5.5 "dry-run is a strict superset of validate." `occ_ref: d32-analysis.md`. Steelman: "the tempting 'gate on the runtime version' hybrid." "the single most decisive filter in the analysis."
6. **[pattern/decided][core]** `RawProperty` (info) KEPT rather than the minimal variant that drops it — the escape valve must be visible at validate. `occ_ref: memo D32 marked assumption`. Evidence: "a silent bypass undercuts the point of B over C."
7. **[restraint/decided][core]** Minimal variant (omit `RawProperty`, rely only on plan-time `UnknownPropertySkew`) rejected. `occ_ref: d32-analysis.md`. Steelman: "two fewer codes and two fewer C1 fixtures," trades validate-time honesty.
8. **[pattern/decided][core]** `RawOnKnownProperty` warning when `raw:` is applied to a known-special property (`language`, `codec_kind`) — flags bypass of normalization/alias expansion. `occ_ref: memo D32`. Evidence: B-4 test case.
9. **[non-decision/deferred][core]** B-8 raw:language field-lookup: implemented SINGLE-FIELD verbatim; memo row read most naturally as dual-field; ratification PENDING Şenol. `occ_ref: task-16-verdict.md / memo D32 addendum`. `blocked_on: Şenol ratification (external)`. Evidence: "raw:language reads only the property literally named `language` (de vs ger = no match)."
10. **[pattern/decided][core]** B-8 single-field RATIFIED by Şenol at the T21 gate — "raw:X reads exactly the property literally named X, byte-exact; the one-sentence no-magic rule holds without a language exception. No code change." `occ_ref: memo D32 addendum RESOLVED (Şenol 2026-07-12)`.
11. **[non-decision/deferred][core]** Schema-drift advisory removal: T16 dropped the live per-file skew warner (the design round's "dead code" premise was FALSE); should a once-per-batch advisory survive? open-question-for-Şenol. `occ_ref: task-16-verdict.md`. `blocked_on: Şenol (external)`. Evidence: "the old signal was mis-scoped per-file noise and announced untyped matching that never happened."
12. **[pattern/decided][core]** Schema-drift notice REBUILT as its own `SchemaDrift` diagnostic, once per batch, info severity, message carries the `raw:` discovery hint — Şenol ruling ("Das ist ein wichtiger Hinweis"), vehicle plan Task 16.5. `occ_ref: memo D32 addendum RESOLVED / plan T16.5`.
13. **[pattern/reinforced][core]** SchemaDrift shipped: own DiagCode info, params found_version(max)+pinned, single site outside the loop (once per batch). `occ_ref: task-16.5-verdict.md`. Evidence: "own DiagCode SchemaDrift info; ... once per batch (single site outside the loop, Option-guarded)."
14. **[restraint/decided][core]** SchemaDrift emitted from primaries only, NOT extended to donors — "over-engineering for an unreachable case." `occ_ref: task-16.5-verdict.md`. Steelman: a donor on a newer schema than the primaries could carry undocumented skew — but structurally impossible ("schema version is a build constant of the one local mkvmerge binary").
15. **[restraint/decided][core]** README content anchor: properties carrying language-like matching magic (normalization, dual-field lookup, absent-boolean-compares-false, curated domains) MUST be explicitly listed in the README. `occ_ref: memo D32 addendum follow-on directive (Şenol)`. Note: standing documentation convention born from the B-8 no-magic ruling. (domain: process would also fit; kept core as it governs core semantics doc.)

## D33 — OverlappingRules auto-narrowing suggestions

16. **[pattern/decided][core]** Overlap suggestions — symmetric, acceptance-filtered (policy 3): generate narrowing candidates for ALL claimants; existing `resolves_without_regression` selects the feasible ones. `occ_ref: memo D33 (Şenol 2026-07-11)`. Evidence: "feasibility... not a precedence guess, determines which rule can be narrowed."
17. **[pattern/reinforced][core]** Policy 3 shipped at T18 — genuinely symmetric incl. claimant 0, no hard-coded two-required early-out. `occ_ref: task-18-verdict.md`. Evidence: "TC-A/B/C/D all pass through the correct mechanisms (acceptance filter + isolation check)."
18. **[restraint/decided][core]** Policy 1 (later-by-list-order narrows rules[1]) rejected as selection policy. `occ_ref: d33-analysis.md / memo D33`. Steelman: "cheapest and fully deterministic; matches a 'first rule wins, the later one is the intruder' mental model." Rejected: Muxsmith's model has no rule precedence; often the earlier rule is the over-broad one. (Kept only as rank tiebreak.)
19. **[restraint/decided][core]** Policy 2 (broader-by-match-domain narrows the more permissive rule) rejected as selection policy. `occ_ref: d33-analysis.md / memo D33`. Steelman: "the strongest intent heuristic — the rule matching more tracks across the batch is the accidental over-claimer." Rejected: "broader" ambiguous to measure; still a guess that can pick the required rule. (Kept only as rank tiebreak.)
20. **[pattern/decided][core]** Rank tiebreak between surviving narrowings on different rules: broader-rule-first, then lower rule index. `occ_ref: memo D33 marked assumption`.
21. **[pattern/decided][core]** Seed overlap candidates NOT-polarity only, from the shared track's property vector (drop positive AddExact/AddSubstring seeding on the overlap path). `occ_ref: memo D33 / d33-analysis.md`. Evidence: "the NOT form is the honest expression of 'this rule should not take that track'... a genuine departure from the ambiguous path, justified by the monotone-narrowing fact."
22. **[restraint/decided][core]** Mirroring the ambiguous path's both-polarity generation on the overlap path rejected. `occ_ref: d33-analysis.md open taste call 3`. Steelman: "mirror the ambiguous path's both-polarity generation for uniformity." Rejected: positive can't redirect, "only adds near-tie noise to the ranked list."
23. **[restraint/decided][core]** An "overlap-specific dimension" mechanism (diff the two rules' matched track sets) rejected. `occ_ref: d33-analysis.md`. Steelman: "the properties on which the two rules' matched track sets differ." Rejected: empty on the single contested file; strict subset of NOT-from-shared-track's-props.
24. **[pattern/decided][core]** Reuse `candidates_for_rule` / `resolves_without_regression` / `partition_for_rule`; no new edit variant, no new selection mechanism, no new DiagCode. `occ_ref: memo D33`. Reinforced at T18: "no new edit variant/DiagCode/Fluent message (tripwire never fired)" (`task-18-verdict.md`).
25. **[pattern/decided][core]** Narrowing an optional rule to empty is accepted as a first-class suggestion (vs down-ranking / annotating "this disables the rule here"). `occ_ref: memo D33 marked assumptions`.
26. **[restraint/decided][core]** Two required rules colliding on one track in one file yields NO suggestion — only the explicit no-fix/partition report. `occ_ref: memo D33 "Tradeoff (accepted)"`. Steelman: "a user expecting a one-click fix for every overlap will find overlap less helpful than ambiguity." Forced by the narrow-only v1 grammar (cannot redirect).
27. **[pattern/decided][core]** >=3 claimants degrade to the partition/no-fix report, which must name ALL claimants (ties to ROADMAP polish (i)). `occ_ref: memo D33 technically-forced list / d33-analysis.md`.
28. **[pattern/decided][core]** Within-rank tiebreak on overlap suggestions is alphabetical on the property name — a deliberate tiebreak recorded at T18 review. `occ_ref: memo D33 TC-A / task-18-verdict.md adjudication 1`.
29. **[restraint/decided][core]** A "contested-property selection dimension" (to honor the memo's default_track-vs-forced_track illustration) rejected — it would be a new selection mechanism D33 forbids. `occ_ref: task-18-verdict.md adjudication 1`. Steelman: "honor the illustration" (id2 carries both flags at equal rank).

## T13 suggestion-engine mechanical completion (D6)

30. **[pattern/decided][core]** D6 completion: external-source suggestions (remove the `SourceCfg::External` skip; algorithm is source-agnostic), codec/id narrowing dimensions, multiset `diag_signature` (BTreeSet -> BTreeMap<sig,count>), §5.3 partition report (`SuggestionPartition` info, cap 5 groups + overflow note). `occ_ref: task-13-verdict.md / plan T13`.
31. **[pattern/decided][core]** Partition grouping keyed on the per-file suggestion (suggestion-keyed grouping), superseding D6's "conflict-signature multiset" key; ratified via spec §5.3:282 (mandates the outcome, not a key). `occ_ref: task-13-verdict.md ratification 1 / memo residues`.
32. **[restraint/decided][core]** D6's property-vector-multiset partition key rejected. `occ_ref: task-13-verdict.md / memo residues`. Steelman: "D6's Algorithm-doc wording (§5.3:286)." Rejected: "can over-report by splitting files sharing a resolution."
33. **[non-decision/deferred][core]** Fixless affected file silently dropped from the partition (best=None skip) — harden with an invariant comment or an "unresolvable" group. `occ_ref: task-13-verdict.md m1 / whole-branch funnel T13-m1`. `blocked_on: idiomacy review (internal)`. Unreachable under v1 id-uniqueness.

## Executor (T3, T4, T4.5)

34. **[pattern/decided][executor]** Robust event-stream reads: `read_until(b'\n')` + `from_utf8_lossy`, only `Ok(0)` ends the stream — survive non-UTF-8 lines, no truncation, no pipe-full hang. `occ_ref: task-3-verdict.md / plan T3`.
35. **[pattern/violated-corrected][testing]** A standalone unix-only integration test must gate at FILE level (`#![cfg(unix)]`), not fn-level — round-1 module-level imports under a fn-level `cfg(unix)` broke Windows clippy `-D warnings`; fix 3353028. `occ_ref: task-3-verdict.md`. Evidence: "the cited precedent lives in an inline #[cfg(test)] mod where use super::* keeps imports live; a standalone unix-only integration crate must gate at FILE level."
36. **[pattern/decided][executor]** Worker-panic handling + mutex-poison hygiene: capture `join()` Err as Failed + `worker-panicked` (not Cancelled); poison-recovery `unwrap_or_else(|p| p.into_inner())` centralized behind getters with soundness docs; panic payload flows only to eprintln, never to user-facing params. `occ_ref: task-4-verdict.md / plan T4`.
37. **[restraint/decided][executor]** Test-only lock sites deliberately left bare (no poison-recovery) — a poisoned test lock should fail loudly. `occ_ref: task-4-verdict.md concern 2`. Steelman: uniform poison-recovery idiom at every site.
38. **[non-decision/deferred][executor]** `eprintln!` in core (queue.rs) — first direct stderr I/O in core; idiomatic fix is a log/tracing facade the binaries route. `occ_ref: task-4-verdict.md I1 / whole-branch-verdict.md I3`. `blocked_on: Plan 6 (internal)`.
39. **[non-decision/deferred][executor]** `worker-panicked` rich message renders on no live surface (only JSON carries the token); fix = route JobOutcome.errors codes through the catalog. `occ_ref: task-4-verdict.md I2 / whole-branch-verdict.md I3`. `blocked_on: Plan 6 (internal)`.
40. **[pattern/violated-corrected][executor]** Child-process leak on post-spawn panic: `killers().remove(&index)` discarded the Killer without invoking it, so a leaked mkvmerge kept writing an output reported Failed; fixed FIX-NOW in the whole-branch wave (invoke-then-remove). `occ_ref: task-4-verdict.md m2 / whole-branch-fix-report.md T4-m2`.
41. **[pattern/decided][executor]** D35 run-log auto-prune: 14 days fixed, no v1 config, age decided by the PARSED NAME only (never mtime), best-effort (every prune io error ignored, symlink-safe). `occ_ref: plan T4.5 (D35) / task-4.5-verdict.md`.
42. **[non-decision/deferred][executor]** Run-log prune configurability parked as IDEAS #7. `occ_ref: plan T4.5`. `blocked_on: future product decision (external)`.
43. **[pattern/decided][process]** D35 vehicle (Şenol at the go): the log-pruning DECISION stayed outside Plan 5.5; only its implementation rides as wave-1 task 4.5 — one SDD apparatus over a standalone run. `occ_ref: plan T4.5 / journal 2026-07-12 Plan 5.5 entry`.
44. **[pattern/decided][executor]** Reuse-before-writing: `run_id_timestamp` parser moved to core; the shell's `started_at_from_run_id` becomes a 4-line delegate, three existing shell tests byte-identical. `occ_ref: plan T4.5 / task-4.5-verdict.md`.

## Planner / validate (T5, T5.9, T6, T7 class)

45. **[pattern/decided][core]** `--list-types` extension validation: once per batch (not per file), degrade-with-warning when mkvmerge absent — same pattern as language validation; new `UnknownExtension` warning. `occ_ref: task-5-verdict.md / plan T5`.
46. **[pattern/decided][core]** `known_extensions` delivered as an `Identify` trait default method + IdentifyCache memo instead of a `plan_core` param — zero production edits (a param would break ~20 call sites and collide with parallel streams). `occ_ref: task-5-verdict.md`.
47. **[non-decision/deferred][core]** `known_extensions` default-None on `Identify` inverts the idiom (sole production impl overrides; a future production impl would silently get vacuous validation) — fix: make the method required, fakes return None explicitly. `occ_ref: task-5-verdict.md m2 / progress.md T5-m2`. `blocked_on: idiomacy review (internal)`.
48. **[pattern/decided][core]** Locator extension validation (spec §4.6, Task 5.9): recursive walk over track-rule / chapters / attachment `add` entries reusing T5's `validate_extension_values`; the two Locator field sites drive exactly three walked positions. `occ_ref: task-5.9-verdict.md / plan T5.9`. Routed in from the T5 review.
49. **[pattern/decided][core]** Zero-track plan warning `EmptyPlan` — warning only (decided), per-file, visible in the batch report/summary; fires when a rendered plan has zero track assignments; keep-mode plans naturally exempt (D20). `occ_ref: task-6-verdict.md / plan T6`.
50. **[pattern/decided][core]** `EmptyPlan` unconditional per-file warning is a DELIBERATE DIVERGENCE from mkvtoolnix-gui (no general zero-selected-tracks warning; mkvmerge exits 0 on empty output) — an interactive GUI shows selection state by construction; a declarative batch tool must say it. `occ_ref: memo residues (EmptyPlan) / task-6-verdict.md SI-3`.
51. **[pattern/violated-corrected][core]** `EmptyPlan` check relocated post-`finalize_plans` gated on `plan.is_some()` — round-1 placement was pre-finalize (structurally unreachable for cross-file passes) and dropped the mandated batch-report test; the relocation also closed an unenumerated `CollisionPolicy::Skip` false-warning case. `occ_ref: task-6-verdict.md fix a60e9a0`.
52. **[pattern/decided][core]** SourceOverwrite completeness (T7): gather protected source paths from ALL files' resolved sources (primaries + donors) independent of plan-render success — protects donors of render-failed files. `occ_ref: task-7-verdict.md / plan T7`.
53. **[pattern/reinforced][core]** SourceOverwrite extended to attachment donors (T7.5) — same data-loss class, wiring only at the same pre-render capture point. `occ_ref: task-7.5-7.6-verdict.md / plan T7.5`. Routed in from the T7 review.
54. **[pattern/reinforced][core]** SourceOverwrite extended to chapters donors (T7.6) — closes the #7 class BY CONSTRUCTION: exactly two Locator field sites, exhaustive match, completeness comment enumerating the three donor kinds. `occ_ref: task-7.5-7.6-verdict.md / plan T7.6`. Found by the T7.5 implementer.

## CLI / diagnostics (T8, T9, T9.5, T10, T11, T12)

55. **[pattern/decided][cli]** Empty-batch summary always prints: `print_batch_human` prints the batch summary line even at zero matches; JSON path unchanged; exit 0. `occ_ref: task-8-verdict.md / plan T8`.
56. **[non-decision/deferred][i18n]** `dry-run-summary` key serves both run and dry-run — rename to `batch-summary`. `occ_ref: task-8-verdict.md m2 / whole-branch funnel T8-m2`. `blocked_on: idiomacy review (internal)`.
57. **[pattern/decided][core]** `OverlappingRules` names ALL claimants via a rendered `$rules` list (T9 item i) — deterministic (BTreeMap + ascending rule index); feeds D33's all-claimants requirement. `occ_ref: task-9-verdict.md (i) / plan T9`.
58. **[pattern/violated-corrected][cli]** query-failed human path (T9 vii): pre-check determined the missing config-diag print was NOT deliberate (F1 commits were --json-scoped; spec 5.5 unconditional) — fixed inside the human else-branch, JSON untouched. `occ_ref: task-9-verdict.md (vii)`.
59. **[restraint/decided][cli]** `IdentifyError` English `detail` KEPT (not routed through a catalog key) and added explicitly to the spec §8.4 exception list (T9 viii). `occ_ref: task-9-verdict.md (viii)`. Steelman: third-party text pass-through like spec 8.4's clap exception. Noted the exception is slightly broader than pure pass-through.
60. **[pattern/violated-corrected][core]** `InvalidPropertyValue` at planner.rs:600 set only property+value while the template needs `$allowed`, rendering a literal `{$allowed}` to users on the plan-time `changes.language` path; found by the T10 review's fixture-fidelity spot-check, routed to T9 item (ix), fixed with a site-specific no-`{$` regression test. `occ_ref: task-10-verdict.md / task-9-verdict.md (ix) / plan T9(ix)`. The per-DiagCode guard is structurally blind to single-site divergence when sibling sites set the full param set.
61. **[non-decision/deferred][cli]** Flat `config_diags` JSON is unsorted, inconsistent with validate's sorted JSON. `occ_ref: task-9-verdict.md (iv) / whole-branch funnel`. `blocked_on: Plan 6 / idiomacy (internal)`. Consumers key on codes; ordering cosmetic.
62. **[pattern/decided][core]** Donor named in donor-side `UnsupportedSource` (T9.5): add a `donor` param, single key + `$kind` selector with a donor branch (mirrors `DonorIsPrimary`'s `$donor`); must land before T21 translates. `occ_ref: task-9.5-verdict.md / plan T9.5`. Routed in from the T9 review.
63. **[pattern/decided][testing]** Catalog param-drift guard (T10): exhaustive no-wildcard match building a fixture table over ALL DiagCodes + full cli.ftl key coverage via a real Fluent parser; assert rendered output has no `{$` substring; allowlist for directly-rendered keys. `occ_ref: task-10-verdict.md / plan T10`.
64. **[pattern/decided][testing]** Test-hardening group T (T11): fixture attachment ids made 1-based, empirically grounded in real mkvmerge v100 -J (attachment ids 1-based, track ids 0-based), with id-agnostic assertions. `occ_ref: task-11-verdict.md / plan T11`.
65. **[non-decision/deferred][testing]** `_comment` fixture-note convention introduced ad hoc for one file — promote to a written convention on second use. `occ_ref: task-11-verdict.md m1 / whole-branch funnel T11-m1`. `blocked_on: second use (trigger-gated)`.
66. **[pattern/decided][ci]** rustdoc as ninth gate part (T12): `RUSTDOCFLAGS="-D warnings" cargo doc --no-deps` on all CI legs; dead intra-doc links delinked when the target is private, real-fixed when public. `occ_ref: task-12-verdict.md / plan T12`.
67. **[restraint/decided][process]** Delink chosen over publicizing `on_close_requested` to satisfy an intra-doc link. `occ_ref: task-12-verdict.md judgment`. Steelman: "the narrow alternative (pub use re-export) named for completeness." Rejected: "src-tauri is an application crate... docs describe architecture, not dictate it."

## Testing infrastructure (T14, T22)

68. **[pattern/decided][testing]** proptest for the correctness core (spec §10, T14): match algebra, language idempotence/symmetry, planner determinism + D6 suggestion-survival; deterministic seeds; proptest exact-pinned; reviewer traced ~100% suggestion-path reach to prove non-vacuity. `occ_ref: task-14-verdict.md / plan T14`.
69. **[non-decision/deferred][testing]** D6 property guards use `prop_assume` where `prop_assert` would localize a future generator regression (currently dies opaquely as "too many global rejects"). `occ_ref: task-14-verdict.md m1 / whole-branch funnel T14-m1`. `blocked_on: idiomacy review (internal)`.
70. **[pattern/decided][testing]** insta snapshots replace wording-coupled asserts (spec §10, T22): redaction filters for absolute paths / mkvmerge version / durations (load-bearing), EN locale pinned, CI strict compare (no INSTA_UPDATE); review-before-accept. `occ_ref: task-22-verdict.md / plan T22`.
71. **[pattern/decided][testing]** Two regex-crate-wording snapshots deliberately break on `cargo update` — an intended signal, not a fragility bug. `occ_ref: task-22-verdict.md concern 1 / whole-branch funnel T22-m1`.

## i18n (T19, T20, T21, T21.5)

72. **[pattern/decided][i18n]** Fluent plural selectors (`{ $errors -> [one] ... *[other] ... }`) replace the `error(s)` provisional across five keys (T19). `occ_ref: task-19-verdict.md / plan T19`.
73. **[restraint/decided][i18n]** Render-boundary plural promotion chosen over a typed-params wire change. `occ_ref: task-19-verdict.md (c)`. Steelman: a typed-params wire change would be principled — rejected because it means "dozens of call sites + JSON schema + IPC type for two codes' benefit."
74. **[pattern/decided][i18n]** The mirrored Rust/TS numeric-promotion lists are pinned by a Rust-side test naming the TS mirror (fix 0d19dc4); TS tightened to `Number.isInteger && >=0`. `occ_ref: task-19-verdict.md (b)`. A real unenforced cross-language contract check:i18n does not cover.
75. **[pattern/decided][i18n]** `check:i18n` enforces cross-locale key parity, extended to include cli.ftl (RATIFIED) — cli.ftl has no UI chrome to mask a silent English fallback (T20). `occ_ref: task-20-verdict.md / plan T20`.
76. **[pattern/violated-corrected][i18n]** T20's regex-vs-real-parser divergence on malformed ftl (a botched multiline indentation could pass parity while the runtime parser rejects) — resolved at T21 by an e2e real-Fluent-parse guard covering every catalog of every locale (c229400). `occ_ref: task-20-verdict.md m1 / task-21-verdict.md / whole-branch funnel T20-m1`.
77. **[non-decision/deferred][testing]** No `check-i18n.mjs` fixture self-test despite grown logic. `occ_ref: task-20-verdict.md m2 / whole-branch funnel T20-m2`. `blocked_on: v1.x test-hardening (internal)`.
78. **[pattern/decided][i18n]** German locale: translate all six catalogs; anchor terminology to mkvtoolnix's de.po FACTS (single domain terms only, no text copying; zero sentence overlap verified); hard Şenol terminology gate before merge (T21). `occ_ref: task-21-verdict.md / plan T21`.
79. **[pattern/decided][i18n]** Şenol terminology gate PASSED with three corrections (Starten, Meldungen, Verweis) applied 362db2d. `occ_ref: journal 2026-07-12 Plan 5.5 entry / progress.md`.
80. **[pattern/violated-corrected][testing]** The reviewer's own suggested guard mechanism (rely on Fluent `addResource` to report Junk) was disproven by the fixer from library source (`addResource` errors only on id collisions; Junk silently dropped); the stronger column-0 presence-cross-check landed and the reviewer conceded. `occ_ref: task-21-verdict.md fix wave 2 / journal`.
81. **[non-decision/deferred][i18n]** Placeable-name / selector-structure drift in de is not machine-checked (check:i18n enforces id parity only). `occ_ref: task-21-verdict.md residual / whole-branch-verdict.md M1`. `blocked_on: future check:i18n extension (internal)`. No actual drift found today.
82. **[pattern/decided][gui]** German reachable from the UI (T21.5, Şenol: fix immediately, not Plan 6): add the German option to the settings dropdown; correct the settings-locale-hint; failing-e2e-first. `occ_ref: task-21.5-verdict.md / plan T21.5`. Routed in at the T21 gate.
83. **[pattern/decided][i18n]** Locale picker labels are endonyms (English/Deutsch identical in both catalogs) — universal picker convention. `occ_ref: task-21.5-verdict.md`. T21's language-name "precedent" was an accidental byproduct.
84. **[pattern/decided][i18n]** Settings hint made evergreen (no language enumeration) — it was re-staling at locale #3; tightened to two plain sentences. `occ_ref: task-21.5-verdict.md`.
85. **[non-decision/deferred][gui]** No live in-session locale switch (bootstrap-once architecture; restart notice added). `occ_ref: task-21.5-verdict.md / whole-branch funnel T21.5-m1`. `blocked_on: Plan 6 (internal)`.
86. **[non-decision/deferred][gui]** Şenol veto on the T21.5 settings-hint texts left OPEN at close. `occ_ref: journal 2026-07-12 Plan 5.5 entry (Open threads)`. `blocked_on: Şenol (external)`.
87. **[pattern/decided][gui]** Şenol veto on the hint texts DECLINED (texts approved unchanged) in the session-9 close addendum. `occ_ref: journal 2026-07-12 session 9 close addendum`.

## Process / doctrine / CI

88. **[pattern/decided][process]** SDD execution model: fresh implementer + independent reviewer per task, controller re-runs gates itself, reviewer verdicts are FILES in the SDD scratch dir at creation. `occ_ref: plan Global Constraints / progress.md`.
89. **[pattern/decided][process]** Wave-1 parallelism via six git worktrees, merged sequentially with a full gate re-run per merge; the DiagCode/key-adding stream (D) merges LAST so its exhaustive guard forces the other streams' fixtures (C1/C4). `occ_ref: progress.md / plan dependency graph`.
90. **[pattern/violated-corrected][process]** T9 resequenced OUT of stream C and run serially after stream B — the plan's disjointness note was wrong for T9 (its file list overlaps stream B's planner/validate region). `occ_ref: progress.md RESEQUENCED / journal (Decisions)`.
91. **[pattern/decided][process]** Reasoned deviation: T22 ran PARALLEL to T21 (EN-pinned snapshots can't be affected by de/ files) so the human terminology gate on T21 would not block the tail; message-stability ordering (T19 before T22) preserved. `occ_ref: progress.md / journal (Decisions)`.
92. **[pattern/decided][process]** Anchor-precise plan style (exact files/line anchors, named tests) with design-gated packages running TDD themselves; Şenol-approved; an underspecified task is a DONE-blocking question, not a license to guess. `occ_ref: plan Style note (line 13)`.
93. **[pattern/decided][process]** mkvtoolnix parity method (SI-3): verify comparable behavior against the real binary/source and record match/divergence in the memo — applied at D32 (unknown-property permissiveness = justified divergence) and T6 (EmptyPlan divergence). `occ_ref: plan Global Constraints / d32-analysis.md SI-3 / task-6-verdict.md`.
94. **[pattern/decided][ci]** mkvmerge on all three CI legs (apt/choco/brew) with a skip-marker assertion requiring count ZERO on every leg — "otherwise silent skipping is traded for silent skipping." `occ_ref: task-2-verdict.md / plan T2`.
95. **[pattern/reinforced][ci]** The skip-marker assertion caught its own target class on its maiden run: 18 silently skipped Windows tests (choco writes machine PATH, running job never re-reads it) — fixed via GITHUB_PATH append; then a real Windows-only bug (set_modified on a read-only handle) on the first-ever live run. `occ_ref: progress.md T2 fix waves / journal (What the process caught)`.
96. **[pattern/decided][ci]** mkvmerge CI versions pinned per-manager (Şenol's standing preference is pin, not float); cross-leg divergence (apt 97 vs choco/brew 100) sanctioned and recorded in a ci.yml comment. `occ_ref: plan T2 step 2 / progress.md T2-m3/m4 / whole-branch funnel`.
97. **[non-decision/deferred][ci]** Skip-marker string is an unenforced cross-file contract (19 call sites + 1 CI grep, no shared const) — a future reword silently reintroduces the false-negative; harden with a shared const. `occ_ref: task-2-verdict.md m1 / whole-branch funnel T2-m1`. `blocked_on: idiomacy review (internal)`.
98. **[pattern/decided][process]** .gitattributes line-ending normalization (`* text=auto eol=lf` + per-extension `-text` for binary assets) — LF-stable text for T22's byte-exact snapshots (T1). `occ_ref: task-1-verdict.md / plan T1`.
99. **[pattern/decided][i18n]** Bilingual cutoff (C2): new Fluent messages EN-only in Waves 1-2, bilingual (en+de) from T19 on. `occ_ref: progress.md C2 / plan Global Constraints`.
100. **[pattern/decided][process]** Spec is authoritative over the plan on conflict; after amending §9.2/§5.3 a spec self-contradiction sweep runs (doctrine §1); the memo carries concrete acceptance test cases as part of the design round. `occ_ref: plan Global Constraints / plan T16 / memo D32`.
101. **[pattern/decided][process]** Whole-branch review on the strongest model at close, plus a roll-up funnel over reviewer minors (37 in -> 3 fix-now / 16 defer / 14 discard / 4 resolved, each with a named vehicle). `occ_ref: whole-branch-verdict.md / plan T23`.
102. **[pattern/violated-corrected][process]** Spec recorded `SchemaDrift` as `warning` against the owner's `info` ruling — introduced by the T16.5 review's own spec amendment (ce4fae1), each side locally consistent, caught ONLY by the whole-branch review (C1); fixed in the close wave. Under spec-wins doctrine it was live ammunition for a future wrong "fix." `occ_ref: whole-branch-verdict.md C1 / whole-branch-fix-report.md`.
103. **[pattern/violated-corrected][process]** §5.2 catalog missing shipped rows: `EmptyPlan`/`UnknownExtension`/`WorkerPanicked` added in the close wave (I1); a FOURTH, `UnsupportedSource`, surfaced by the fixer and added via controller row f25b02d. `occ_ref: whole-branch-verdict.md I1 / whole-branch-fix-report.md "Anything unexpected" / progress.md f25b02d`.
104. **[non-decision/deferred][i18n]** Core emits English prose through the `allowed` param (2 sites), rendering mixed-language in de mode on the marquee `language` property; correct fix is catalog-side (a `kind` selector), too large for the close. `occ_ref: whole-branch-verdict.md I2`. `blocked_on: pre-1.0 polish / Plan 6 (internal)`.
105. **[pattern/violated-corrected][process]** Collaboration correction: the question-dialog overlay swallowed pre-question context twice (owner could not see what he was deciding) — new pattern: context as a plain message first, question after. `occ_ref: journal 2026-07-12 Plan 5.5 entry (Friction and failure)`.
