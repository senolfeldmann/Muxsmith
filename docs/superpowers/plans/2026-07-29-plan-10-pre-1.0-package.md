# Plan 10: the pre-1.0 product package

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **House deviation from the skill text:** progress NEVER enters this document. No box in this file is ever ticked; the checkbox syntax is structure, not a tracking surface. The tracker is `.superpowers/sdd/plan-10/progress.md`.
>
> **Execution starts only on the owner's plan approval** (standing gate; same rule the Plan-8.5 and Plan-9 headers carry).
>
> **There is no design document for this package, deliberately and with the owner's approval.** Three of the five work items are specified to the line by a prior review or by an owner ruling; the two that carried real design content were settled controller-side with verified facts and written into the plan brief. The coverage ground truth for this plan is therefore `.superpowers/sdd/plan-10/plan-brief.md` together with the ROADMAP entries it cites, not a design file. A later reader looking for `docs/superpowers/specs/...plan10...` will not find one, and none is missing.

**Goal:** land the five work items the plan brief enumerates, none of which changes shipped product behaviour: the missing D102 producer for the preserved-order half of the report contract (W1); the sweep that removes line-number code citations from source comments under the owner's `comments-locate-by-symbol-never-by-line-number` ruling (W2); a canonical statement of the gate's part count in `BUILDING.md` plus a check in `scripts/ledger-lint.py` that verifies it against the enumerated commands (W3); the settled `renovate.jsonc` at the repo root (W4); and the README accuracy pass over the CLI usage reference, the exact-typed-matching paragraph and the recorded matching-magic content anchor (W5).

**Where this package sits, because it changes what "done" means.** This is the last planned product package before the 1.0 tag and it is **not** the last thing that happens before the tag. Tier-2 `owner-manual-qa-gates-the-1-0-release`: no 1.0 release is cut before the owner has personally run a manual QA and bug-hunting pass on his own hardware, and that pass's output (real bugs, behaviour he dislikes even where it matches the spec, v1.x items he decides belong in 1.0) is first-class scope input. His pass runs in parallel with this package, on the rehearsal build at commit `a5b63ba`, because none of the five work items changes product behaviour. **No task in this plan bumps a version, prepares or creates a tag, or edits a release body**, and no sentence in this plan claims the package completes 1.0 scope. The version bump to 1.0.0 belongs to the tag pass.

**Architecture:** five strictly serial tasks on `master` in the main worktree - no branches, no worktrees (ruling and reasoning in the sequencing section). Each task commits. One gate run, foreground, before the single push at the plan close (the gate's binding site is pre-push; this plan has no merges). No task edits any house-knowledge YAML; ledger writes are controller close actions.

**Tech Stack:** Rust workspace (toolchain pinned via `rust-toolchain.toml`, currently 1.96.1), Tauri 2 / Vue 3 / TypeScript frontend, Playwright e2e with the in-repo mock+mount harness (`e2e/`), Fluent catalogs under `locales/`, insta snapshots for CLI human output, `scripts/ledger-lint.py` (Python 3 + PyYAML) as the house-knowledge checker. **No new dependency of any kind, cargo or npm, and no new gate part, CI job or runtime dependency.** W4's validation runs the vendor's validator once via `npx` and leaves nothing behind.

## Global Constraints

- **Ground truth and precedence:** the v1 spec (`docs/superpowers/specs/2026-07-08-muxsmith-v1-design.md`) is authoritative on conflict. Below it: the plan brief (`.superpowers/sdd/plan-10/plan-brief.md`) for the five work items and the decisions already settled; `docs/ROADMAP.md` for the D102 paragraph in the Plan-9 anchor with its "RULED 2026-07-29 ... BUILD IT" close, the "Gate-count derivation has no check" section including its MEASURED and NARROWED FORM blocks, the "Docs accuracy" stale-citation entry including its RULED block, and the README and Renovate entries in the Pre-1.0 release gates section; `BUILDING.md` for the gate, verbatim, and as W3's own subject. The four house-knowledge files (`docs/product-boundaries.yaml`, `docs/conventions.yaml`, `docs/process-conventions.yaml`, `docs/decision-ledger.yaml`) are ground truth alongside them: cite entries by id. The ones that bind this package hardest: `comments-locate-by-symbol-never-by-line-number`, `tests-ship-with-the-feature-never-after`, `ci-04-dependabot-cadence`, `owner-manual-qa-gates-the-1-0-release`, `ledger-lint-runs-before-every-push`.
- **The gate as `BUILDING.md` enumerates it**, run foreground, no subsets, **before any push**. This constraint deliberately names the FILE and states no count: W3 is about to change what that file says about its own total, and a plan that hardcoded a number here would fork the contract it executes against. Per-task verification below names the subset each task must run green before committing; that subset is a task exit bar, not a gate substitute. The mandatory full run is the pre-push run in the close actions.
- **No design decision is re-opened, softened, or "improved".** A contradiction discovered on code contact is refuted with evidence or returned, never silently absorbed.
- **Every fork in this plan is closed.** No task brief, verdict or fix-round dispatch may carry a design-latitude clause, in either form: an explicit permission, or an omission - an unenumerated set in a normative position, a list ending open, a "one per X" with no X list, a step that requires inventing a name, a string, or a file that is not written down somewhere the implementer can read. A fork discovered on code contact returns as **NEEDS_CONTEXT with a decision memo** (options, costs against the named invariants, a recommendation) and is routed by the controller, never resolved at the keyboard (`proc-latitude-clause-boundary`).
- **No task edits any house-knowledge YAML** (`docs/decision-ledger.yaml`, the three Tier-2 files). The controller is the single writer; a task that finds something ledger-worthy SURFACES it in its report. No task edits `docs/ROADMAP.md` or `docs/process-journal.md` either; ROADMAP dispositions are close actions.
- **No task creates a tag, bumps a version, publishes or edits a release, resolves a README `placeholder(1.0)` comment, or removes the README work-in-progress banner.**
- **No new permanent gate part, CI job or runtime dependency** for W4's validation. W3 adds a check INSIDE an existing gate part (`scripts/ledger-lint.py`, already the gate's house-knowledge part per `ledger-lint-runs-before-every-push`), which is why it does not change the gate's own count - the recursion the brief names and avoids. **`scripts/ledger-lint.py` is NOT renamed**: a rename would ripple into `BUILDING.md`, `.github/workflows/ci.yml` and every doc citing the script. Its self-description widens instead.
- **SI-4 (restate in every dispatch that expects a commit; `dispatch-restates-the-standing-commit-grant`):** commits and pushes on this repo are standing-authorized by the owner; agent commits are deliberately unsigned - `git -c commit.gpgsign=false commit ...` - with exactly one trailer, `Co-Authored-By: Claude <model> <noreply@anthropic.com>`, where `<model>` is the canonical model name **derived from that dispatch's explicit model parameter, never written as a literal in this plan or a task brief** (`agent-commit-trailer-set`; no `Claude-Session` line, no context-window suffix). Stage files explicitly, **never `git add -A`**; the single push gets a `gh-log.md` entry.
- **Counts are recomputed from their enumerations** (`proc-normative-count-recomputed`): every count in this plan was recomputed from its list at plan-authoring (2026-07-29); a task that changes a set re-recounts and updates the consuming line in the same change.
- **Verification steps whose expected result is an absence are fire-verified AND have a reachable green state** (`proc-verification-step-must-be-falsifiable`, `proc-check-green-state-reachable`): break it or run it on the pre-state where it must hit, watch it fire, then reach the pass on the intended end state. Each absence check below carries both halves.
- **A proposed safeguard stays** (`proc-proposed-safeguard-stays`): a guard, test or check this plan proposes is removed only after it is built and MEASURED redundant, never argued away during authoring or review.
- **Every test this package's own behaviour makes observable ships in the same task** (`tests-ship-with-the-feature-never-after`), not in a later item.
- **Evidence lines carry pasted output** (`design-empirical-claims-reproducible`): every observed value in a task report is pasted from the run that produced it, never recalled, and never attributed to a command that was not the one run.
- **Typography:** ASCII hyphens, straight quotes, no Unicode ellipsis, in this plan, in every code comment, and in every string it prescribes. German orthography in Fluent catalogs is orthography, not an AI-tell glyph, and is copied exactly where it appears.
- **Implementer preamble, verbatim in every dispatch:** subagents never call session-relocation tools (EnterWorktree/ExitWorktree or any equivalent); absolute paths; foreground runs only; work on `master` in the main worktree.

## Execution method (binding)

Subagent-driven development (`superpowers:subagent-driven-development`): a fresh implementer subagent per task, an independent reviewer per task grading against this plan, the plan brief, the ROADMAP entries it cites and the spec, and a whole-branch review at the plan close before the close actions. Progress lives in `.superpowers/sdd/plan-10/progress.md`.

## Model tiers (`proc-03-model-assignment`)

Every task reviewer runs the mid tier; the whole-branch review at the plan close runs the top tier, which is the ONLY role the top tier serves; the controller loop runs mid. The controller sets the model parameter explicitly at every dispatch - an omitted parameter inherits the session default, which is not an assignment. **No task in this plan qualifies for the cheap tier:** the cheap tier is reserved for work this plan carries verbatim (transcription). Task 3 comes closest, since its file content is fenced here character for character, but its mandated re-verification of every option against current vendor documentation and its validator run are judgment, not transcription.

| task | tier | ground |
|---|---|---|
| 1 (W3: gate-count invariant) | mid | a parser plus its three fire-verifications and a widened self-description across three files; the counting rule is fixed, the integration into an existing script is composed |
| 2 (W1: D102 producers) | mid | a four-mutation measurement whose outcome selects among enumerated producers; test composition against existing core test idioms |
| 3 (W4: renovate.jsonc) | mid | file content is fenced, but re-verifying every option against current vendor docs and adjudicating a validator finding is judgment |
| 4 (W5: README accuracy) | mid | prose against a measured surface; every claim verified against binary, spec and code before it is written |
| 5 (W2: comment sweep) | mid | twenty sites, each needing the cited code read to name the right symbol |

## Authoring-time verification (2026-07-29, at `de4ea38`)

Every value below is pasted from a run made at plan-authoring on the current `master` head `de4ea38`. The three commits since the brief's measurement commit `6b12d0c` are `6b12d0c` -> `d64a921` -> `de4ea38`, all docs/house-YAML only, so no source file moved between the brief and this plan.

- **The gate, run end to end in BUILDING.md's own order on the unchanged tree, green:** `37.36s user 5.45s system 249% cpu 17.186 total`. Working tree after the run: `git status --porcelain` prints nothing. This is a WARM number (populated `target/`, installed `node_modules/`), and it is the number the sequencing section uses.
- **`mkvmerge` on this machine:** `/home/linuxbrew/.linuxbrew/bin/mkvmerge`, `mkvmerge v100.0 ('Do Hot Girls Like Chords') 64-bit`. Load-bearing for Task 2: the existing `batch_document` sort guard skips itself when mkvmerge is absent.
- **Gate-block command counts in `BUILDING.md`** (`sed -n '<span>p' BUILDING.md | grep -cvE '^\s*(#|$)'`): Rust block (lines 77-82) `6`; frontend block (105-108) `4`; house-knowledge block (114) `1`. Sum `11`. Continuation check over the same three spans (`grep -c '\\$'`): `0`. **Fired control for that continuation check:** the same grep over the release-reproduction block (142-147) returns `1`, so the pattern demonstrably matches a backslash continuation when one is present.
- **`BUILDING.md` states no total and exactly one section count.** `grep -niE "\b(one|two|three|four|five|six|seven|eight|nine|ten|eleven|twelve)\b" BUILDING.md` returns three lines: `:74` `### The Rust gate (six parts, run from the repo root, workspace-wide)`, `:98` "five runs in Plan 8", `:122` "three OS legs". Only `:74` is a gate count. **The brief's premise that the file "says six parts in one section and four in another" is refuted: the Frontend checks section states no count** (correction 1 below).
- **Fenced code blocks in `BUILDING.md`** (`grep -n '^```'`): seven, at `28/30`, `34/37`, `62/66`, `76/83`, `104/109`, `113/115`, `141/148`. Only three of the seven are gate blocks, which is why Task 1's check selects them by explicit marker and not by fence position or heading prose.
- **No reference to the Rust-gate heading's wording needs to move with it.** `git grep -n "The Rust gate\|the-rust-gate\|Rust gate"` excluding `docs/ROADMAP.md` and `docs/process-journal.md` returns **57 lines**, distributed as: `BUILDING.md:74` (the heading itself); **45 under `docs/process-journal/artifacts/`**; **6 in four retired plan documents under `docs/superpowers/plans/`** (`2026-07-14-plan-5.7-...:102`, `:124`, `:148`; `2026-07-21-plan-7-help-i18n.md:1926`; `2026-07-27-plan-8.5-...:20`; `2026-07-28-plan-9-...:20`); and 5 in this plan. The ground is not location but **principle, the same one the ROADMAP's MEASURED block already establishes for every other stale gate count: a retired plan document is history and is not edited.** The plan-8.5 and plan-9 hits are the sharpest case and are named rather than hidden - both quote the heading's wording inside their own normative gate constraint ("Ten-part gate per BUILDING.md ('The Rust gate' six parts + the four frontend checks)"), and both were true when written for a gate that had ten parts. Rewriting them to today's file would falsify their own record. **No live consumer therefore has to follow the heading**, which is what Step 1(b) needs and all this line asserts. Separately, no markdown anchor link targets the heading: `git grep -nE '\]\([^)]*#the-rust-gate'` returns nothing, and the **fired control** for that pattern is `git grep -nE '\]\([^)]*#[a-zA-Z]' -- '*.md'`, which returns 4 files starting with `.github/release/draft-body.md:1` (`INSTALL.md#windows`), so the expression demonstrably matches an anchor link when one exists.
- **The line-citation corpus has two expressions, and its union is 24 matched lines across 16 files.** Both are pasted here and both are re-run by Task 5 before it edits anything.
  - **Expression A, filename citations** - the brief's own derivation rule, "a filename followed by a colon and digits inside a comment": `git ls-files -- '*.rs' '*.ts' '*.vue' '*.mjs' '*.js' '*.py' | xargs grep -nE '[A-Za-z0-9_./-]+\.(rs|ts|vue|mjs|js|py|toml|ftl|json|yaml|md):[0-9]+'`. **20 lines across 13 files, not the brief's 17** (correction 2 below), pasted by file: `crates/muxsmith-cli/tests/run_live.rs` (`:273`, `:274`, `:315`, `:326`, `:327`, `:361`), `crates/muxsmith-core/src/identify.rs` (`:557`), `crates/muxsmith-core/src/planner.rs` (`:2226`), `crates/muxsmith-core/src/report/json.rs` (`:161`), `crates/muxsmith-core/tests/suggestions.rs` (`:1015`, `:1035`, `:1071`), `e2e/smoke.spec.ts` (`:1434`), `src-tauri/src/lib.rs` (`:730`), `src/components/ResolutionTable.vue` (`:32`), `src/editor/fieldSpec.ts` (`:65`), `src/editor/widgets/FieldWidgetDispatcher.vue` (`:6`), `src/editor/widgets/OptionalFlagWidget.vue` (`:4`), `src/editor/widgets/PropertyMapWidget.vue` (`:48`), `src/views/EditorView.vue` (`:83`). File-count control: the same expression with `grep -l` returns `13`.
  - **Expression B, bare line spans** - a colon-and-digits preceded by start-of-line, whitespace, a backtick, a comma or an open paren, i.e. a line-number locator whose FILE is named in the surrounding prose rather than in the token. Run per file so the `file:line:` prefix cannot pollute the match (the naive one-pipeline form matches its own prefix, which is how a first pass at this measurement came back empty): `git ls-files -- '*.rs' '*.ts' '*.vue' '*.mjs' '*.js' '*.py' | while read -r f; do grep -nE '(^|[[:space:]`,(])[:][0-9]+' "$f" | sed "s|^|$f:|"; done`, with the `file:line:` prefix stripped before the second filter. **4 lines across 4 files**, pasted: `crates/muxsmith-core/tests/profile_save.rs:95` ("design `:1517-1535`"), `crates/muxsmith-core/tests/suggestions.rs:1016` ("`// :1817). Returns None ...`"), `crates/muxsmith-core/tests/ts_export.rs:10` ("D44 `:592-611`"), `src/editor/registries.ts:12` ("design `:889-936`").
  - **Union: 24 matched lines across 16 files** (`suggestions.rs` is the one file both expressions hit). Expression B is what makes the enumeration honest in two ways at once. It catches the three bare-span design citations, which **controller ruling folds INTO Task 5's corpus** (see the task): a bare span without a filename is the WORSE form of what the owner's ruling bans, not an exempted one, and excluding them on a property of the search pattern would repeat exactly the mis-enumeration that cost the corpus its first count. And it catches `suggestions.rs:1016`, the CONTINUATION of the citation opened at `:1015` (`planner.rs:1812,` / `:1817)`), which expression A cannot see and which a backtick-only variant of B also misses - which is why **the rewrite unit is the COMMENT, not the matched line.**
- **D102's contract surfaces and their current guards**, from reading the tree:
  - The contract is asserted in three normative places: **spec section 5.2 "Diagnostics"** (the sentence "In every report document, the `config_diagnostics` array is ordered errors-first (error, warning, info), stable within a severity (ties keep collection order); per-file `diagnostics` and `batch_diagnostics` keep collection order", at `:255` at `de4ea38` - a line citation in a process artifact recording a measurement at a named commit, which the convention's own scope boundary permits and which the section name makes durable anyway), the Plan-9 design's D102, and both builder rustdocs in `crates/muxsmith-core/src/report/json.rs`.
  - `batch_document` sorts `config_diagnostics` via `severity_sorted` and emits `files[].diagnostics` and `batch_diagnostics` through `rendered_diags`, which preserves order. `config_only_document` sorts `config_diagnostics` and emits `files` and `batch_diagnostics` as literal `[]`, so **no preserved-order half exists on that builder** and the contract has exactly four halves, enumerated in the acceptance map.
  - Existing guards found: `crates/muxsmith-cli/tests/dry_run_cli.rs::dry_run_json_sorts_config_diagnostics_errors_first_when_planning_ran`, whose doc comment names itself "The `batch_document` half of the same D102 change" and which is gated on `have_mkvmerge()`; `crates/muxsmith-cli/tests/dry_run_cli.rs::dry_run_and_validate_json_agree_on_config_diagnostics_ordering`, which forces the no-mkvmerge path and therefore exercises `config_only_document`; and `crates/muxsmith-core/src/report/mod.rs`'s inline `severity_sorted_orders_errors_first_stable_within_severity`, which tests the FUNCTION, not either document. All three ran green in the authoring gate run (pasted names from that run's output: `test dry_run_and_validate_json_agree_on_config_diagnostics_ordering ... ok`, `test dry_run_json_sorts_config_diagnostics_errors_first_when_planning_ran ... ok`, `test report::tests::severity_sorted_orders_errors_first_stable_within_severity ... ok`).
  - `crates/muxsmith-core/tests/report_json.rs` exists and today covers `run_document` only, with hand-built JSON bases and no `DiagnosticRenderer` implementation. The two production implementations are `crates/muxsmith-cli/src/i18n.rs` (`Renderer`) and `src-tauri/src/lib.rs` (`ShellRenderer`); neither is reachable from a core test, so Task 2 writes the stub this plan fences.
- **README premises, verified against the binary and the source at authoring:**
  - `./target/debug/muxsmith schema --help` prints the about line `Print the profile JSON Schema`, then `Usage: muxsmith schema`, then `Options:` carrying `-h, --help  Print help` as its only entry. **No `--json`, no `--locale`.** `README.md:115` says "Every one of them takes `--json` ... and `--locale`" and `README.md:91` says "every command takes `--json`". **Both are false for `schema`.**
  - `README.md:181` states the exit-code contract as "`0` clean, `1` finished with warnings, `2` errors" and stops there. `crates/muxsmith-cli/src/commands/run.rs` contains `std::process::exit(130);` and `return 130;`, and `crates/muxsmith-cli/src/cli.rs`'s `Cli` doc comment carries "0 clean / 1 warnings / 2 errors / 130 cancelled (spec 8.1, D16)". **130 is missing from the README.**
  - `--on-collision`'s value domain is `error` (default policy), `skip`, `overwrite`, each carrying a one-line description in the binary's long help. The README names the flag as `<policy>` in two synopses and never enumerates the domain.
  - The exact-typed-matching paragraph is `README.md:53`, inside the profile-sample section, not in the CLI section.
- **Renovate premises, each re-verified at the vendor's current documentation or source, 2026-07-29.** Every settled item in the brief held; the two refinements are corrections 3 and 4 below.
  - `renovate.jsonc` is item 2 of 14 in the documented configuration-filename search order (docs.renovatebot.com/configuration-options/, "Locations for configuration filenames"), backed by `lib/config/app-strings.ts`'s `renovate.json{,c,5}` brace expansion. The same page: "We also recommend you prefer using a `.jsonc` file if you want to add comments to your configuration, instead of a `.json5` file."
  - `$schema: https://docs.renovatebot.com/renovate-schema.json` is the current value, and is what `lib/config/options/index.ts` writes into its own `onboardingConfig` default.
  - `config:recommended` exists and is the recommended base preset. `helpers:pinGitHubActionDigests` is `{ matchDepTypes: ['action'], pinDigests: true }`.
  - `config:best-practices` extends `[..., 'security:minimumReleaseAgeNpm', ':maintainLockFilesWeekly']`, and `:maintainLockFilesWeekly` is `lockFileMaintenance: { enabled: true, extends: ['schedule:weekly'] }`; it also extends `docker:pinDigests`. Both reasons the brief gives for not using it hold.
  - `schedule:monthly` is literally `const monthly = ['* 0-3 1 * *'];` in `lib/config/presets/internal/schedule.preset.ts`. The scheduling docs state: "If the backend configuration for Renovate means it runs scheduled jobs per-repo approximately every X hours, it is not possible for repository configuration to reduce that to less than X, or to force Renovate to run at exact times on specific repos." A four-hour window on one day per month can therefore be missed entirely.
  - `prHourlyLimit` default is `2` (`lib/config/options/index.ts`, `default: 2`).
  - GitHub Actions depTypes `action` ("A repository-based action reference in a uses: field") and `github-runner` ("A GitHub-hosted runner version in a runs-on: field") both exist (`lib/modules/manager/github-actions/dep-types.ts`). npm depType `packageManager` exists (`lib/modules/manager/npm/dep-types.ts`). Managers `mise` and `rust-toolchain` are both registered in `lib/modules/manager/api.ts` (`api.set('mise', mise);`, `api.set('rust-toolchain', rustToolchain);`); the `rust-toolchain` manager matches `/(^|/)rust-toolchain(\.toml)?$/`.
  - **Majors stay out of the ecosystem groups by default, quoted from `docs/usage/configuration-options.md`:** "This option also has priority over package groups configured by `packageRule`. So Renovate will propose separate PRs for major and minor updates of packages even if they are grouped. If you want to enforce grouped package updates, you need to set this option to `false` within the `packageRule`." The correct implementation is therefore to set `separateMajorMinor` nowhere.
  - `vulnerabilityAlerts` defaults are already the ruled behaviour: "When Renovate creates a vulnerabilityAlerts PR, it ignores settings like branchConcurrentLimit, commitHourlyLimit, prConcurrentLimit, prHourlyLimit, or schedule ... In short: vulnerability alerts 'skip the line'", with the shipped default object carrying `schedule: []` and `prCreation: 'immediate'`. The same page states the GitHub-side precondition: "For this to work, you must enable the Dependency graph, and Dependabot alerts."
  - Onboarding suppression: `lib/workers/repository/onboarding/branch/check.ts` returns `isOnboarded() == true` as soon as `configFileExists()`, before any onboarding branch is created. A config already on `master` therefore suppresses the vendor's onboarding PR.
- **Repo facts W4's comments assert:** `mise.toml` pins `node = "26.5.0"` and `pnpm = "11.10.0"`; `package.json:6` carries `"packageManager": "pnpm@11.10.0"` (the deliberate mirror); `rust-toolchain.toml` pins `channel = "1.96.1"`; `.github/workflows/release.yml` pins Ubuntu 22.04 at three sites - `runs-on:` at `:29` and `:186`, and the matrix `os:` key at `:81`, which `runs-on: ${{ matrix.os }}` at `:83` consumes - with an in-file comment at `:22-23` recording that it "deliberately diverges from the test matrix's ubuntu-26.04". No `renovate*` or `dependabot*` file is tracked today (`git ls-files | grep -iE "renovate|dependabot"` returns nothing; fired control: the same pipeline with `mise` returns `mise.toml`).

### Corrections to the brief found at plan-authoring (`proc-57-briefs-not-ground-truth`)

None of the four changes a ruling; two change a number a task acts on.

| # | Brief statement | Reality |
|---|---|---|
| 1 | `BUILDING.md` "says six parts in one section and four in another" | **The Frontend checks section states no count.** The file's only gate count is `### The Rust gate (six parts, ...)` at `:74`; the number-word grep above returns no other gate count. The consequence for Task 1 is real but small: exactly one pre-existing section count has to be dealt with, not two, and Task 1 deals with it by moving the split into the canonical statement rather than leaving a second unchecked number in the file. |
| 2 | The comment-citation corpus is "17 sites" | **20 comment lines across 13 files** under the brief's own derivation rule (expression A above), and **24 lines across 16 files** once the controller's ruling folds in the bare-span form (expression B). The extra three under expression A are `crates/muxsmith-cli/tests/run_live.rs:274`, `:315` and `:327`, which cite `README.md:71-78` and `README.md:91`. The ROADMAP's measurement pattern looked for `<file>.rs:<line>` and its widening widened the SOURCE languages, not the cited file types, so citations pointing at a markdown file were never in the 17. They match the ruling's trigger exactly ("you are typing a colon followed by digits after a filename inside a comment") and are in scope. This is also the sharpest possible demonstration of why the ruling exists: **Task 4 of this plan edits `README.md`, so those three line spans are about to go stale inside this very package** - which is why the sequencing puts Task 5 after Task 4. |
| 3 | "the manager's default only updates `Cargo.lock` and leaves the manifest string stale" | **True in effect, imprecise in wording.** The cargo manager's literal default is the global `rangeStrategy: "auto"`; the manager readme states that `auto` resolves to `widen` when a "less than" instruction is present and to `update-lockfile` otherwise, and that "The update-lockfile default means that most upgrades will update Cargo.lock files without the need to change the value in Cargo.toml." The brief's conclusion stands and `rangeStrategy: "bump"` is still the right setting; only the description of what the default IS is corrected here, and the fenced comment in Task 3 uses the corrected wording. |
| 4 | "Validate the file with the official validator at a pinned version" (form unstated) | The currently documented invocation is `npx --yes --package renovate -- renovate-config-validator`, with `--strict` and a file argument both documented. Neither of the two forms the recon started from is current. Task 3's fence uses the documented form with the version pinned through npm's own `--package` syntax; the latest published version read from `https://registry.npmjs.org/renovate/latest` at authoring is `43.287.0`. |

## Work-item coverage map

The walk the plan reviewer repeats. A row missing here is a defect.

| Brief work item | Implemented by |
|---|---|
| W1. The D102 producer | Task 2 (measurement + the producers its measurement selects) |
| W2. The comment line-citation sweep | Task 5 |
| W3. The gate-count invariant | Task 1 |
| W4. The Renovate configuration | Task 3 (the file); the two ACTIVATION actions are OWNER actions and stay OPEN at the close |
| W5. The README accuracy pass | Task 4 |

## Sequencing, dependency graph, and the no-worktree ruling

**Strictly serial: Task 1 -> 2 -> 3 -> 4 -> 5.**

**The file overlap that exists, stated before the ordering argument leans on it.** The five tasks' **Files lists** are pairwise disjoint. Their **write-sets are not**, and the difference is the load-bearing part:

- **Task 2 transiently writes `crates/muxsmith-core/src/report/json.rs`** - four times, once per mutation, restored after each and proven byte-identical before the commit (Step 2). That file is item 4 of **Task 5's** Files list. It is the one production file two tasks touch, and it is invisible in the Files lists because Task 2's touch is a measurement rather than a deliverable.
- **Task 5 is the broad one, by construction**: its 16 files span every Rust crate (`muxsmith-core` src and tests, `muxsmith-cli` tests, `src-tauri`), the frontend (`src/views`, `src/components`, `src/editor`, `src/editor/widgets`) and the e2e suite. It therefore sits in the same crates and frontend directories as the other tasks' work even where it shares no individual file, which is a reason it runs LAST rather than a reason to fear it.
- Beyond those, nothing: no Task-5 site cites `BUILDING.md`, `ci.yml`, `ledger-lint.py` or `renovate.jsonc`, and the only other cross-task coupling is already stated at Task 4 (`crates/muxsmith-cli/tests/run_live.rs` inlines the README passthrough recipe verbatim, which Task 4 does not touch).

What orders the five is one hard edge and two cheap-and-real preferences.

- **4 -> 5, the only hard edge.** Task 4 edits `README.md`; three of Task 5's expression-A sites cite `README.md` line spans (correction 2). Both orders reach the same end state, because Task 5 removes those line numbers entirely - but only in this order does Task 5 name the anchors of the README that Task 4 actually shipped, rather than anchors Task 4 is about to move. Running Task 5 first would also mean this package knowingly staleifies three citations it had just repaired.
- **1 first, deliberately.** Task 1 changes the gate's own tooling (`scripts/ledger-lint.py`, the gate's house-knowledge part). Every later task runs the gate before committing, so putting Task 1 first buys four independent executions of the new check on a real tree at zero cost, and surfaces a broken check at Task 1's review rather than at the plan close.
- **Parallelism is unavailable even in principle, for two independent reasons, not one.** A concurrent writer's gate run would exercise a half-edited `ledger-lint.py` while Task 1 is live - and, sharper, **a concurrent Task 5 would be editing comments in `report/json.rs` while Task 2 has that same file deliberately broken by a mutation.** One tree means one index and one working state; the transient write-set above is what makes that concrete rather than cautionary.
- **2, 3 free.** Task 2 (core test file) and Task 3 (a new root-level config file) touch nothing any other task touches and depend on nothing. They sit between the gate-tooling task and the README chain because that keeps the two Rust-recompiling tasks (1 has none, 2 has one) away from the doc tail, where the gate runs are cheapest.

**No worktrees, as a ruling.** The doctrine's handle (`proc-08-parallel-worktrees`) is a comparison, not a count: a worktree stream costs a setup, a merge, a full gate on the merged state and the controller choreography around both, so it earns its place only when the task's own work exceeds that. Measured, so this is not a hand-wave: **the gate on the unchanged tree in the main worktree runs in 17.186 s wall** (authoring section). A fresh worktree carries neither `target/` nor `node_modules/`, so its first gate run is a cold cargo build plus a `pnpm install` - a number this plan could not measure, because creating a worktree is a git command this authoring dispatch is forbidden to run, and the honest statement is therefore "unmeasured but strictly larger, by construction". The comparison still resolves cleanly in one direction: the work in every one of these five tasks is a test file, a comment sweep, a check in an existing script, one config file and one doc pass, and not one of them exceeds even the WARM gate plus a merge, let alone a cold one. Serial in one tree wins, the way plans 8.5 and 9 ruled for their own reasons. **The serial ruling binds the CONTROLLER's dispatch concurrency too, not merely the task order** (`a-serial-ruling-binds-dispatch-concurrency-too`): no second writer is dispatched while a task is live. If the controller ever must dispatch a concurrent writer into this one tree anyway (a mid-plan amendment), pathspec-scoped commits (`git commit -- <paths>`) are mandatory, because one tree means one index (`concurrent-writers-need-pathspec-scoped-commits`).

Commits: each of the five tasks commits on `master` with explicit pathspecs (blocks per task). One push, at the close, after the full gate.

## Acceptance map

Every work item, walked in its HALVES, to the task implementing it and the named producer of each half. "MV" = machine-verifiable by a command or test named here. Where an item's producer is an owner observation it is marked as such.

| # | Observable half | Producer | MV |
|---|---|---|---|
| W1-a | `batch_document` sorts `config_diagnostics` errors-first | Task 2. If mutation M1 goes red: the existing `dry_run_cli.rs::dry_run_json_sorts_config_diagnostics_errors_first_when_planning_ran`, named in the report with its pasted failure. If M1 stays green: the new `report_json.rs::batch_document_sorts_config_diagnostics_errors_first` (Step 3, P3) | yes |
| W1-b | `config_only_document` sorts `config_diagnostics` errors-first | Task 2. If M2 goes red: the existing `dry_run_cli.rs::dry_run_and_validate_json_agree_on_config_diagnostics_ordering`. If M2 stays green: the new `report_json.rs::config_only_document_sorts_config_diagnostics_errors_first` (Step 3, P4) | yes |
| W1-c | `batch_document` leaves `batch_diagnostics` in collection order | Task 2. If M4 goes red: the guarding test, named with its pasted failure. If M4 stays green: the new `report_json.rs::batch_document_preserves_batch_diagnostics_collection_order` (Step 3, P1) | yes |
| W1-d | `batch_document` leaves per-file `files[].diagnostics` in collection order | Task 2. If M3 goes red: the guarding test, named with its pasted failure. If M3 stays green: the new `report_json.rs::batch_document_preserves_per_file_diagnostics_collection_order` (Step 3, P2) | yes |
| W2-a | No tracked source comment cites `<filename>:<line>` | Task 5, absence check A with its fire (Step 4) | yes |
| W2-b | No tracked source comment carries a bare `:<line>` span either | Task 5, absence check B with its own fire (Step 4). Separate row because expression A cannot see this form, which is exactly how its four members escaped the corpus's first two measurements | yes |
| W2-c | Each rewritten comment still points at the code it meant | Task 5, per-site symbol named and the gate green; the reviewer re-derives both expressions with its own instrument | yes |
| W3-a | `BUILDING.md` states the gate total once, canonically | Task 1, the fenced sentence plus its anchor (Step 1) | yes |
| W3-b | A stated total that disagrees with the enumerated commands turns the gate red | Task 1, fire F1 (canonical number edited) with pasted red output | yes |
| W3-c | A gate block that gains or loses a command without the sentence following turns the gate red | Task 1, fire F2 (a command line appended to a gate block) with pasted red output | yes |
| W3-d | The TOTAL anchor is load-bearing, not decorative | Task 1, fire F3 (the `gate-total` marker line removed) with pasted red output | yes |
| W3-e | Each BLOCK anchor is load-bearing, not decorative | Task 1, fire F4 (a `gate-block` marker line removed) with pasted red output. Separate row because F3 tests a different marker: four absence-shaped checks exist in this task, and an argued red state is not a measured one - in the one task whose entire subject is that claim | yes |
| W4-a | `renovate.jsonc` exists at the repo root and is valid | Task 3, `renovate-config-validator` run at a pinned version, plain and `--strict`, both outputs pasted (Step 3) | yes |
| W4-b | The config expresses the ruled cadence and shape | Task 3, per-option re-verification against current vendor docs, each with the URL read and the value observed (Step 2) | yes |
| W4-c | Renovate is ACTIVATED | **OWNER action, stays OPEN at the plan close.** Two steps only the owner can take: install the Renovate app against this repository, and enable the repository's dependency graph and Dependabot alerts so the immediate-security half exists at all. Ordering that matters: the config must be on `master` BEFORE the app is installed, which is what suppresses the vendor's onboarding PR | no, by nature |
| W5-a | The CLI usage reference matches the shipped surface | Task 4, the divergence table re-derived from the binary's own help output and pasted in the report; every corrected claim named against the help text that carries it | yes |
| W5-b | The exact-typed-matching paragraph matches actual behaviour | Task 4, each claim in the paragraph checked against spec section and core symbol, pasted per claim | yes |
| W5-c | The four magic properties are explicitly listed | Task 4, the enumerated four-item list written into the README, each item verified against its spec section and core symbol before it is written (Step 3) | yes |

---

## Task 1: `BUILDING.md` states the gate total once, and `ledger-lint` checks it (W3)

Read first: the plan brief section 4 W3; `docs/ROADMAP.md`'s "Gate-count derivation has no check" section in full, including its MEASURED and NARROWED FORM blocks; `BUILDING.md` in full; `scripts/ledger-lint.py` in full; Tier-2 `ledger-lint-runs-before-every-push`. Model tier: mid.

**Files (EXHAUSTIVE):**
- Modify: `BUILDING.md` (the canonical statement and its anchor; the three gate-block markers; the Rust-gate heading; the House-knowledge check section's description and its fence's inline comment; **and the CI paragraph's `ledger-lint` parenthetical** - nothing else in the file)
- Modify: `scripts/ledger-lint.py` (the module docstring, the new check, its wiring into `main()`, the summary line)
- Modify: `.github/workflows/ci.yml` (the `ledger-lint` job's leading comment block **and that job's step `name:` value** - the two self-descriptions this task's change falsifies; no runner, no pin, no `run:` line, no other job or step is touched)

**Why the two extra regions are in scope rather than fenced off.** This task's stated purpose is that the script's name must not mislead the next reader, and the brief requires that "its header comment and any self-description widen accordingly". **Four self-descriptions exist**, all measured: `BUILDING.md`'s House-knowledge section, `BUILDING.md:125`'s CI paragraph ("`cargo deny check` and `scripts/ledger-lint.py` (house-knowledge invariants, Plan-8 rider) run as independent jobs"), `ci.yml`'s job comment, and `ci.yml:180`'s step `name: ledger-lint (house YAML invariants)`. Widening two and fencing the other two off would leave the purpose half-served and would force an implementer to file a finding rather than repair a parenthetical its own edit falsified. Both regions are therefore NAMED in the Files list above, so the enumeration stays exhaustive and the within-file qualifier still bites everywhere else.

**Interfaces:**
- Consumes: nothing.
- Produces: the widened `scripts/ledger-lint.py`, which every later task's gate run executes.

- [ ] **Step 1: `BUILDING.md`.** Five edits, each fenced here, plus one deliberate non-edit at (f).

  (a) Insert, immediately after the paragraph that ends "and 'Reproducing a release bundle locally' below covers the local invocation." and immediately before the `### The Rust gate` heading, exactly:

```
<!-- gate-total; checked by scripts/ledger-lint.py -->
The pre-push gate is 11 parts: 6 Rust, 4 frontend, 1 house-knowledge. The three
blocks below are its authoritative enumeration; `scripts/ledger-lint.py` checks
this sentence against them, so the stated number cannot drift from what the file
actually lists.
```

  **Why the number is in this plan at all**, stated because the Global Constraints deliberately carry none: the gate CONSTRAINT names `BUILDING.md` and states no count, so this plan never forks the contract it executes against. The sentence above is not that constraint - it is this task's DELIVERABLE, the thing `BUILDING.md` will say. Its numbers are the authoring measurement pasted above (6 + 4 + 1 = 11), and the check this task ships is what verifies them. **If the implementer's own recount of the three blocks disagrees with `11 / 6 / 4 / 1`, that is not a licence to adjust the fence: the tree moved since authoring, and it returns as NEEDS_CONTEXT with both counts pasted.**

  (b) Replace the heading `### The Rust gate (six parts, run from the repo root, workspace-wide)` with exactly `### The Rust gate (run from the repo root, workspace-wide)`. The count moves into the canonical statement, where it is checked; leaving it here would leave the file with one checked number and one unchecked one, which is the defect class this task closes. The parenthetical's other content is preserved verbatim. No live reference to the old wording exists (authoring section); the historical process artifacts that quote it read "four parts" and "five parts" and are not edited.

  (c) Insert, each on its own line immediately above the opening ```` ```bash ```` fence of its block, exactly these three lines:

```
<!-- gate-block: rust; checked by scripts/ledger-lint.py -->
<!-- gate-block: frontend; checked by scripts/ledger-lint.py -->
<!-- gate-block: house; checked by scripts/ledger-lint.py -->
```

  **Why an HTML-comment marker and not a heading**, the anchor-stability argument the brief asks for: a marker is not prose. Headings in this file get reworded - this very step rewords one - and the frontend and house headings carry no token that distinguishes them from any future sibling, so `### Frontend checks (optional)` would silently break a heading match while a marker survives it. A marker is also invisible in every rendered view, so it does not cost the reader anything. **Both marker kinds are proved load-bearing by measurement rather than by argument:** fire F3 deletes the total anchor and fire F4 deletes a block anchor, and each must turn the gate red instead of silently disabling the check. Each marker names the script that consumes it, so the next reader knows what it is for without grepping.

  (d) Widen the House-knowledge check section so the script's name does not mislead. Two verbatim replacements, no other change to the section. The fence's single command line becomes exactly:

```
python3 scripts/ledger-lint.py   # house-knowledge YAML invariants + BUILDING.md's gate-count invariant
```

  and the whole paragraph beneath the fence is replaced by exactly:

```
A gate part like the Rust and frontend ones above, binding before every push
(house rule `ledger-lint-runs-before-every-push`). Despite the name it is the
house's docs-invariant checker: besides the four house-knowledge YAML files it
verifies that the canonical gate-total sentence above agrees with the commands
the three marked gate blocks enumerate, so changing a gate block without the
sentence, or the sentence without the blocks, turns this part red. Needs
PyYAML; CI runs it from a throwaway venv as its own job.
```

  The replacement keeps both facts the original paragraph carried and nothing else in this file states - the PyYAML prerequisite and the CI-job arrangement - because dropping either would trade one stale statement for a missing one. The script keeps its name (Global Constraints).

  (e) Widen the CI paragraph's `ledger-lint` parenthetical, which the same change falsifies. Replace exactly `and \`scripts/ledger-lint.py\` (house-knowledge invariants, Plan-8 rider)` with exactly `and \`scripts/ledger-lint.py\` (house-knowledge and gate-count invariants, Plan-8 rider)`. Nothing else in that paragraph changes - in particular its "Rust-gate parts 1-4" and "part 6" ordinals stay exactly as they are, for the reason in Step 1(f).

  (f) **Two positional ordinals in this file are OUT of this task, surfaced rather than dropped.** `BUILDING.md:95` ("The cross-target clippy run (part 6)") and `:121` ("runs Rust-gate parts 1-4") number parts WITHIN the Rust block; they are positions, not totals, and the new check parses totals. Two things are true at once and both belong in the report rather than in a silent decision: Step 1(b)'s reasoning about unchecked numbers does reach them in spirit, and once the file says "The pre-push gate is 11 parts" a bare "part 6" acquires a second possible referent, resolved only by section context. But covering them would need a second parser with its own anchor scheme, over a numbering the canonical sentence does not define, which is scope this package's brief did not authorize. **The task SURFACES both lines to the controller with this reasoning and edits neither**, the same treatment the plan gives the stale README counts.

- [ ] **Step 2: the check in `scripts/ledger-lint.py`.** Add a function `check_building_gate_total(violations: list[str]) -> None` and call it from `main()` before the summary, so its findings join the existing `violations` list and the existing exit-code logic. No new import beyond `re` and the already-imported `Path`; no new dependency. The behaviour, fixed:
  - Read `REPO / "BUILDING.md"`. A missing file is a violation.
  - **Blocks.** For each of the three marker lines `<!-- gate-block: rust; checked by scripts/ledger-lint.py -->`, `<!-- gate-block: frontend; checked by scripts/ledger-lint.py -->`, `<!-- gate-block: house; checked by scripts/ledger-lint.py -->` (matched by exact string on the stripped line): exactly one occurrence must exist (zero or more than one is a violation naming the marker). The next non-empty line after the marker must be exactly ```` ```bash ```` (otherwise a violation). Count lines until the next line that is exactly ```` ``` ````; an unterminated fence is a violation. A counted COMMAND LINE is a line whose stripped form is non-empty and does not start with `#`. **If any line inside a gate block ends with a backslash, that is a violation** with the message that the counter does not model shell continuations - explicit failure rather than a silent miscount.
  - **The stated total.** Exactly one occurrence of the line `<!-- gate-total; checked by scripts/ledger-lint.py -->` must exist. The next non-empty line after it must match `^The pre-push gate is (\d+) parts: (\d+) Rust, (\d+) frontend, (\d+) house-knowledge\.` (otherwise a violation quoting the line found, so the author sees what did not match). **If the marker is absent, or the line does not match, no stated numbers exist at all, so every comparison below is SKIPPED and that one violation stands alone** - the symmetric case to a missing block marker, and what fire F3 measures.
  - **The comparison.** Each of the three stated per-block numbers is compared against its block's counted commands, and the stated total against the sum of the three counted blocks. Every mismatch is its own violation line, in the existing `FAIL {rel}: ...` shape, naming both numbers, e.g. `BUILDING.md: gate-block 'frontend' states 4 commands but enumerates 5`. **A block whose marker was not found is reported as the missing-marker violation, and then BOTH that block's own comparison AND the total comparison are SKIPPED.** The block is not treated as zero commands, and the total is not compared against a partial sum, because with a block missing the counted total is not derivable at all. Otherwise a single deleted marker would emit three violations - missing marker, that block's mismatch, and a total mismatch - where exactly one names the cause, and fire F4 walks into precisely that state by construction. The same holds if the marker is present but its fence is unterminated: report that violation, skip the two comparisons.
  - The existing success line gains the new check: it currently reads `ledger-lint: {total} entries across {len(FILES)} files, all invariants hold` and becomes `ledger-lint: {total} entries across {len(FILES)} files plus BUILDING.md's gate enumeration, all invariants hold`.
  - The module docstring's first line and its numbered check list widen: the title stops claiming the script only covers the four YAML files, and check `7` is added, stating that `BUILDING.md`'s canonical gate-total sentence is compared against the commands its three marked gate blocks enumerate, and that the markers are the anchor because heading prose is not stable.

- [ ] **Step 3: `.github/workflows/ci.yml`.** Two regions, both named in the Files list, both self-descriptions this task's own edit falsified.
  - The `ledger-lint` job's leading comment enumerates the script's checks ("count==occurrences, refs present, blocked/tier fields, duplicate ids, per-entry duplicate keys"). Extend that enumeration with the gate-count invariant.
  - The job's step name is exactly `- name: ledger-lint (house YAML invariants)`. Replace the parenthetical so it reads exactly `- name: ledger-lint (house YAML + gate-count invariants)`; the CI log line is the most-read self-description of all, and leaving it narrow defeats the step's purpose.
  - **Nothing else in the file changes** - no `runs-on`, no pin, no `run:` line, no other job or step. Both repairs are in scope because each is a reference this task's own edit falsified inside a listed file, and both are named regions rather than a general licence (`latitude-carveout-zero-content-structural-forks`).

- [ ] **Step 4: fire-verification, five fires, all outputs pasted in the report.** A check whose passing result is an absence proves nothing until it has produced output once, and a green run alone is not evidence. This task specifies more absence-shaped checks than any other: four marker-existence checks (one total anchor, three block anchors, sharing two code paths - one per anchor KIND) and the continuation guard. Every one of those code paths gets a fire below.
  - **F1, the stated total.** Change `11 parts` to `12 parts` in the canonical sentence; run `python3 scripts/ledger-lint.py`; it must exit 1 and name the total mismatch. Restore.
  - **F2, the enumeration.** Append one command line (`pnpm lint`, duplicated) to the frontend gate block; run; it must exit 1 and name the frontend block AND the total. Restore.
  - **F3, the total anchor.** Delete the `<!-- gate-total; checked by scripts/ledger-lint.py -->` marker line; run; it must exit 1 and its violations must be EXACTLY ONE, naming the missing total anchor - the comparisons are skipped because no stated numbers exist (Step 2). Restore.
  - **F4, a block anchor.** Delete the `<!-- gate-block: frontend; checked by scripts/ledger-lint.py -->` marker line; run; it must exit 1 and its violations must be EXACTLY ONE, naming the missing `frontend` block marker - the frontend and total comparisons are skipped because a missing block makes the counted total underivable (Step 2). Restore. The frontend block is the chosen member because F2 already exercises it, so a mis-wired frontend branch cannot pass both fires by coincidence.
  - **F5, the continuation guard.** Rewrite the house-knowledge gate block's single command as two backslash-continued lines (`python3 \` / `  scripts/ledger-lint.py   # ...`); run. **It must exit 1 and its violations must INCLUDE the message that the counter does not model shell continuations; the accompanying house-block and total mismatches are expected, because the guard does not suppress the comparison. A run that reports only a count mismatch, with no continuation message, is the failure this fire exists to catch.** Restore. The expected output is spelled out rather than left to inference because this one mutation trips three checks at once, and they are all consequences of the fixed counting rule (Step 2), which this task may not alter: both continued lines are non-empty and neither starts with `#`, so the house block counts 2 against its stated 1; the total then counts 6 + 4 + 2 = 12 against its stated 11; and `python3 \` ends with a backslash, so the guard fires as well. Without this fire the guard would be the one absence-shaped check in the task with no measured red state, and its whole purpose is to fail loudly where the counter would otherwise miscount silently. Its own soundness control is separate and already measured: the same backslash pattern returns 1 against `BUILDING.md`'s release-reproduction block, which does carry a continuation (authoring section).
  - **Green state, reached on the intended end state, not on the pre-state:** after all five restores, `python3 scripts/ledger-lint.py` exits 0 and prints the widened summary line. `git status --porcelain` after the restores shows only this task's intended edits - paste it.

- [ ] **Step 5: verification.** The full gate as `BUILDING.md` enumerates it, foreground, green. `git diff --stat` limited to the three files in the Files list; anything else is a defect signal -> NEEDS_CONTEXT, not a local fix.

- [ ] **Step 6: commit.**

```bash
git add BUILDING.md scripts/ledger-lint.py .github/workflows/ci.yml
git -c commit.gpgsign=false commit -m "gate: BUILDING.md states the gate total once, ledger-lint checks it against the enumerated commands"
```

(Trailer per SI-4, derived from this dispatch's model parameter.)

**Must not decide:** the marker strings, the canonical sentence, the two fenced replacements in Step 1(d) and the one in Step 1(e), the counting rule and its continuation guard, the five fires, that `BUILDING.md`'s two positional ordinals are surfaced rather than edited (Step 1(f)), the home of the check (`scripts/ledger-lint.py`, not a new script - a new script would need a new gate part, which would change the gate's own count). The script is NOT renamed. No cross-file "N parts per BUILDING.md" lint is built in any form: the ROADMAP's MEASURED block establishes that every current occurrence outside `BUILDING.md` is a historical record and such a lint would demand that history be falsified.

---

## Task 2: the D102 preserved-order producers, selected by measurement (W1)

Read first: the plan brief section 4 W1; `docs/ROADMAP.md`'s D102 paragraph in the Plan-9 anchor through its "RULED 2026-07-29 ... BUILD IT" close; spec line 255; `crates/muxsmith-core/src/report/json.rs`'s two builder rustdocs; the existing guards named in the authoring section; Tier-2 `tests-ship-with-the-feature-never-after`. Model tier: mid.

**Files (EXHAUSTIVE):**
- Modify: `crates/muxsmith-core/tests/report_json.rs` (the stub renderer, the shared fixture, and the producers Step 2's measurement selects)

No other file is touched. In particular `crates/muxsmith-core/src/report/json.rs` is mutated and fully restored inside Step 2 and must be byte-identical at commit time, and `crates/muxsmith-cli/tests/dry_run_cli.rs` is NOT edited - its existing `batch_document` guard is reused, not duplicated (`reuse before writing`).

**Interfaces:**
- Consumes: nothing.
- Produces: the missing halves of the D102 contract's coverage.

- [ ] **Step 1: record the precondition.** Run `mkvmerge --version` and paste it into the report. The existing `batch_document` sort guard is gated on `have_mkvmerge()` and turns into a no-op without it, so a measurement run on a machine without mkvmerge would report that half unguarded for the wrong reason. **If mkvmerge is absent, this task returns NEEDS_CONTEXT rather than measuring** (the authoring machine had `mkvmerge v100.0`).

- [ ] **Step 2: measure per array, not once.** Four mutations, each applied alone to `crates/muxsmith-core/src/report/json.rs`, each followed by `cargo test --workspace` foreground, each restored before the next. Paste every run's result line and every failing test name.
  - **M1** (`batch_document`, sorted half): replace `severity_sorted(config_diags).into_iter().map(|d| rendered_diag(d, renderer)).collect()` in `batch_document` with `rendered_diags(config_diags, renderer)`.
  - **M2** (`config_only_document`, sorted half): the same replacement in `config_only_document`.
  - **M3** (`batch_document`, per-file preserved half): change the per-file mapping so `"diagnostics"` is built from `severity_sorted(&f.diagnostics).into_iter().map(|d| rendered_diag(d, renderer)).collect::<Vec<_>>()` instead of `rendered_diags(&f.diagnostics, renderer)`.
  - **M4** (`batch_document`, batch preserved half): the same substitution for `"batch_diagnostics"` against `batch.batch_diagnostics`.
  - **Reading the result, fixed:** a mutation that turns the suite RED means that half is already guarded; name the failing test in the report and write NO producer for it. A mutation that leaves the suite GREEN means that half is unguarded and gets its enumerated producer from Step 3. This is the whole reason the measurement exists rather than an eyeball: the contract has four halves and the review that surfaced it measured one.
  - **Restore and prove it:** after M4's restore, `git status --porcelain` and `git diff --stat` both print nothing for `crates/muxsmith-core/src/report/json.rs`. Fire: during M1-M4 the same commands show the modification - paste one mutated state and the restored state.

- [ ] **Step 3: the producers, in `crates/muxsmith-core/tests/report_json.rs`.** Write exactly the producers Step 2 selected, and only those. All four are fully specified here; none is invented.

  First, the shared scaffolding, written once regardless of which producers land (if Step 2 selects none, this task returns NEEDS_CONTEXT instead, because that outcome contradicts the ROADMAP's recorded measurement):

```rust
/// A stub renderer for the document-shape tests below. The `"rendered"`
/// field is not what these tests assert, so it echoes the diagnostic's own
/// catalog key rather than prose - core produces codes and params, never
/// text (spec 5.2, 8.4).
struct KeyRenderer;

impl DiagnosticRenderer for KeyRenderer {
    fn diagnostic(&self, d: &Diagnostic) -> String {
        d.code.key().to_string()
    }
}

/// Three diagnostics in COLLECTION order info, warning, error - deliberately
/// the reverse of the errors-first order, so an array that is sorted and an
/// array that is preserved cannot produce the same code sequence. Every test
/// below reads that discriminating property.
fn mixed_severity() -> Vec<Diagnostic> {
    vec![
        Diagnostic::info(DiagCode::RawProperty, "tracks[0].match.exact.raw:x"),
        Diagnostic::warning(DiagCode::RawOnKnownProperty, "tracks[1].match.exact.raw:language"),
        Diagnostic::error(DiagCode::InvalidRegex, "tracks[2].match.regex.title"),
    ]
}

/// The `code` sequence of a document array, which is what every ordering
/// assertion in this file compares.
fn codes(array: &serde_json::Value) -> Vec<&str> {
    array
        .as_array()
        .expect("diagnostic array")
        .iter()
        .map(|d| d["code"].as_str().expect("diagnostic code"))
        .collect()
}
```

  Imports this adds to the file's existing `use` block: `muxsmith_core::planner::{Batch, FileReport}`, `muxsmith_core::report::{DiagCode, Diagnostic}`, `muxsmith_core::report::json::{DiagnosticRenderer, batch_document, config_only_document}`, `std::path::PathBuf`. `config_only_document` is imported only if P4 lands.

  - **P1** (write iff M4 stayed green) - `batch_document_preserves_batch_diagnostics_collection_order`: build `Batch { files: Vec::new(), batch_diagnostics: mixed_severity(), suggestions: Vec::new() }`; call `batch_document(&[], &batch, &KeyRenderer)`; assert `codes(&doc["batch_diagnostics"]) == ["raw-property", "raw-on-known-property", "invalid-regex"]`, i.e. collection order, with the assertion message naming D102's scope boundary and **spec section 5.2, by section and never by line**. This is the shape the Plan-9 Task-5 review named. The section-not-line form is deliberate and applies to every string this task writes: an `assert!` message is a string rather than a comment, so `comments-locate-by-symbol-never-by-line-number` does not reach it by its letter, but writing one more line-number locator inside the package whose own W2 sweeps that class out would be incoherent, and the section name is equally precise and durable.
  - **P2** (write iff M3 stayed green) - `batch_document_preserves_per_file_diagnostics_collection_order`: build one `FileReport { source: PathBuf::from("S01E01.mkv"), identifier: "S01E01".to_string(), plan: None, diagnostics: mixed_severity() }` inside `Batch { files: vec![file], batch_diagnostics: Vec::new(), suggestions: Vec::new() }`; call `batch_document(&[], &batch, &KeyRenderer)`; assert `codes(&doc["files"][0]["diagnostics"])` equals the same collection-order sequence.
  - **P3** (write iff M1 stayed green) - `batch_document_sorts_config_diagnostics_errors_first`: call `batch_document(&mixed_severity(), &Batch { files: Vec::new(), batch_diagnostics: Vec::new(), suggestions: Vec::new() }, &KeyRenderer)`; assert `codes(&doc["config_diagnostics"]) == ["invalid-regex", "raw-on-known-property", "raw-property"]`.
  - **P4** (write iff M2 stayed green) - `config_only_document_sorts_config_diagnostics_errors_first`: call `config_only_document(&mixed_severity(), None, &KeyRenderer)`; assert the same errors-first sequence on `doc["config_diagnostics"]`.

- [ ] **Step 4: fire-verification of each producer that landed.** For each written producer, re-apply the mutation whose green result selected it, run `cargo test -p muxsmith-core --test report_json`, and paste the new test's FAILURE; restore, re-run, and paste the pass. That is the producer's own red state, measured rather than argued, and it is the evidence the reviewer re-measures with its own instrument.

- [ ] **Step 5: verification.** `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace` - green, foreground. Every pre-existing test passes unchanged: this task adds tests and touches no production code, so any pre-existing test's behaviour changing at all is a defect signal -> NEEDS_CONTEXT. Then the full gate as `BUILDING.md` enumerates it before the commit (Task 1's new check runs in it).

- [ ] **Step 6: commit.**

```bash
git add crates/muxsmith-core/tests/report_json.rs
git -c commit.gpgsign=false commit -m "report: guard D102's preserved-order half in batch_document, selected by mutation measurement"
```

(Trailer per SI-4, derived from this dispatch's model parameter.)

**Must not decide:** which halves get a producer (the measurement decides, among the four enumerated candidates); the producers' names, homes, fixtures and assertions; the reuse of the two existing dry_run_cli guards rather than duplicating them; that no production code changes. A mutation that reveals a REAL ordering defect rather than a coverage gap is a finding, not a test to relax: NEEDS_CONTEXT.

---

## Task 3: `renovate.jsonc` at the repo root (W4)

Read first: the plan brief section 4 W4 in full; Tier-2 `ci-04-dependabot-cadence` for the ruled cadence, grouping, major-PR and manager decisions; the ROADMAP's Renovate entry in the Pre-1.0 release gates section and its two riders; the authoring section's Renovate verification block. Model tier: mid.

**Files (EXHAUSTIVE):**
- Create: `renovate.jsonc`

Nothing else. No workflow, no gate part, no CI job, no dependency, and no edit to `deny.toml` (its RUSTSEC-ignore pruning is a rider that fires on Renovate PRs, not on this file).

**Interfaces:**
- Consumes: nothing.
- Produces: the config that must be on `master` BEFORE the owner installs the app, which is what suppresses the vendor's onboarding PR.

- [ ] **Step 1: write the file.** Exactly this content, transcribed character for character:

```jsonc
{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",

  // config:best-practices is deliberately NOT extended: it pulls
  // :maintainLockFilesWeekly (a weekly lock-file refresh, which contradicts
  // the ruled monthly cadence) and docker:pinDigests (this repo has no
  // Docker surface). security:minimumReleaseAgeNpm is deliberately not used
  // either: its interaction with a narrow schedule window is unverified, and
  // a monthly cadence already means most picked-up versions are weeks old.
  "extends": ["config:recommended", "helpers:pinGitHubActionDigests"],

  "timezone": "Europe/Berlin",

  // NOT the schedule:monthly preset. That preset is ['* 0-3 1 * *'] - a
  // four-hour window on the 1st - and the hosted service runs a repo's job
  // no more often than its own backend cadence allows, which the vendor
  // documents as something repo config cannot tighten. A four-hour window
  // on one day per month can therefore be missed entirely, for months. The
  // three-day window below is the same monthly cadence with no starvation.
  // Do not "simplify" this back to the preset.
  "schedule": ["* * 1-3 * *"],

  // The schema default is 2, which would dribble the ecosystem group PRs
  // across successive jobs instead of opening them in one pass.
  "prHourlyLimit": 0,

  // separateMajorMinor is deliberately absent. Its default is true and it
  // has priority over package groups, so major updates already get their
  // own PRs even inside the groups below - which is the ruled behaviour
  // (ci-04-dependabot-cadence). Setting it to false here would fold them in.
  "packageRules": [
    {
      "matchManagers": ["cargo"],
      "groupName": "cargo dependencies",
      // The cargo manager's default is the global rangeStrategy "auto",
      // which resolves to update-lockfile for cargo in the normal case:
      // Cargo.lock moves and the Cargo.toml version string stays put. This
      // repo's manifest pins are meant to be read as true, so bump.
      "rangeStrategy": "bump"
    },
    {
      "matchManagers": ["npm"],
      "groupName": "npm dependencies"
    },
    {
      "matchManagers": ["github-actions"],
      "matchDepTypes": ["action"],
      "groupName": "github actions"
    },
    {
      // release.yml pins ubuntu-22.04 on purpose (D85: the oldest supported
      // base for the AppImage glibc floor) while the test matrix runs
      // ubuntu-26.04. Renovate cannot know that distinction, so runner
      // images are not managed at all.
      "matchManagers": ["github-actions"],
      "matchDepTypes": ["github-runner"],
      "enabled": false
    },
    {
      // pnpm is pinned in BOTH mise.toml and package.json's packageManager
      // field, as a deliberate mirror: CI takes its pnpm from mise, a
      // developer without mise takes it from corepack via package.json.
      // Managing one side while the other stands still breaks the mirror
      // instead of maintaining it, so both move by hand until mise leaves.
      // This rule follows the npm group above on purpose: later rules win.
      "matchManagers": ["npm"],
      "matchDepTypes": ["packageManager"],
      "enabled": false
    },
    {
      // Owner ruling: mise is slated to leave the setup anyway, so it is
      // not worth managing in the meantime.
      "matchManagers": ["mise"],
      "enabled": false
    },
    {
      // Its own group, by owner ruling: a toolchain bump's characteristic
      // failure is new lints under -D warnings, and that must not hold an
      // unrelated dependency bump hostage inside a shared PR.
      "matchManagers": ["rust-toolchain"],
      "groupName": "rust toolchain"
    }
  ]
}
```

- [ ] **Step 2: re-verify every option against current vendor documentation before trusting the fence.** This is fast-moving vendor tooling and the plan's own verification is a day old. For each item below, name the URL read and paste the value observed: the `renovate.jsonc` filename search order; the `$schema` URL; `config:recommended`; `helpers:pinGitHubActionDigests`; what `config:best-practices` extends; the literal value of `schedule:monthly`; the documented statement about backend job cadence versus schedule windows; `prHourlyLimit`'s default; the cargo manager's default `rangeStrategy` and what `bump` does; the `action` and `github-runner` depTypes; the npm `packageManager` depType; the `mise` and `rust-toolchain` managers; `separateMajorMinor`'s default and its stated priority over package groups; and `vulnerabilityAlerts`' shipped defaults including whether they bypass schedule and rate limits with no explicit configuration.

  **One named trap on that list, so the re-verification does not "correct" a value that is already right.** The rendered configuration-options page yields **`prHourlyLimit` default `10`** under naive reading; that is `prConcurrentLimit`'s default, picked up from the neighbouring section. Only `lib/config/options/index.ts` settles it, where the entry reads `default: 2`. The fence's `prHourlyLimit: 0` and its comment's "the schema default is 2" are CORRECT, and a Step-2 pass that reads only the rendered page will appear to refute them. The general rule this instance teaches, binding for the whole list: **where a rendered docs page and the source disagree, `lib/config/options/index.ts` and `lib/config/presets/internal/` are the ground truth**, and the report names which of the two it read for each item.

  **A premise refuted at the source is a valid completion**: report it with the evidence and return NEEDS_CONTEXT rather than building around it silently. If the `vulnerabilityAlerts` defaults have moved, the immediate-and-ungrouped behaviour is configured explicitly and the report says so.

- [ ] **Step 3: validate, and paste the evidence.** Two runs, both pasted with their exact command and full output:

```bash
npx --yes --package renovate@43.287.0 -- renovate-config-validator renovate.jsonc
npx --yes --package renovate@43.287.0 -- renovate-config-validator --strict renovate.jsonc
```

  `43.287.0` is the version read from `https://registry.npmjs.org/renovate/latest` at plan-authoring. If that version no longer resolves, read the same URL, use the version it returns, and paste both the URL read and the version used. **A `--strict` finding is not fixed at the keyboard**: a migration or deprecation warning means one of this plan's fenced options has moved, which is a design question -> NEEDS_CONTEXT with the pasted output. Nothing about this validation becomes permanent: no gate part, no CI job, no dependency, no lockfile entry.

- [ ] **Step 4: what this task must NOT claim.** Writing this file does not activate Renovate. The report states plainly that activation needs two OWNER actions - installing the Renovate app against this repository, and enabling the repository's dependency graph and Dependabot alerts so the immediate-security half exists at all - and that the ordering matters: the config must be on `master` before the app is installed, which is what suppresses the onboarding PR. The report does NOT claim the ROADMAP trigger "Renovate/Dependabot activated -> prune deny.toml RUSTSEC ignores; TS-7 bump arrives via its PRs" has fired. It has not: its condition is activation, not configuration.

- [ ] **Step 5: verification.** The full gate as `BUILDING.md` enumerates it, foreground, green. `renovate.jsonc` is not consumed by any gate part, so the gate proves only that nothing else broke; the validator run in Step 3 is this file's own evidence.

- [ ] **Step 6: commit.**

```bash
git add renovate.jsonc
git -c commit.gpgsign=false commit -m "renovate: the ruled monthly per-ecosystem config, majors and security on their own paths"
```

(Trailer per SI-4, derived from this dispatch's model parameter.)

**Must not decide:** the file name and location; every key and value in the fence, including which options are deliberately ABSENT (`separateMajorMinor`, `vulnerabilityAlerts`, `config:best-practices`, `security:minimumReleaseAgeNpm`) - an absent option is as fixed as a present one here, and adding one "helpfully" is a defect; the comment texts, which exist so the next reader does not undo the reasoning; the validator invocation; that no gate part, CI job or dependency is added. Rule ORDER inside `packageRules` is fixed as fenced, because later rules win.

---

## Task 4: the README accuracy pass (W5)

Read first: the plan brief section 4 W5; `docs/ROADMAP.md`'s README entry in the Pre-1.0 release gates section, including its owner split of 2026-07-29 and its Content anchors block; `README.md` in full; the authoring section's README verification block. Model tier: mid.

**Files (EXHAUSTIVE):**
- Modify: `README.md`

**Interfaces:**
- Consumes: nothing.
- Produces: the final README anchors Task 5 will name in place of three line-span citations.

- [ ] **Step 1: re-derive the CLI surface from the binary, not from the source.** Build or locate `target/debug/muxsmith`; capture `muxsmith --help`, `muxsmith --version`, and `<sub> --help` for every subcommand it lists. Paste all of it in the report and build a divergence table (README says / binary says / verdict) at FLAG granularity, including value names, defaults and possible-value sets. A prior reviewer warned this section is "easy to lose": treat divergence as expected, not as surprising. Exit codes are not in the help text; derive them from `crates/muxsmith-cli/src/cli.rs`'s `Cli` doc comment and the `severity_exit` / `job_exit_code` functions, and name the symbols in the report.

- [ ] **Step 2: correct the CLI usage reference.** Every correction below was measured at plan-authoring and must be RE-measured in Step 1 before it is written; a correction the re-measurement does not reproduce is a finding, not a silent drop.
  - The blanket claim that every subcommand takes `--json` and `--locale` is false for `schema`, which accepts only `-h`/`--help`. Both sites carry it: the "Scriptable everything" bullet and the CLI section's opening sentence. Both are corrected.
  - `--on-collision`'s value domain (`error` as the default policy, `skip`, `overwrite`) is enumerated where the flag is introduced, instead of being left as a bare `<policy>` placeholder in two synopses.
  - `--locale`'s documented default (system, falling back to en) is stated.
  - The exit-code sentence gains `130` (cancelled), which `cli.rs` treats as part of the shared contract and which the README omits.
  - Anything else Step 1's table marks as a divergence is corrected in the same pass, with the table in the report as the record.

- [ ] **Step 3: correct the exact-typed-matching paragraph and write in the content anchor.** The paragraph is the "Matching is **typed, not stringly**" paragraph in the profile-sample section. Two of its claims are measurably over-broad and are corrected; the correction and the anchor land together, because the anchor's four items are exactly what makes the paragraph's absolutes true again:
  - "compares every property in its own domain" - `raw:`-prefixed properties are matched untyped, so "every" is false as written.
  - "Aiming `substring` or `regex` at a non-string property is a config-time error" - a `raw:`-prefixed property bypasses the type check entirely, and `codec_kind` is String-typed yet still rejected under a different code. So "non-string property" is neither necessary nor sufficient.
  - **The content anchor, recorded under the same ROADMAP entry, is a closed four-item list** and is written immediately after that paragraph: (1) `language` - ISO/BCP-47 normalization plus the dual-field lookup against both `language` and `language_ietf`; (2) absent boolean flags comparing equal to `false` under `exact`; (3) the curated closed domains for `type` and `codec_kind`, where an out-of-domain value is a config-time error rather than a silent never-match; (4) `raw:`'s contrast - byte-exact, single-field, no normalization, no aliasing, no false-when-absent shortcut.
  - **Verify each of the four against the spec AND the code before writing it.** The anchor is a paraphrase of an owner remark, not a specification. Name the spec section and the core symbol in the report for each item; the symbols measured at authoring are `matcher.rs::exact_matches` (its `"language"`, `"codec_kind"` and `raw:` arms), `matcher.rs::lang_eq` and `canonical_tag`, `capability::runtime::LanguageIndex::normalize`, `capability::matchable_type` and `capability::matchable_domain`, and `profile/validate.rs::validate_expr`. A claim the code does not support is reported, not written.
  - **Register:** the README is written in the owner's sell-tone, a case-scoped exception to the house writeup voice, recorded on its ROADMAP entry as "sell-tone per Şenol's register override - a case-scoped exception to the writeup-stimme rule". That entry is the whole basis for the register; no other house entry is cited for it, and in particular `latitude-carveout-presentation-tokens` is NOT invoked here - it covers visual presentation tokens and its own boundary sentence excludes anything semantic-carrying, which README prose is by construction. What keeps this out of the latitude ban is not a carve-out but the shape of the work: the SET of facts above is closed and fixed by this plan, so the implementer decides nothing and only writes the sentences that carry them.

- [ ] **Step 4: what stays untouched, stated here because a reader would look for it.**
  - The four `placeholder(1.0)` comments (GIF, dry-run output snippet, install artifact table, GUI screenshot) STAY as placeholders. The owner ruled 2026-07-29 that they may remain, since the README already states publicly that the project is in progress; two need artifacts that do not exist yet and two are his taste call. Filling one is out of scope, not helpful.
  - The work-in-progress banner STAYS. Its own text ties it to the tag, and the install-section placeholder explicitly binds its removal to that pass.
  - **Two stale counts in the same README paragraph are OUT of this task's scope and are SURFACED instead**: the "How this got built" paragraph says design decisions are numbered "D1 through D35 so far" and cites the preserved review verdicts as "all 78 of them". Measured at plan-authoring: the highest D-number in `docs/decision-ledger.yaml` is D105, and `find docs -ipath '*verdict*' -type f -name '*.md' | wc -l` returns 219. They sit in neither the CLI usage reference nor the exact-typed-matching paragraph, and the owner split this entry deliberately; a task that widens its own scope on a document he personally split is exactly the drift this plan exists to prevent. The report names them so the controller can route them.

- [ ] **Step 5: verification.** No test asserts README prose, so this task's evidence is its report: the pasted help outputs, the divergence table, and the per-claim verdicts with their named spec sections and core symbols. The full gate as `BUILDING.md` enumerates it runs green before the commit (`crates/muxsmith-cli/tests/run_live.rs` inlines the README's passthrough recipe verbatim and asserts it still runs, so a change to that recipe would turn the gate red - **this task does not touch the recipe**, and the green run is the evidence it did not).

- [ ] **Step 6: commit.**

```bash
git add README.md
git -c commit.gpgsign=false commit -m "readme: CLI reference and typed-matching paragraph against the shipped surface; the matching-magic list"
```

(Trailer per SI-4, derived from this dispatch's model parameter.)

**Must not decide:** which claims are corrected (Step 1's re-measurement decides, and every correction is reported); the four anchor items; that placeholders, the banner and the two stale "How this got built" counts stay untouched; that the passthrough recipe's YAML is not edited.

---

## Task 5: the comment line-citation sweep (W2)

Read first: the plan brief section 4 W2; Tier-2 `comments-locate-by-symbol-never-by-line-number` in full, including its handle and its SCOPE BOUNDARY sentence; `docs/ROADMAP.md`'s "Docs accuracy" stale-citation entry with its RULED block; the authoring section's corpus measurement and its OUT list. Model tier: mid.

**Files (EXHAUSTIVE)** - sixteen, from the authoring measurement of BOTH corpus expressions, to be RE-MEASURED first (Step 1):
- Modify: `crates/muxsmith-cli/tests/run_live.rs` (A)
- Modify: `crates/muxsmith-core/src/identify.rs` (A)
- Modify: `crates/muxsmith-core/src/planner.rs` (A)
- Modify: `crates/muxsmith-core/src/report/json.rs` (A)
- Modify: `crates/muxsmith-core/tests/suggestions.rs` (A and B)
- Modify: `crates/muxsmith-core/tests/profile_save.rs` (B)
- Modify: `crates/muxsmith-core/tests/ts_export.rs` (B)
- Modify: `e2e/smoke.spec.ts` (A)
- Modify: `src-tauri/src/lib.rs` (A)
- Modify: `src/components/ResolutionTable.vue` (A)
- Modify: `src/editor/fieldSpec.ts` (A)
- Modify: `src/editor/registries.ts` (B)
- Modify: `src/editor/widgets/FieldWidgetDispatcher.vue` (A)
- Modify: `src/editor/widgets/OptionalFlagWidget.vue` (A)
- Modify: `src/editor/widgets/PropertyMapWidget.vue` (A)
- Modify: `src/views/EditorView.vue` (A)

In every one of them the edit is COMMENT TEXT ONLY. Not one line of code, markup or test logic changes, which is what makes the gate's green run meaningful evidence here.

**Why expression B's four sites are IN, since the brief's derivation rule does not reach them.** Controller ruling: a bare `:1517-1535` with no filename is the WORSE form of what the owner's ruling bans, not an exempted one - it carries the same volatility with less information - and excluding it on a property of the search pattern would repeat exactly the mis-enumeration that cost the corpus its first count (correction 2). Each of the three design-document citations already names its D-entry (D44, D45, D48), so the repair is a token deletion; the fourth, `suggestions.rs:1016`, is the continuation of a citation this task rewrites anyway. The implementer does not re-derive this ruling: it is settled here.

**Interfaces:**
- Consumes: Task 4's committed `README.md`, whose final anchors three of these comments name.

- [ ] **Step 1: RE-MEASURE the corpus before editing anything, with BOTH expressions.** Line numbers inside the citations shift as this sweep edits files, and a stale corpus list is the exact defect the ruling exists to remove. Run both, and paste both commands with their full output.

  **Expression A, filename citations:**

```bash
git ls-files -- '*.rs' '*.ts' '*.vue' '*.mjs' '*.js' '*.py' \
  | xargs grep -nE '[A-Za-z0-9_./-]+\.(rs|ts|vue|mjs|js|py|toml|ftl|json|yaml|md):[0-9]+'
```

  **Expression B, bare line spans.** Run it PER FILE and strip the `file:line:` prefix before the second filter. This is not fussiness: the one-pipeline form's own prefix matches the filename pattern, so a naive `grep -v` filter excludes every line and the expression reports a clean tree that is not clean. That failure was observed at plan-authoring and is the reason the form below is prescribed rather than left to taste.

```bash
git ls-files -- '*.rs' '*.ts' '*.vue' '*.mjs' '*.js' '*.py' \
  | while read -r f; do grep -nE '(^|[[:space:]`,(])[:][0-9]+' "$f" | sed "s|^|$f:|"; done
```

  The authoring runs returned **20 lines across 13 files** (A) and **4 lines across 4 files** (B), union **24 lines across 16 files**, all enumerated in the authoring section. **If a re-measurement returns a different set, that is the ground truth and the report says so**; the Files list above is the authoring measurement, not a ceiling. A hit **outside a comment, or in a file not listed above**, returns as NEEDS_CONTEXT rather than being edited silently - the first because this task may not touch code and a code-line hit would make the green state unreachable within its own constraints, the second because the Files list is exhaustive. At authoring all 24 hits sit inside `//`, `///`, `//!` or `/* */` comments, so the green state is reachable today.

- [ ] **Step 2: rewrite each site by the ruling's own handle.** "Replace the number with the symbol the line sits in; where no symbol names it, name the nearest one plus what you mean ('the third arm of `scalar_eq`')." Naming the FILE stays normal and wanted. Two rules make this a sweep rather than a judgment call, both from the ruling:
  - **Historical citations lose their numbers too.** Several sites record where something was BEFORE a fix - `crates/muxsmith-cli/tests/run_live.rs` says "pre-fix: panics at `report/json.rs:44`" in so many words. Under the ruling those become "pre-fix: panicked in `batch_document` while building the `Set` plan value", which is both true and durable. **Nothing is classified as live-versus-historical**; the distinction disappears rather than being made.
  - **Ambiguous file references are disambiguated while you are there.** `crates/muxsmith-cli/tests/run_live.rs` says "same `Set` value, run.rs:274-275" and two tracked files are named `run.rs`; the CLI crate's `commands/run.rs` at those lines is unrelated code. Naming the symbol solves it. Where a file name survives the rewrite, it survives as a path that is unambiguous in the repo.
  - **The rewrite unit is the COMMENT, not the matched line.** A citation can continue onto a line neither expression matches: `crates/muxsmith-core/tests/suggestions.rs:1015` carries `planner.rs:1812,` and the sentence finishes at `:1016` with a bare `:1817`, which only expression B sees, and a backtick-only variant of B would see neither. Rewrite the whole comment; the two absence checks then come back empty because every matched line sits inside a comment this task rewrote, not because each matched line was patched in isolation.
  - **Bare spans lose the span and keep what they already say.** The three design-document citations name their D-entry already (`design \`:1517-1535\`` -> `design D48`; `D44 \`:592-611\`` -> `D44`; `design \`:889-936\`` -> `D45`, whose surrounding prose already names the 43-row table). Nothing is invented: the surviving text is the identifier the comment itself supplies.
  - **Read the cited code before naming its symbol.** Several citations are already stale - `crates/muxsmith-core/tests/suggestions.rs` cites `planner.rs:1812, :1817` for `delta_for`'s two exact-bearing arms, and at HEAD `delta_for` begins at `planner.rs:1820`, so neither cited line is inside it. Naming the symbol from the cited LINE would therefore name the wrong symbol. Name the symbol the comment MEANS, verified by opening the target.
  - **The three README-citing sites** in `crates/muxsmith-cli/tests/run_live.rs` name the README's own anchors - its section heading and the bullet text - as Task 4 committed them, never a line span.

- [ ] **Step 3: scope boundary, from the convention entry itself.** This touches comments in SOURCE files. It does NOT touch dated evidence citations in PROCESS artifacts - review verdicts, journal entries, ledger occurrence refs, tracker measurements - which legitimately cite a line at a named commit, because the moment is part of the claim. **No file under `docs/` is edited by this task**, and that is the boundary's whole content: it separates by the artifact DOING the citing, not by the artifact cited. A source comment pointing INTO a design document is still a source comment, which is why expression B's three design citations are swept rather than exempted (the ruling's ground is in the Files-list note above).

- [ ] **Step 4: verification.**
  - **Two absence checks, each with its own fire.** Both Step-1 commands must return NOTHING on the end state. **Fires:** each command's own Step-1 run on the pre-state IS its fire - expression A returned 20 lines, expression B returned 4 - so neither empty result can be a malformed pattern reported as a clean tree. That is not a formality here: expression B's naive one-pipeline form returns empty against a tree that has four hits, which is precisely the failure a fire catches and an unfired negative does not. **Reachable green state, argued member-by-member:** every returned line sits inside a comment this task rewrites, the enumeration unit is the comment rather than the matched line (Step 2), and the Files list is exhaustive, so no member survives.
  - The full gate as `BUILDING.md` enumerates it, foreground, green. Since this task changes comment text only, `pnpm lint`, `pnpm build`, `pnpm test:e2e` and `cargo test --workspace` are all behaviour-preserving by construction; any failure is a real finding -> NEEDS_CONTEXT.
  - `git diff --stat` covers exactly the sixteen files (or the re-measured set); anything else is a defect signal.

- [ ] **Step 5: commit.**

```bash
git add crates/muxsmith-cli/tests/run_live.rs crates/muxsmith-core/src/identify.rs crates/muxsmith-core/src/planner.rs crates/muxsmith-core/src/report/json.rs crates/muxsmith-core/tests/suggestions.rs crates/muxsmith-core/tests/profile_save.rs crates/muxsmith-core/tests/ts_export.rs e2e/smoke.spec.ts src-tauri/src/lib.rs src/components/ResolutionTable.vue src/editor/fieldSpec.ts src/editor/registries.ts src/editor/widgets/FieldWidgetDispatcher.vue src/editor/widgets/OptionalFlagWidget.vue src/editor/widgets/PropertyMapWidget.vue src/views/EditorView.vue
git -c commit.gpgsign=false commit -m "comments: locate code by symbol, never by line number (owner ruling, whole corpus)"
```

(Trailer per SI-4, derived from this dispatch's model parameter.)

**Must not decide:** the corpus (measured with both prescribed expressions, not chosen); the transformation (the convention's own handle); that historical citations lose their numbers too; that expression B's four sites are IN by controller ruling and are not re-argued at the keyboard; that no file under `docs/` and no code line changes.

---

## Plan close (controller actions, not tasks)

- **Entry condition:** Tasks 1-5 committed, working tree clean, and **the gate as `BUILDING.md` enumerates it green, foreground, no subsets** (the pre-push site). Then the single push (SI-4; `gh-log.md` entry) and the push-triggered CI run green on the head SHA, including the `ledger-lint` job, which now also carries Task 1's check.
- **Whole-branch review** by an independent reviewer on the **top tier** (`proc-03-model-assignment`), against this plan, the plan brief, the ROADMAP entries the brief cites and the spec - before any further close action.
- **Verdict-harvest mining** into the ledger: the roll-up funnel over every ledger-worthy and review-minor finding the five task reports and their reviews surfaced. Known inputs already: `BUILDING.md`'s two positional gate ordinals, which Task 1 surfaces and does not edit (Step 1(f)); the two stale "How this got built" counts Task 4 surfaces and does not edit; and whatever Task 2's four-mutation measurement establishes about which halves were guarded. The corpus's two mis-measurements - the controller's 17 and the plan's own first pass at 20 - are a harvest item in their own right: both came from a search pattern's shape rather than from counting, and the second was caught only because a control was fired.
- **Blocked-pool sweep** per the standing duty.
- **ROADMAP dispositions**, one per item this package closes: the Plan-9 anchor's D102 producer paragraph (its "STILL OPEN after the plan-9 close" and its RULED block resolve against Task 2's commit, and the disposition records WHICH halves got producers, since the measurement decides that); the "Gate-count derivation has no check" section (its NARROWED FORM is what Task 1 built; the cross-file form stays killed, with the MEASURED block as the record); the "Docs accuracy" stale-citation entry (Task 5 closes the whole class it names, and the disposition records the corrected corpus size - 24 lines across 16 files under two expressions, against the entry's recorded 17); the README entry's first bullet, the half that rode this package (its second bullet, the placeholders and banner, STAYS open for the tag pass and the disposition says so); and the Renovate entry - **which does NOT close.** Its config half lands with Task 3; its activation half is two OWNER actions and stays OPEN, and the ROADMAP trigger "Renovate/Dependabot activated -> prune deny.toml RUSTSEC ignores; TS-7 bump arrives via its PRs" stays NOT FIRED, because its condition is activation, not configuration.
- **SDD salvage** of `.superpowers/sdd/plan-10/` per the standing salvage rule, with its `diff -r` re-check and the salvaged file COUNT verified against the commit rather than asserted; the citation re-point sweep over the four house YAML files applies if any review round ledgered a plan-10 scratch basename.
- **Journal + HANDOFF snapshot** per the standing duty.
- **The completion statement this close is allowed to make, and the one it is not.** This package's completion is NOT 1.0 completeness. `owner-manual-qa-gates-the-1-0-release` binds: no completeness claim about 1.0 may be made until the owner's manual QA and bug-hunting pass has run and produced its findings, however short the ROADMAP's open list looks afterwards, and that pass's output is first-class scope input. The close records the package as executed and closed; it does not propose the tag.

## Self-review (writing-plans skill duty, run at authoring)

Coverage: all five brief work items appear in the work-item coverage map with a named task, and all eighteen acceptance halves appear in the acceptance map with a named producer or an explicit owner-observation marker - walked item by item against the brief on 2026-07-29, re-walked at fix round 1. Producer-less observables: zero; the one non-machine-verifiable row (W4-c, activation) is marked as an owner action rather than claimed. Halves: W1 is deliberately split into four rows rather than one, because one producer named for a whole observable is how a real gap survived a plan review two packages ago; W2 into two, because its two corpus expressions see disjoint forms; W3 into the statement plus its four fires, one per anchor KIND, because a check whose passing result is an absence is not evidence until it has been made to fire, and an argued red state is not a measured one. Placeholders: none - searched for TBD/TODO/"appropriate"/"similar to"; every string a task writes is fenced in this plan or, for W5's prose, is a closed fact set whose wording is explicitly delegated with its register named. Counts recomputed from their enumerations at fix round 1, each by counting its own list in this file rather than from memory: 5 tasks, 5 work items, 18 acceptance halves, 4 brief corrections, 4 D102 contract halves, 4 mutations, 4 candidate producers, 24 corpus lines across 16 files (20/13 under expression A, 4/4 under expression B, `suggestions.rs` shared), 4 `ledger-lint` self-descriptions, 4 fires in Task 1, 7 packageRules entries in Task 3's fence, 4 anchor items in Task 4, 16 files in Task 5 - whose Files list and `git add` pathspec set were verified SET-EQUAL, not merely equal in count, and every member verified tracked. **The gate's own part count, audited by a search aimed at the forms actually used:** `grep -nE 'Sum \`|[0-9]+ parts|[0-9]+-part|(one|two|three|four|five|six|seven|eight|nine|ten|eleven|twelve)-part|six parts|\`6\`|\`4\`|\`1\`\.'` over this file. The spelled-number alternation is there because `[0-9]+-part` cannot match `ten-part`, which is exactly the form the earlier, failing audit searched for - a control naming a token its expression cannot see claims coverage it does not have. **Fired per alternative, each token alone on its own line rather than bundled** (a compound control reports a match for the LINE, not for each member, so it proves only that at least one fires): `a ten-part gate` -> 1, `the eleven-part block` -> 1, `11 parts` -> 1, ``Sum `11` `` -> 1, `six parts` -> 1, ``the `6` count`` -> 1, ``the `4` count`` -> 1, ``ends in `1`.`` -> 1; negative control `the frontend checks run in order` -> 0. Companion sweep for the split form the canonical sentence uses, `grep -nE '[0-9]+ (Rust|frontend|house-knowledge)'`, returns two lines: `:182`, the fence itself, and this sentence, which matches because it quotes its own scratch line `x 6 Rust y` - the same self-reference the count below accounts for, and the fire for that sweep. Result, unchanged by the extension: **nine sites, all deliberate**, and none of them a constraint. One is the authoring measurement that produced the number; four are quotations - of `BUILDING.md`'s current heading (twice), of the brief's own wording in correction 1, and of two retired plan documents in the Rust-gate evidence line; one is Task 1's fenced deliverable, where writing the number IS the work; two are internal references back to that fence (Step 1(f) and fire F1); and the ninth is **this sentence**, which matches because it quotes its own search expression. That last one is not padding: a document auditing itself is inside its own search space, and a count that forgets to include the auditor is the same defect one turn later. **The Global Constraints' gate clause is not among them: it names `BUILDING.md` and states no count**, which is the whole of what the brief's ban protects. The earlier form of this sentence claimed a clean sweep and cited a grep for hyphenated compounds that could not match anything it audited - recorded here because a self-audit that cannot fire is worse than none, being read downstream as the check having been done. Absence checks, swept at fix round 1, with the sweep stated as an enumeration rather than a tally so a reader can check it: Task 1's four marker-existence checks share two code paths, one per anchor kind, and F3 fires the total path while F4 fires the block path on the frontend marker - chosen because F2 exercises that same block, so a mis-wired branch cannot satisfy both fires by coincidence; Task 1's continuation guard gets its own F5, since a plain appended line (F2) does not reach it; Task 5's two corpus expressions are each fired by their own pre-state run; and the authoring section's anchor-link absence is fired against a real anchor link elsewhere in the repo. Each also carries a reachable green state argued against enumerated survivors. The one negative claim this document makes about ITSELF - the gate-count audit above - was rewritten after its first form proved unfireable. Brief refutations: four, in the corrections table with pasted evidence - the frontend section states no count; the citation corpus is 20 lines, not 17; the cargo default is `auto` resolving to `update-lockfile`, not `update-lockfile` literally; and the validator's documented invocation is neither form the brief's recon started from.
