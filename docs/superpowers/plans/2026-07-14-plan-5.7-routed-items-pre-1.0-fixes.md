# Plan 5.7: routed-items pre-1.0 fixes

**Goal:** close the ROADMAP pre-1.0 gate "Correctness/security/perf review
of the idiomacy pass's 11 routed-out items" by landing the five fixes the
review + owner triage selected (2026-07-14). Provenance: the adjudication
verdict at `.superpowers/sdd/routed-items-review/verdict.md` (base commit
b96f27b); owner rulings: four-item plan approved, dry-run indent = YES,
ctrlc warning bundled in.

**Architecture:** no new subsystems. One new wire-format element
(DiagCode `NonUtf8Path`, ADR D37 in
docs/superpowers/specs/2026-07-14-plan-5.7-decisions.md). Everything else
is behavior-preserving except the two named rendered-output changes
(dry-run indent, new degradation warning).

## Coverage ledger (n-in/n-out: 11 verdict items accounted)

- 1 FIXED-BY (item 1, '|'-key, plan-5.6 T2 commit 403e573) - no action.
- 5 land in this plan: items 9 (T1), 6 (T2), 8 + 5 (T3), 2 (T4).
- 1 already tracked: item 10 (floating mise binary) - ROADMAP v1.x
  "Remove mise from CI"; nothing to add.
- 4 explicitly re-deferred v1.x with observable triggers (ROADMAP edit at
  close, T5):
  - item 3 (duplicate section comment) -> folds into ROADMAP cosmetic
    sweep group K.
  - item 4 (bare-PATH fixture spawn) -> trigger: any change moving a
    live-test gate from `Mkvmerge::locate()` to `detect()`.
  - item 7 (`/^\d+$/` strictness comment) -> trigger: any change to
    diagnostic wire params or `NUMERIC_DIAGNOSTIC_PARAMS`.
  - item 11 (double test run per CI leg) -> RE-DEFERRED past this plan's
    ci.yml touch (T1 is a 2-line permissions block, not the structural
    edit the item's investigation wants); new trigger: the v1.x
    mise-out-of-CI structural work, which rewrites the toolchain steps -
    run the one-off single-run verification (falsify the ci.yml:96-98
    interleaving rationale empirically) inside that work.

## Standing constraints (SI-1/SI-4 + doctrine, as plan 5.6)

- SDD: fresh implementer + independent reviewer per task; controller
  re-runs gates itself; controller edits no product artifact.
- House-knowledge wiring (doctrine section 7): implementer briefs conform
  to docs/product-boundaries.yaml / conventions.yaml /
  process-conventions.yaml and surface deviations; reviewer briefs treat
  the Tier-2 files as ground truth (house dimension) and harvest;
  controller is the single ledger writer.
- Nine-part gate per BUILDING.md before every commit; full gate re-run
  after every merge. Agent commits unsigned
  (`git -c commit.gpgsign=false`), trailer per convention, never
  `git add -A`.
- Spec authoritative over this plan
  (docs/superpowers/specs/2026-07-08-muxsmith-v1-design.md); T4 amends
  spec section 5.2 per D37 and sweeps for self-contradictions.
- Fluent: every new/changed message lands bilingual (en+de) in the same
  commit.
- SDD scratch: `.superpowers/sdd/plan-5.7/`; reviewer verdicts are FILES
  there at creation.
- `.worktrees/` for streams; max 3 concurrent (limit-resilience); merges
  sequential, mechanical, by the controller.

## Dependency graph / execution

```
Wave 1 (parallel worktree streams, disjoint files, cap 3):
  Stream A: T1  .github/workflows/ci.yml (permissions block)
  Stream B: T2  src-tauri/src/settings.rs (fsync)
  Stream C: T3  CLI catalog pair (dry-run indent + ctrlc warning)
  Stream D: T4  core planner NonUtf8Path guard (+ADR D37 spec row)
Close: T5 whole-branch review + funnel + ROADMAP consume/redefer +
        salvage + journal + HANDOFF + push + CI verification (T1 proof)
```

Disjointness: A touches only ci.yml; B only src-tauri/src/settings.rs;
C touches locales/{en,de}/cli.ftl, crates/muxsmith-cli/src/commands/run.rs,
crates/muxsmith-cli/tests/catalog_completeness.rs; D touches
crates/muxsmith-core (planner.rs, report/mod.rs rustdoc if needed),
locales/{en,de}/diagnostics.ftl, the diagnostics fixture test, spec 5.2,
and the D37 decisions file. C and D both touch `locales/` but different
files; no shared file anywhere.

### Task 1: ci.yml workflow permissions (Stream A) [verdict item 9]

- [ ] Add a workflow-level `permissions:` block with `contents: read` to
  `.github/workflows/ci.yml` (top level, after `name:`/`on:`). Two lines
  plus comment stating the least-privilege rationale (repo default
  verified `read` on 2026-07-14; the explicit block guards against
  out-of-band default changes and satisfies OSSF Scorecard).
- Verify: local gate parts that cover workflow lint (if any) + YAML
  well-formedness; the on-runner proof is the plan-close CI run (T5).
  Reviewer question: do rust-cache / mise-action / all steps need more
  than contents:read? (Adjudicated no in the verdict - re-check.)

### Task 2: settings.rs fsync before atomic rename (Stream B) [item 6]

- [ ] In `src-tauri/src/settings.rs` (save(), body at ~:169-173): replace
  `fs::write(&tmp_path, &bytes)` with `File::create` + `write_all` +
  `sync_all()`, all mapped to `SettingsError::Io` exactly like the
  current io mapping. Keep the rename; keep the rustdoc claim (it becomes
  true as written). NO directory fsync (outside the doc's claim); NO
  joblog changes (separately tracked v1.x).
- Verify: existing settings tests pass unchanged; foreground
  `cargo test -p muxsmith-gui` (or the src-tauri test target per
  BUILDING.md) plus the Rust gate parts.

### Task 3: CLI catalog pair - dry-run indent + ctrlc warning (Stream C) [items 8 + 5]

- [ ] Dry-run indent (owner ruling: indent YES): in `locales/en/cli.ftl`
  (:20-21) and `locales/de/cli.ftl` (:25-26), turn the parser-stripped
  leading spaces of `dry-run-assignment` and `dry-run-output` into
  `{" "}` string-literal placeables (two spaces rendered). Verified
  upstream: no snapshot pins these lines; update any test that asserts
  the rendered lines if one surfaces.
- [ ] ctrlc registration warning: in
  `crates/muxsmith-cli/src/commands/run.rs` (:220), replace
  `let _ = ctrlc::set_handler(...)` with handling that, on `Err`, renders
  a new bilingual key `run-signal-handler-unavailable` via
  `renderer.msg(...)` - the established degradation-warning shape of
  `run-joblog-unavailable` (:332, doc at :311-314). Message content: D16
  cleanup semantics unavailable (no graceful abort/partial-output
  cleanup); the run continues. Register the key in
  `tests/catalog_completeness.rs` (allowlist + fixture arm like its
  siblings).
- Verify: foreground `cargo test -p muxsmith-cli` + i18n gate part
  (`pnpm check:i18n` covers frontend; the CLI catalog completeness test
  covers this pair) + full Rust gate parts.

### Task 4: NonUtf8Path guard at plan finalize (Stream D) [item 2, ADR D37]

- [ ] Implement D37: in the core planner's finalize pass, for every
  argv-bound path (output path, donor/source paths - every value that
  reaches `crates/muxsmith-core/src/command.rs` argv via
  `Path::display().to_string()`, incl. push_global/push_group inputs),
  check `to_str()`; on failure emit error-severity
  `DiagCode::NonUtf8Path` (params: lossy path rendering + role) once per
  offending file and drop that plan (pattern-clone of the core-31
  rendered-filename invariant; consistency precedent: the identify path
  already rejects non-UTF-8 at runtime.rs:206 with a Parse error).
- [ ] Catalog: `locales/en/diagnostics.ftl` + `locales/de/diagnostics.ftl`
  entries; fixture coverage in the diagnostics fixture test
  (fixture_args / C1 pattern); exhaustive-match guard will force the new
  arm - follow it.
- [ ] Spec: add the section-5.2 diagnostics-table row per D37; sweep the
  spec for contradictions with the new row (D20 passthrough semantics,
  section 9.2 forward-compat language).
- [ ] Test: one Unix-only integration test building a non-UTF-8 path via
  `std::os::unix::ffi::OsStrExt::from_bytes`, asserting the diagnostic
  (code, severity, params) and that no job is built for that file;
  cfg-gated at file level like executor_no_hang_live.rs.
- Verify: foreground `cargo test -p muxsmith-core` + full Rust gate parts
  + catalog gates.

### Task 5: plan close (controller + whole-branch reviewer)

- [ ] Whole-branch review on the strongest model (all four streams merged;
  ground truth: spec + Tier-2 files + this plan; explicit check of the
  two intended output changes and nothing-else-changed).
- [ ] Roll-up funnel over all task-review minors (fixed / promoted /
  recorded / discarded, n-in/n-out).
- [ ] ROADMAP edit: mark the routed-items gate resolved (pointer to the
  verdict + this plan); write the four re-deferrals with their triggers
  (coverage ledger above).
- [ ] Ledger harvest (doctrine section 7) from reviewer reports.
- [ ] Salvage `.superpowers/sdd/plan-5.7/` + the routed-items-review dir
  to journal artifacts (verify count IN THE COMMIT); journal entry per
  PROMPT.md; HANDOFF superseded + snapshot (SI-5).
- [ ] Push (pure git shape per SI-4); verify the CI run green on all
  three legs = T1's on-runner proof; log push in gh-log.md.
