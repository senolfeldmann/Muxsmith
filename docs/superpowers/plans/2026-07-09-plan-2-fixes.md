# Plan 2 fix pass

> Execute via superpowers subagent-driven-development: fresh implementer subagent per task (TDD), independent reviewer subagent per task, controller verifies + commits. NOT inline. Derived from the independent review (docs/process-journal/artifacts/plan-2-review/independent-review-2026-07-09.md) and Şenol's decisions (2026-07-09).

**Ground truth:** the v1 spec (amended 2026-07-09 for the three decisions) and this doc. Global constraints from the Plan 2 doc still hold (prose-free core, deny(missing_docs), ASCII, fmt+clippy+deny+test all green per commit, trailer `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`, GPG off).

**Sequencing:** F5/F6/F7 all edit planner.rs, so run tasks sequentially (no parallel implementers on the same file). F2 before F3 and F5 (they emit its new codes).

---

### F2: new diagnostic codes (do first)
- Files: crates/muxsmith-core/src/report.rs, locales/en/diagnostics.ftl
- Add `EmptyMatchList => "empty-match-list"` (config-time) and `UnidentifiableSource => "unidentifiable-source"` (planning) to the `diag_codes!` macro with real rustdoc, and their Fluent messages. UnidentifiableSource message references `{ $detail }` (carries the underlying error).
- Test: extend the report.rs key/ALL test; the CLI catalog guard must stay green.

### F1: dry-run runs validate + emits rendered JSON (bugs A, F)
- Files: crates/muxsmith-cli/src/commands/dry_run.rs (mirror validate.rs's collect+render)
- dry-run MUST run `profile::validate::validate` + `lint::provable_overlaps` and fold those diagnostics into the report BEFORE/alongside planning (spec 5.5: dry-run is a strict superset of validate). `--json` must attach the rendered message per diagnostic (as validate.rs does with `v["rendered"]`), for both batch and per-file diagnostics.
- Tests (crates/muxsmith-cli/tests/dry_run_cli.rs, mkvmerge-gated or a unit-level split): a profile with a broken `input.pattern` regex yields an `InvalidRegex` diagnostic and exit 2 (NOT empty + exit 0); `--json` output carries a rendered message string per diagnostic.

### F3: validate rejects empty any/not (decision #2 -> EmptyMatchList)
- Files: crates/muxsmith-core/src/profile/validate.rs (in validate_expr recursion)
- A present-but-empty `any: []` or `not: []` emits `EmptyMatchList` (error) at its config_path. Non-empty and omitted are unaffected.
- Tests: tests/validate_hardening.rs -> `any: []` and `not: []` each produce EmptyMatchList; a populated `any`/`not` does not.

### F4: matcher absent-boolean-is-false (decision #1, mirror mkvmerge)
- Files: crates/muxsmith-core/src/matcher.rs (exact_matches fallback), uses capability::matchable_type
- For `exact`, when a property is absent from the track AND capability types it Boolean, compare against `false` (so `exact:{flag:false}` matches a track lacking the flag; `exact:{flag:true}` still does not). Non-boolean absent properties stay no-match. language/codec_kind special cases unchanged.
- Tests: matcher.rs -> exact flag_hearing_impaired:false matches a track without the property; :true does not; a non-boolean absent property still fails.

### F5: planner resolution - SourceOverwrite + identify-failure code (bugs H, K, #5)
- Files: crates/muxsmith-core/src/planner.rs (resolve_file)
- Collect ALL input paths for the batch = primaries + every resolved donor; SourceOverwrite fires if a rendered output equals any of them (currently only primaries). Remove the redundant `out == &primary.path` clause.
- On identify failure (primary or donor) emit `UnidentifiableSource` (not MissingTrack/MissingExternal), carrying the real IdentifyError text via `detail` (third-party pass-through, allowed). A present-but-unidentifiable file is a HARD error regardless of `rule.optional` (optional means "zero matches is acceptable", not "a broken file is acceptable"; spec 5.2 UnidentifiableSource is unconditional error) - decided in the F5 dispatch, superseding an earlier draft line here that said it should respect optional.
- Tests (tests/planner_resolution.rs): a fake Identify that errors -> UnidentifiableSource, plan None; an output path equal to a donor path -> SourceOverwrite.

### F6: planner output + collision (bug B, decision #3, bug E)
- Files: crates/muxsmith-core/src/planner.rs (render_output, detect_output_collisions, finalize_plans)
- render_output: the empty/`.`/`..` check must test the pre-`.mkv` stem, not `name` after append (dead branch today). A template rendering to `.`, `..`, or empty -> EmptyRenderedName.
- detect_output_collisions (per amended spec 4.8): two plans -> same path is ALWAYS OutputCollision error (drop both plans), regardless of policy. On-disk pre-existing file: error -> error+drop, skip -> warning + DROP the plan (skip currently a no-op), overwrite -> info + keep the plan. Prefer a `Diagnostic::with_severity` builder over mutating the public field.
- Tests: template "." -> EmptyRenderedName; two files rendering to one path -> OutputCollision error, both plan None; on-disk collision under skip -> warning + plan None; under overwrite -> info + plan Some.

### F7: suggestion engine - no-clobber, valid YAML, cap logging (bugs C, D, D6 nit)
- Files: crates/muxsmith-core/src/planner.rs (with_rule_match, yaml_fragment, suggest)
- with_rule_match must NOT overwrite an existing exact/substring key (use or_insert semantics); a candidate that would clobber becomes a no-op and is then rejected by the acceptance simulation (never widens). `not` list append is fine.
- yaml_fragment must emit valid YAML for any value (a track_name with `:`/`,`/`{`): serialize the delta via yaml_serde, or quote+escape. Round-trip test.
- Log the cap-3 truncation (D6): when >3 candidates were accepted, emit a note (a diagnostic or a logged field) recording how many were dropped; do not truncate silently.
- Tests (tests/suggestions.rs): a rule already `substring:{track_name:X}` never gets that widened; a fixture track_name containing a colon produces a yaml_fragment that parses back to the intended edit.

### F8: discovery symlink handling (bug I)
- Files: crates/muxsmith-core/src/discovery.rs (walk_files)
- A symlink whose target is a regular file must be discovered (currently dropped: symlink is neither is_file nor is_dir under symlink_metadata). Resolve symlink targets with fs::metadata to classify; include file targets; do NOT recurse into directory symlinks (cycle guard).
- Tests: a symlinked .mkv in the source dir is found (or, minimally, is not silently dropped without any diagnostic).

### F9: human renderer file attribution + skew message (bugs J, G)
- Files: crates/muxsmith-cli/src/i18n.rs, locales/en/cli.ftl, locales/en/diagnostics.ftl
- Renderer::diagnostic must include `d.file` when present (add a `$file` to a diagnostic-line variant, or prefix it) so batch-level diagnostics (IgnoredFile, DuplicateIdentifier) are attributed.
- unknown-property-skew message must reference only params the emitter supplies (`$version`), not `$property`. Reword to describe the file/version.
- Tests: a rendered batch diagnostic carrying a file shows it; the skew message renders without a literal `{$property}`.

### Nits (opportunistic, not blocking)
OverlappingRules names only first two of >=3 claimants; lint (`0`) vs planner (`tracks[0]`) rule-ref formatting; regex recompiled per matches() call; candidate gen ignores top-level codec/id; identify --json subset of human; proptest coverage (spec 10); LanguageIndex row-collision + separator empty-name guards.

## After all tasks
- Whole-branch review (independent), CI green, journal entry, then supersede HANDOFF (carrying the STANDING INSTRUCTION forward).
