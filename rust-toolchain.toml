[toolchain]
# Pinned (Şenol 2026-07-10, pin-everything policy): bumps are deliberate
# PRs; a floating stable can break the -D warnings gate on new lints.
channel = "1.96.1"
components = ["rustfmt", "clippy"]
